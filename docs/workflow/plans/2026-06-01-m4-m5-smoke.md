# M4 + M5 冒烟测试清单

> **目的**: `feature/m4-terminal` → `master` 合并前的人工验证
> **预计时间**: 30-40 分钟
> **前提**: 装好 Pi CLI, Node 22.19+, Windows 10/11

---

## 0. 环境准备 (5 min)

```powershell
# 在仓库根
cd C:\Ai\pi-desktop
git checkout feature/m4-terminal
pnpm install
```

**验证**:
- [ ] `pnpm -r test` 输出 `Test Files  17 passed (17) | Tests  107 passed | 2 skipped (109)`
- [ ] `pnpm --filter @pi-desktop/desktop typecheck` 0 error (排除 M1-M4 范围外的 stale `index.ts:10-13` pre-existing 问题)
- [ ] 装 SkillHub CLI (可选): `where.exe skillhub` 应当返回路径

---

## 1. M1 基础 — 3 个核心 bug (10 min)

启动应用: `pnpm --filter @pi-desktop/desktop dev`

### 1.1 cwd 修复
- [ ] 添加一个 workspace (例如 `C:\tmp\pi-test`), 选一个子目录
- [ ] 在 chat 框输入: `pwd`
- [ ] **预期**: Pi 回答包含 workspace 实际路径 (不是 `process.cwd()` 也不是 Electron 进程目录)
- [ ] **验证**: `Get-Content <workspace>/.pi/pid` (如果 Pi 创建的话) 显示 pi 在该 workspace 跑

### 1.2 长连接 Pi
- [ ] 在 workspace 跑 2-3 轮对话, 第 1 句用 `pwd`, 第 2 句用 `ls`
- [ ] **预期**: 第 2 句响应比第 1 句快 (无冷启动, session 保持)
- [ ] **验证**: 关闭对话 tab 5 秒内, 内存不暴涨 (electron task manager 看)

### 1.3 分层审批
- [ ] 输入 `rm -rf /tmp/foo` (或 Pi 视为 high-risk 的命令)
- [ ] **预期**: 弹模态确认框, 拒绝后 Pi 回答"已拒绝" + 不执行
- [ ] **验证**: 第 2 句输入 `cat package.json`, 应当无模态 (read 类放行)
- [ ] **验证**: 让 Pi 改一个文件, 改完后应当出现 diff+undo UI (file_edit 类延迟审批)

---

## 2. M2 上下文 (8 min)

### 2.1 @ 文件引用
- [ ] chat 框输入 `@pa` (假设有 `package.json`)
- [ ] **预期**: 浮窗出现 fuzzy 排序结果, 键盘上下键可选, Enter 插入完整路径
- [ ] **验证**: chip 显示文件名, 发出去后 Pi 看到完整路径

### 2.2 图片粘贴
- [ ] 复制一个截图到剪贴板, 在 chat 框按 Ctrl+V
- [ ] **预期**: 出现图片 chip, 缩略图 + 文件名
- [ ] **验证**: 发出去后 Pi 能描述图片内容 (需 vision-capable model)

### 2.3 Ctrl+K Command Palette
- [ ] 任意位置按 Ctrl+K
- [ ] **预期**: 模态弹出, 3 个 tab (文件/历史/命令), 键盘完全可控
- [ ] **验证**: 输入文件名 fuzzy 过滤, Enter 跳转或执行
- [ ] **验证**: Esc 关闭

---

## 3. M3 Skills (5 min, 需装 SkillHub)

### 3.1 市场 tab
- [ ] 切到 Skills 面板, 默认 "市场" tab
- [ ] 搜 `hello`
- [ ] **预期**: 卡片出现, 显示 slug / name / description / version / source

### 3.2 我的 tab
- [ ] 装一个技能 (例 `hello-world`)
- [ ] 切到 "我的" tab
- [ ] **预期**: 状态点变绿, 显示已装, 启/禁/卸载按钮可用

### 3.3 + 创建
- [ ] 点 + 按钮
- [ ] **预期**: 3 选项下拉 (用 Pi 构建 / 编写 / 从 GitHub)

---

## 4. M4 Terminal (5 min)

### 4.1 多 tab
- [ ] 打开 Terminal 面板
- [ ] 点 + 新建 tab, 至少建 3 个
- [ ] **预期**: tab 行出现 3 个 tab, 切换 tab 各自独立
- [ ] **验证**: 关中间一个, 左右 tab 切换正常

### 4.2 真 PTY (修旧 bug)
- [ ] 跑一个会产生长输出的命令: `node -e "for(let i=0;i<200;i++) console.log(i)"`
- [ ] **预期**: 完整输出 200 行, 无丢行
- [ ] **跑 TUI 应用** (需 git bash 或 WSL): `htop` 或 `vim`
- [ ] **预期**: 完整 ANSI 颜色 + 鼠标响应 + resize 正常
- [ ] **验证**: 拖窗口改尺寸, htop/vim 立即跟随 (旧 child_process.spawn 做不到)

---

## 5. M5 工程卫生 (5 min)

### 5.1 auto-updater
- [ ] 装一份 packaged 版本: `pnpm --filter @pi-desktop/desktop dist:win`
- [ ] 装装好的 exe 启动
- [ ] **预期**: 控制台无 "update available" (因为没发新版)
- [ ] 验证逻辑: 主进程看 `services/updater.ts` 初始化逻辑跑通 (日志)

### 5.2 CI/CD
- [ ] 在 GitHub 上 push 一次 commit
- [ ] **预期**: Actions 跑 `ci.yml` (lint + typecheck + test + build), 都绿
- [ ] 创建 tag `git tag v0.0.1 && git push --tags`
- [ ] **预期**: `release.yml` 跑 (装 + 上传 + Release), artifact 是 `.exe` NSIS installer

### 5.3 文档
- [ ] 打开 `README.md` 中文版, 检查:
  - [ ] 安装步骤可跟
  - [ ] 架构图清晰
  - [ ] Roadmap 列了 v1.1
- [ ] 打开 `CONTRIBUTING.md` 检查 workflow
- [ ] 打开 `CHANGELOG.md` 检查 M1-M5 都记录了

### 5.4 ErrorBoundary
- [ ] 在 `App.tsx` 临时包一层 `<ErrorBoundary>` 包裹 ChatPanel
- [ ] 故意抛错 (`throw new Error("test")` 在某个组件里)
- [ ] **预期**: 红屏 fallback 出现, 不是白屏/全挂

---

## 6. 跨场景 (5 min)

### 6.1 重启 + 状态恢复
- [ ] 跑几轮对话, 关 app, 重开
- [ ] **预期**: workspace 列表 + chat 历史 + skills 状态都恢复

### 6.2 大 workspace
- [ ] 选一个 `node_modules` 大的项目当 workspace
- [ ] 跑 chat + terminal
- [ ] **预期**: 不卡死, 启动时间 < 3s (M2 跳了 `node_modules` / `.git`)

### 6.3 错误兜底
- [ ] 删掉 Pi CLI (临时: `move C:\Users\48818\.local\bin\pi.exe pi.exe.bak`)
- [ ] 启动 app
- [ ] **预期**: 启动不挂, workspace 添加时给清晰错误
- [ ] 恢复: `move pi.exe.bak pi.exe`

---

## 通过标准

- [ ] M1-M4 所有 M 章节的 checkbox 都打了
- [ ] 没有 blocker 级问题
- [ ] 已知限制 (UI 集成 / macOS) 不在冒烟范围

## 已知限制 (不在冒烟范围)

- App.tsx 还没把所有组件挂上 — 用户在主对话外做
- macOS / Linux 不支持 (v1.0 Windows only)
- 真正的 Pi 扩展 (替代 M1 interceptor) 是 v1.1 升级

---

**完成时间**: ____
**签字**: ____
