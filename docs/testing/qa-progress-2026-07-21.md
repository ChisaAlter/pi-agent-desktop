# QA Progress — 2026-07-21

目标：代码级审查 + 功能穷举测试 + 使用体验优化（持续迭代，非一次性完成）。

## 本轮门禁

| 门禁 | 结果 |
|---|---|
| `pnpm -r typecheck` | PASS（desktop typecheck 在本波修复后复验通过） |
| `pnpm -r lint` | PASS（前一轮） |
| `pnpm -r test` | PASS — **190 files / 2121+ tests**；本波定点 sqlite/RightRail/git/settings-window 等单测通过 |
| 定点 Electron E2E（post-fix build，早波） | **PASS — 18/18**（compose/history/m2/m5/main-token/terminal/overlay 集群） |
| **全量 Electron E2E（wave-6，e2e:build）** | **116 passed / 1 failed / 0 flaky / 6 skipped**（**28.7m**；见下方 scoreboard） |
| **wave-6 残差定点** | `command-palette-callbacks` **7/7 PASS**（selector 修复后；修闭 full-suite 唯一 fail） |
| 早波 residual（visual + auth + settings） | **8 passed / 1 flaky（generated-ui retry pass）**；visual-audit 全绿 4/4 + 单独 5/5 |
| P0 闭环补强 | unit 4 files / 59 tests PASS；E2E `status-onboarding` 4/4 + `session-resume` PASS（B-002/D-002/D-003/H-002/K-016） |
| PARTIAL 边界波 | unit 6 files / 81 tests PASS；fetchModels/skills toggle/pi install IPC/codex bad fixture/git auth-fail/log Cookie；DPI host smoke E2E |
| PARTIAL 波 3 | Markdown XSS / workbench set-active-file / slash edges / skills network fail unit 55 PASS；tray quitApp A-008 smoke E2E |
| PARTIAL 波 4 | Monaco readOnly/save/language/long-text unit；Terminal multi-tab isolation unit；theme data-theme contrast unit；AboutTab/updater download progress+fail unit + existing updater E2E（47 tests PASS） |
| residual 波 5 | tray quit + child windows E2E；PtyManager closeAll kill-all unit；force-device-scale-factor 1/1.25/1.5/2 E2E；K-013 structured perf JSON；NSIS artifact inventory（无安装）11/11 E2E PASS |
| 矩阵 174 穷举 | **未**声称完成；**PASS 100 / UNIT 71 / PARTIAL 0 / BLOCKED 3 / NOT_RUN 0**；I-012 wave9b live **PASS**（模型写 plan_probe）；NSIS 装机仍 BLOCKED |
| wave-7 live 门禁 | `RUN_DEEP_INTERACTIVE=1` + `PI_DESKTOP_DEEP_USE_CURRENT_PROVIDER=1` → **deep-interactive 5/5 PASS**（minimax/MiniMax-M3）；long-horizon 初版 settings+Build 绿、Plan hang |
| wave-8 long-horizon | `RUN_LONG_HORIZON_ACCEPTANCE=1` → **1 passed / 52.9s**（`/tmp/e2e-live-i012-wave8g-*.log`）：settings+Build+Plan 卡+执行 UI+Run 任务/记忆+Compose+disable 门禁；当时 **plan_probe seed** → 曾 PARTIAL |
| wave-9 plan→build 产品修 | `registry.withPlanToBuildReminder` 在 plan→build 出站注入 `BUILD_SWITCH`；registry unit 断言更新；spec 去掉 seed，要求模型写 `plan_probe.txt` |
| wave-9b long-horizon | `RUN_LONG_HORIZON_ACCEPTANCE=1` → **1 passed / 35.0s**（`/tmp/e2e-live-i012-wave9b-20260721-065806.log`）：**plan source=model**；**plan_probe 模型写出 PLAN_OK（no seed）** → I-012 **PASS**（`/tmp/live-i012-wave9b-evidence.txt`） |
| wave-7/10 NSIS 隔离门禁 | 非 admin、无 WindowsSandbox.exe、无 Docker、主机已装 Pi Desktop 1.0.13 → **禁止** host 装机；K-004/K-014/K-015 **BLOCKED**（`/tmp/nsis-isolation-gate-wave10.txt` + static inventory + runbook） |
| wave-10 NSIS 静态证据 | setup 1.0.11/12/13 尺寸+SHA256；`latest.yml`↔1.0.13；inventory E2E 加强（head hash + size match）**1/1 PASS**；runbook `docs/testing/nsis-isolation-runbook.md` — **仍非装机 PASS** |
| wave-10 BUILD_SWITCH 统一 | `buildAgentModePrompt` 支持 `previousMode` 注入 BUILD_SWITCH；registry 去掉重复 wrapper；chat.ipc 捕获 previousMode 供 legacy path；unit **89/89**（agent-modes+registry+insert-reminders） |
| wave-11 NSIS 隔离就绪包 | probe `scripts/nsis-isolation-probe.ps1`；smoke `scripts/nsis-sandbox-smoke.ps1`（host 有装机 **exit 2 REFUSE** 已验证）；`docs/testing/PiDesktop-NSIS.wsb`；unit residual **144+40** 绿 — **装机仍未执行 / 仍 BLOCKED** |
| wave-11 typecheck | desktop `tsc --noEmit` 修复 test-only 错误：`useInputShortcuts.test` satisfies 类型、`ChatInput.test` onSend 断言；typecheck **PASS** |
| wave-12 NSIS honesty E2E | `e2e/nsis-isolation-gate.spec.ts` **3/3 PASS**（ready-pack + probe + host refuse exit 2）；BOM-safe JSON；unit plan/task/files/memory **212 PASS**；security **130 PASS**；agent-mode-store **5 PASS** — **装机仍 BLOCKED**（`/tmp/nsis-wave12-evidence.txt`） |
| wave-13 residual | 隔离仍 BLOCKED（enable Sandbox 需提升失败）；E2E residual **10/10**（nsis gate+inventory+session-resume+status-onboarding+updater）；unit residual **85 PASS**；`buildPlanExecutionPrompt` 抽出 + unit **5 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED** |
| wave-14 residual | E2E **21/21 PASS**（palette 7 + nsis 4 + tray 3 + window/DPI 7）；unit plan/task/registry/agent-modes/prompt/store **100 PASS**；NSIS 仍 **BLOCKED**（`/tmp/e2e-residual-wave14.log`） |
| wave-15 residual | 隔离仍 BLOCKED；E2E **12/12**（a11y/overlay/settings-interactions/pi-config-sync）+ **16/16**（smoke/file-git/terminal-tools）；unit security/IPC **184 PASS**；NSIS **仍 BLOCKED**（`/tmp/wave15-evidence.txt`） |
| wave-16 residual | 隔离 re-probe **BLOCKED**（admin=False、WSB missing、host 1.0.13）；`plan-utils` unit **16 PASS** + `useInputShortcuts` expand **9 PASS** + plan-execution **5 PASS**；E2E residual **9/9** + **12**（chat-view/permission/notification；1 flaky retry PASS）；security/window/files unit **108 PASS**；desktop typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/e2e-residual-wave16*.log` + `/tmp/nsis-probe-wave16.txt`） |
| wave-17 residual | 隔离 re-probe **BLOCKED**；新增 unit：fuzzy-match/project-scripts/subscription-manager/run-control/runtime-feature-store/first-launch/toast-store **45 PASS**；E2E residual **12/12**（session-center/history/resume/bound-agent + draft-clear + nsis gate）；typecheck **PASS**；stale Electron 清障后复跑 — **K-004/014/015 仍 BLOCKED**（`/tmp/e2e-residual-wave17.log` + `/tmp/wave17-evidence.txt`） |
| wave-18 residual | 隔离 re-probe **BLOCKED**；新增 unit：path-canonical + attachments/permission/skills store **26 PASS**；E2E residual **16/16**（launch/layout/motion/m2/visual-audit/settings-persistence/skills + nsis gate）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/e2e-residual-wave18.log` + `/tmp/wave18-evidence.txt`） |
| wave-19 residual | 隔离 re-probe **BLOCKED**；新增 unit：ipc/sounds/useFocusTrap/useMotionPresence **14 PASS**；E2E residual **12/12**（core-workflow/continuous-window-sidebar/workspace-persistence/right-rail-git/main-token/generated-ui + nsis gate）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/e2e-residual-wave19.log` + `/tmp/wave19-evidence.txt`） |
| wave-20 residual | 隔离 re-probe **BLOCKED**；新增 unit：useCommandPalette/useSession **7 PASS**；E2E residual **12/12**（compose-runtime/m5/m6/programmer/settings-redesign-v2/third-party-model + nsis gate）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/e2e-residual-wave20.log` + `/tmp/wave20-evidence.txt`） |
| wave-21 residual | 隔离 re-probe **BLOCKED**；新增 unit：useMentions **5**（cluster **12 PASS**）；E2E residual **10/10**（deep-use×4/continuous-f06-f10/provider-error-real + nsis gate）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/e2e-residual-wave21.log` + `/tmp/wave21-evidence.txt`） |
| wave-22 residual | 隔离 re-probe **BLOCKED**；新增 unit：file-workspace-utils **12** + tool-call-normalization **5**（**17 PASS**）；E2E residual **18/18**（smoke/draft-clear/overlay/running-control/plan-mode-smoke/status-onboarding + nsis gate）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/e2e-residual-wave22.log` + `/tmp/wave22-evidence.txt`） |
| wave-23 residual | 隔离 re-probe **BLOCKED**；新增 unit：sdk-runtime **4 PASS**；E2E residual **26/26**（a11y/chat-view/command-palette/file-and-git/notification/permission-enforcement + nsis gate）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/e2e-residual-wave23.log` + `/tmp/wave23-evidence.txt`） |
| wave-24 residual | 隔离 re-probe 仍 **BLOCKED**（nsis gate 3/3）；E2E residual **27/27**（permission-more/plan-mode-current-ui/session-center/session-history/session-resume/settings-interactions/window-geometry + nsis gate）；**K-004/014/015 仍 BLOCKED**（`/tmp/e2e-residual-wave24.log` + `/tmp/wave24-evidence.txt`） |
| wave-25 residual | 隔离 re-probe 仍 **BLOCKED**（nsis gate 3/3）；E2E residual **16/16**（session-bound-agent/settings-pi-config-sync/terminal-and-tools/tray-lifecycle/updater + nsis gate）；**K-004/014/015 仍 BLOCKED**（`/tmp/e2e-residual-wave25.log` + `/tmp/wave25-evidence.txt`） |
| wave-26 residual | 隔离仍 **BLOCKED**；新增 unit：permission tool-category/wildcard/evaluate + mutation-queue + IpcError + tab-defs + diff-parser **29 PASS**；UX 定点：`parseDiff` 正确识别 `/dev/null` 新建/删除；E2E residual **6/6**（full-demo/interactive-demo/nsis-artifact-inventory + nsis gate）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/e2e-residual-wave26.log` + `/tmp/wave26-evidence.txt`） |
| wave-27 residual | 隔离仍 **BLOCKED**（nsis gate 3/3）；E2E residual **4/4**（continuous-acceptance.generated + nsis gate）；非 gated 自动化 E2E residual 基本覆盖完毕；**K-004/014/015 仍 BLOCKED**（`/tmp/e2e-residual-wave27.log` + `/tmp/wave27-evidence.txt`） |
| wave-28 residual | 隔离 re-probe **BLOCKED**；live 门控 E2E：**deep-interactive 5/5** + **long-horizon-live 1/1** （host minimax/MiniMax-M3；`RUN_DEEP_INTERACTIVE=1` + `RUN_LONG_HORIZON_ACCEPTANCE=1`）；**K-004/014/015 仍 BLOCKED**（`/tmp/e2e-deep-interactive-wave28.log` + `/tmp/e2e-long-horizon-wave28.log` + `/tmp/wave28-evidence.txt`） |
| wave-29 residual | 隔离仍 **BLOCKED**；unit permission defaults **3** + evaluate/tool-category/wildcard/diff-parser regression **23 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave29a.log` + `/tmp/wave29-evidence.txt`） |
| wave-30 residual | 隔离 re-probe **BLOCKED**；新增 unit：spawn-handler **11 PASS** （formatSubagentSummary + buildSubagentCustomTools）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave30a.log` + `/tmp/wave30-evidence.txt`） |
| wave-31 residual | 隔离仍 **BLOCKED**（nsis gate+inventory 4/4 honesty）；新增 unit：session-summary-tools **4** + asset-inventory-tools **4** （cluster **19 PASS** w/ spawn-handler）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave31a.log` + `/tmp/e2e-nsis-wave31.log` + `/tmp/wave31-evidence.txt`） |
| wave-32 residual | 隔离 re-probe **BLOCKED**；新增 unit：useLatestRequest/useTransientState/useDebouncedSave **9 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave32a.log` + `/tmp/wave32-evidence.txt`） |
| wave-33 residual | 隔离 re-probe **BLOCKED**；新增 unit：usePrefillConsumer/useInputText/useRenderedPlanCardIds **8 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave33a.log` + `/tmp/wave33-evidence.txt`） |
| wave-34 residual | 隔离 re-probe **BLOCKED**；新增 unit：useShortcuts **4** + contentWithGeneratedUiText（generated-ui suite **4**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave34a.log` + `/tmp/wave34-evidence.txt`） |
| wave-35 residual | 隔离 re-probe **BLOCKED**；新增 unit：ipc helpers **7** + packages.ipc **6** + usePlanSyncEffect pure **5** + settings-nav-metadata **4**（**22 PASS**）；export plan-sync pure helpers；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave35a.log` + `/tmp/wave35-evidence.txt`） |
| wave-36 residual | 隔离 re-probe **BLOCKED**；nsis honesty E2E **4/4**；新增 unit：list-local-skills **4** + desktop-overlay.ipc **1** + pi-api **2** + claude/codex sessions.ipc **2**（**9 PASS**）；cluster helpers/packages **22 PASS**；security/memory refresh **PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/e2e-nsis-wave36.log` + `/tmp/unit-wave36b.log` + `/tmp/wave36-evidence.txt`） |
| wave-37 residual | 隔离仍 **BLOCKED**；shared-types ipc-guards **4 PASS**（isSettingsWindowTab + ipcError/isIpcError）；shared-types suite **47 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-shared-wave37.log` + `/tmp/wave37-evidence.txt`） |
| wave-38 residual | 隔离仍 **BLOCKED**；UX：ResizablePanel separator a11y + SkillCard focus/type；unit ResizablePanel **3** + RunPanel **1** + SkillCard **3**（**7 PASS**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave38a.log` + `/tmp/wave38-evidence.txt`） |
| wave-39 residual | 隔离仍 **BLOCKED**；UX：FileChangeItem a11y（aria-expanded/label/focus）+ formatTimeAgo testability；unit FileChangeItem **3** + RecentWorkspaces **3**（cluster **13 PASS** w/ wave-38）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave39a.log` + `/tmp/wave39-evidence.txt`） |
| wave-40 residual | 隔离仍 **BLOCKED**；unit TopTabBar **2** + UsageStatsPanel **1**（**3 PASS**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave40a.log` + `/tmp/wave40-evidence.txt`） |
| wave-41 residual | 隔离 re-probe **BLOCKED**；UX：SkillCreateDropdown menu a11y；unit SkillCreateDropdown **2 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave41a.log` + `/tmp/wave41-evidence.txt`） |
| wave-42 residual | 隔离仍 **BLOCKED**；UX：Settings ModelSelector button a11y；unit ModelSelector **2** + AppearanceTab **2** + PermissionsTab **1**（**5 PASS**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave42a.log` + `/tmp/wave42-evidence.txt`） |
| wave-43 residual | 隔离仍 **BLOCKED**；unit MemoryPanel **3 PASS**（recordMeta + disabled + recent）；export recordMeta；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave43a.log` + `/tmp/wave43-evidence.txt`） |
| wave-44 residual | 隔离仍 **BLOCKED**；export TaskOverviewPanel pure mappers；UX：ChangeApprovalCard a11y；unit pure **5** + ChangeApprovalCard **3**（**8 PASS**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave44b.log` + `/tmp/wave44-evidence.txt`） |
| wave-45 residual | 隔离仍 **BLOCKED**；unit ApprovalPanel **3** + LongHorizonTab **3**；UX ApprovalPanel bulk focus-visible；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave45c.log` + `/tmp/wave45-evidence.txt`） |
| wave-46 residual | 隔离仍 **BLOCKED**；unit theme **7** + directives-pure **6** + judge-prompt **4**（**17 PASS**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave46c.log` + `/tmp/wave46-evidence.txt`） |
| wave-47 residual | 隔离仍 **BLOCKED**；UX：Button type default + ErrorBoundary focus + ThinkingBlock chevron a11y；unit Button **3** + ErrorBoundary **3** + ThinkingBlock **3**（**9 PASS**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave47a.log` + `/tmp/wave47-evidence.txt`） |
| wave-48 residual | 隔离仍 **BLOCKED**；unit session-path **3** + AnimatedCollapse **3**（**6 PASS**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave48b.log` + `/tmp/wave48-evidence.txt`） |
| wave-49 residual | 隔离仍 **BLOCKED**；unit SettingsNav **3** + Popover **3**（**6 PASS**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave49b.log` + `/tmp/wave49-evidence.txt`） |
| wave-50 residual | 隔离仍 **BLOCKED**；UX：ShortcutsCheatsheet close a11y；unit ShortcutsCheatsheet **3** + SessionRow **3**（**6 PASS**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave50a.log` + `/tmp/wave50-evidence.txt`） |
| wave-51 residual | 隔离 re-probe **BLOCKED**（admin=False / Sandbox missing / host 1.0.13）；UX：SessionExportDialog dialog/a11y；unit ModelSelector **4** + MotionPanelLayer **2** + CustomMessageCard **1** + SessionExportDialog **3**（**10 PASS**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/nsis-probe-wave51.txt` + `/tmp/unit-wave51a.log` + `/tmp/wave51-evidence.txt`） |
| wave-52 residual | 隔离仍 **BLOCKED**；export GeneratedUiTable pure helpers；unit pure **2** + UI **2** + SessionExport recheck **3**（**7 PASS**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave52a.log` + `/tmp/wave52-evidence.txt`） |
| wave-53 residual | 隔离仍 **BLOCKED**；export GeneratedUiForm pure helpers；unit pure **2** + form UI **2** + MiniMaxCodeTitleBar **2**（**6 PASS**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave53a.log` + `/tmp/wave53-evidence.txt`） |
| wave-54 residual | 隔离仍 **BLOCKED**；export DateGroupedSessionList pure（injectable `now`）；local-calendar pure fixtures；UX group focus-visible；unit pure **2** + UI **2**（**4 PASS**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave54-55b.log` + `/tmp/wave54-evidence.txt`） |
| wave-55 residual | 隔离仍 **BLOCKED**；UX ProjectGroupedSessionList focus-visible；unit ProjectGrouped **3** + session-grouping expand **3 新**（file **4**；cluster w/ wave-54 **11 PASS**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave54-55b.log` + `/tmp/wave55-evidence.txt`） |
| wave-56 residual | 隔离仍 **BLOCKED**；export DiffViewer `splitHunkLines`；UX FoldRow button a11y；unit pure **3** + UI **3**（**6 PASS**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave56a.log` + `/tmp/wave56-evidence.txt`） |
| wave-57 residual | 隔离 re-probe **BLOCKED**（admin=False / Sandbox missing / host 1.0.13）；UX PiConfigEditor aria-pressed + ShortcutsSettings a11y；unit PiConfigEditor **3** + ShortcutsSettings **3**（**6 PASS**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/nsis-probe-wave57.txt` + `/tmp/unit-wave57a.log` + `/tmp/wave57-evidence.txt`） |
| wave-58 residual | 隔离仍 **BLOCKED**；export GeneratedUiChart `buildChartOption`；unit VirtualizedMessageList **3** + chart pure **3** + chart UI **1**（**7 PASS**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave58a.log` + `/tmp/wave58-evidence.txt`） |
| wave-59 residual | 隔离仍 **BLOCKED**；ClaudeSessionImporter unit **5**；tool-call normalize expand（file **7**）；export `findActiveSlashCommand` pure + slash suite **5**（cluster **17 PASS**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave59c.log` + `/tmp/wave59-evidence.txt`） |
| wave-60 residual | 隔离仍 **BLOCKED**；export useTaskProgress pure mappers；unit pure **2** + hook **5** + PLAN_DIRECTIVE **2**（**9 PASS**）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave60c.log` + `/tmp/wave60-evidence.txt`） |
| wave-61 residual | 隔离 re-probe **BLOCKED**（admin=False / Sandbox missing / host 1.0.13）；export native-session-fork pure helpers；unit pure **7 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/nsis-probe-wave60.txt` + `/tmp/unit-wave61b.log` + `/tmp/wave61-evidence.txt`） |
| wave-62 residual | 隔离仍 **BLOCKED**；fix `uninstallSkill` path containment；expand skillhub pure + WorkspaceNoticeBanner a11y + format/fuzzy/main-window/tray/workspace-selection；unit cluster **59 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave62a.log` + `/tmp/wave62-evidence.txt`） |
| wave-63 residual | 隔离仍 **BLOCKED**；UX WorkspaceSwitcher menu focus-visible + ProgressReminderToast stop a11y；export `basename` pure；ssrf-guard/web-security edge expand；unit cluster **44 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave63a.log` + `/tmp/wave63-evidence.txt`） |
| wave-64 residual | 隔离仍 **BLOCKED**；expand protected-paths credential edges；PiStatusPanel install/cancel/error + dialog a11y；terminal-command + crash-diagnostics dispose；unit cluster **19 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave64a.log` + `/tmp/wave64-evidence.txt`） |
| wave-65 residual | 隔离 re-probe **BLOCKED**（admin=False / Sandbox missing / host 1.0.13）；export SearchHistory pure (`sliceMatchWindow`/`compareSearchResults`) + close/result a11y；unit **7 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/nsis-probe-wave65.txt` + `/tmp/unit-wave65a.log` + `/tmp/wave65-evidence.txt`） |
| wave-66 residual | 隔离仍 **BLOCKED**；UX PersistenceBanner dismiss + RecentWorkspaces rows focus-visible；unit cluster **8 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave66a.log` + `/tmp/wave66-evidence.txt`） |
| wave-67 residual | 隔离仍 **BLOCKED**；SettingsNav a11y assert；mention-parser edges；SettingsContent close/done + SwitchControl + ToolPermissions preset focus-visible；unit cluster **39 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave67a.log` + `/tmp/wave67-evidence.txt`） |
| wave-68 residual | 隔离仍 **BLOCKED**；UX TerminalPanel toolbar + MySkills uninstall dialog + InstalledAddons actions focus-visible；unit cluster **16 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave68a.log` + `/tmp/wave68-evidence.txt`） |
| wave-69 residual | 隔离仍 **BLOCKED**；UX SkillsPanel tabs/dialogs + PiPackagesMarketplace catalog/install + AppearanceTab theme cards focus-visible；unit cluster **12 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave69a.log` + `/tmp/wave69-evidence.txt`） |
| wave-70 residual | 隔离仍 **BLOCKED**；UX AboutTab updater/diagnostics + ManagedModelsPanel row/dialog focus-visible；unit cluster **13 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave70a.log` + `/tmp/wave70-evidence.txt`） |
| wave-71 residual | 隔离仍 **BLOCKED**；UX SessionRow select/actions/menu/confirm + UsageTab RangeButton chips focus-visible；unit cluster **6 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave71a.log` + `/tmp/wave71-evidence.txt`） |
| wave-72 residual | 隔离仍 **BLOCKED**；UX RightRail browse/file/diff/expand + ModelSelector trigger/options focus-visible；unit cluster **21 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave72a.log` + `/tmp/wave72-evidence.txt`） |
| wave-73 residual | 隔离仍 **BLOCKED**；UX GitRailControls row/popover + SessionCenter row/delete + MiniMaxCodeLayout floating toggle focus-visible assert；unit cluster **33 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave73a.log` + `/tmp/wave73-evidence.txt`） |
| wave-74 residual | 隔离仍 **BLOCKED**；UX GitPanel change/branch/commit + FileWorkspace tree/toolbar + TaskProgressPanel rows focus-visible；unit cluster **77 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave74a.log` + `/tmp/wave74-evidence.txt`） |
| wave-75 residual | 隔离仍 **BLOCKED**；UX CommandCard/PermissionRequestStack/MessageBubble/GeneratedUiCard/CommandPalette focus-visible；unit cluster **63 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave75a.log` + `/tmp/wave75-evidence.txt`） |
| wave-76 residual | 隔离仍 **BLOCKED**；UX PlanCard actions/options + DesktopOverlay progress stop focus-visible；unit cluster **24 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave76a.log` + `/tmp/wave76-evidence.txt`） |
| wave-77 residual | 隔离仍 **BLOCKED**；UX Button focus-visible 升级 + ThinkingBlock/GeneratedUiTable/Form/ToastContainer/Onboarding focus-visible；unit cluster **23 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave77a.log` + `/tmp/wave77-evidence.txt`） |
| wave-78 residual | 隔离仍 **BLOCKED**；UX App panel retry + ChatView title/retry/scroll/readOnly + UsageTab chart/session + Sidebar 新建对话 focus-visible；unit cluster **91 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave78a.log` + `/tmp/wave78-evidence.txt`） |
| wave-79 residual | 隔离仍 **BLOCKED**；UX ApprovalPanel close + ChatInput reference toolbar + SessionExport cancel + Shortcuts cancel + PiConfigEditor actions focus-visible；unit cluster **60 PASS**；typecheck **PASS**；plain-string no-fv buttons **0** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave79a.log` + `/tmp/wave79-evidence.txt`） |
| wave-80 residual | 隔离仍 **BLOCKED**；UX ChatInput primary send/stop（default+reference）+ @mention option focus-visible；unit **46 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave80a.log` + `/tmp/wave80-evidence.txt`） |
| wave-81 residual | 隔离仍 **BLOCKED**；UX form fields focus-visible（Skills/MySkills/General/LongHorizon/Git branch/CommandPalette/PlanCard feedback）；unit cluster **79 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave81a.log` + `/tmp/wave81-evidence.txt`） |
| wave-82 residual | 隔离仍 **BLOCKED**；UX MySkills actions + ChatInput slash options type/focus-visible + bare-button type hygiene；unit cluster **68 PASS**；typecheck **PASS**；bare type-less buttons **0** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave82a.log` + `/tmp/wave82-evidence.txt`） |
| wave-83 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / host 1.0.13）；UX ChatInput composer triggers/menus restore focus-visible rings；unit **47 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/nsis-probe-wave83.txt` + `/tmp/unit-wave83a.log` + `/tmp/wave83-evidence.txt`） |
| wave-84 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / host 1.0.13）；NSIS honesty E2E **4/4**（gate+inventory，无装机）；classifier multi-tool/path/redirect residual + ssrf edges + ChatInput composer FV asserts；unit cluster **161 PASS**（classifier 77 + ssrf 36 + ChatInput 48）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/nsis-probe-wave84.txt` + `/tmp/e2e-nsis-wave84.log` + `/tmp/unit-wave84b.log` + `/tmp/wave84-evidence.txt`） |
| wave-85 residual | 隔离仍 **BLOCKED**；security residual：log-redaction + web-security + schemas session/package/project edges；unit cluster **84 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave85b.log` + `/tmp/wave85-evidence.txt`） |
| wave-86 residual | 隔离仍 **BLOCKED**；path residual：local-file-protocol sensitive/sibling/404/encode + path-canonical home/nested；unit cluster **18 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave86a.log` + `/tmp/wave86-evidence.txt`） |
| wave-87 residual | 隔离仍 **BLOCKED**；lifecycle residual：single-instance + diagnostics empty/multi + crash recheck；unit cluster **10 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave87a.log` + `/tmp/wave87-evidence.txt`） |
| wave-88 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / host 1.0.13）；UX form-field focus-visible 扫尾（text inputs/selects/ranges；checkbox/radio 除外）+ permission evaluate residual；unit cluster **77 PASS**；typecheck **PASS**；text form no-fv **0** — **K-004/014/015 仍 BLOCKED**（`/tmp/nsis-probe-wave88.txt` + `/tmp/unit-wave88a.log` + `/tmp/wave88-evidence.txt`） |
| wave-89 residual | 隔离仍 **BLOCKED**；permission/memory residual：tool-category + wildcard + fts-query edges；unit cluster **32 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave89a.log` + `/tmp/wave89-evidence.txt`） |
| wave-90 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / host 1.0.13）；runtime-policy + guarded-tools + memory-path-guard + protected-paths + Markdown XSS residual；unit cluster **148 PASS**；typecheck **PASS**；NSIS honesty E2E **4/4**（no install）— **K-004/014/015 仍 BLOCKED**（`/tmp/nsis-probe-wave90.txt` + `/tmp/unit-wave90b.log` + `/tmp/e2e-nsis-wave90.log` + `/tmp/wave90-evidence.txt`） |
| wave-91 residual | 隔离仍 **BLOCKED**；shared-types residual：command-risk + normalizeLongHorizon + ipc-guards；agent directives-pure residual；shared unit **61 PASS** + directives **9 PASS**；typecheck shared+desktop **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave91-shared.log` + `/tmp/unit-wave91-directives.log` + `/tmp/wave91-evidence.txt`） |
| wave-92 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / host 1.0.13）；pending-edits + approval-bridge + PtyManager residual；unit cluster **43 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/nsis-probe-wave92.txt` + `/tmp/unit-wave92b.log` + `/tmp/wave92-evidence.txt`） |
| wave-93 residual | 隔离仍 **BLOCKED**；session-path + log-redaction + ipc helpers residual；unit cluster **42 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave93b.log` + `/tmp/wave93-evidence.txt`） |
| wave-94 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / host 1.0.13）；web-security + Task/Goal/Subagent schemas residual；unit cluster **85 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/nsis-probe-wave94.txt` + `/tmp/unit-wave94b.log` + `/tmp/wave94-evidence.txt`） |
| wave-95 residual | 隔离仍 **BLOCKED**；permission/approval/toast store + event-bridge residual；unit cluster **54 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave95a.log` + `/tmp/wave95-evidence.txt`） |
| wave-96 residual | 隔离仍 **BLOCKED**；attachments/agent-mode store + ssrf-guard residual；unit cluster **55 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave96a.log` + `/tmp/wave96-evidence.txt`） |
| wave-97 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / host 1.0.13）；agent/workspace/updater store + window-lifecycle + tray-icon + local-file-protocol residual；unit cluster **82 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/nsis-probe-wave97.txt` + `/tmp/unit-wave97b.log` + `/tmp/wave97-evidence.txt`） |
| wave-98 residual | 隔离仍 **BLOCKED**；crash-diagnostics + workspace-selection + main-window-options + diagnostics + single-instance + overlay bounds residual；unit cluster **42 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（ + ） |
| wave-99 residual | 隔离仍 **BLOCKED**；desktop-overlay/updater/workbench/diagnostics/agents/window IPC residual；unit cluster **35 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave99b.log` + `/tmp/wave99-evidence.txt`） |
| wave-100 residual | 隔离仍 **BLOCKED**；settings-window/packages/claude-codex/git-undo/pi-driver IPC residual；unit cluster **38 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave100b.log` + `/tmp/wave100-evidence.txt`） |
| wave-101 residual | 隔离仍 **BLOCKED**（沿用 wave-97 probe）；sqlite-session-repository + mimocode-runtime-port + workspace.ipc + config.ipc residual；unit cluster **39 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave101.log` + `/tmp/typecheck-wave101.log` + `/tmp/wave101-evidence.txt`） |
| wave-102 residual | 隔离仍 **BLOCKED**；settings/skills/project-shell/plan/terminal IPC residual；unit cluster **66 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave102.log` + `/tmp/typecheck-wave102.log` + `/tmp/wave102-evidence.txt`） |
| wave-103 residual | 隔离仍 **BLOCKED**；files.ipc + list-local-skills + updater service + skills-store residual；unit cluster **44 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave103.log` + `/tmp/typecheck-wave103.log` + `/tmp/wave103-evidence.txt`） |
| wave-104 residual | 隔离仍 **BLOCKED**；permission defaults + agent-mode/attachments/pi-status stores + desktop-overlay/updater/diagnostics IPC residual；unit cluster **59 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave104.log` + `/tmp/typecheck-wave104.log` + `/tmp/wave104-evidence.txt`） |
| wave-105 residual | 隔离仍 **BLOCKED**；export/notifications/terminal-command/plan-prompt/checkpoint/pi-api residual；unit cluster **21 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave105.log` + `/tmp/typecheck-wave105.log` + `/tmp/wave105-evidence.txt`） |
| wave-106 residual | 隔离仍 **BLOCKED**；mutation-queue + session-path + GeneratedUi pure + date grouping residual；unit cluster **28 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave106.log` + `/tmp/typecheck-wave106.log` + `/tmp/wave106-evidence.txt`） |
| wave-107 residual | 隔离仍 **BLOCKED**；extension-ui-bridge resolve/always/plan parse + DiffViewer fold edges + useDebouncedSave residual；unit cluster **16 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave107.log` + `/tmp/typecheck-wave107.log` + `/tmp/wave107-evidence.txt`） |
| wave-108 residual | 隔离仍 **BLOCKED**；globals focus-visible/reduced-motion + tab-defs + partition + fuzzy-match + no-duplicate-ipc residual；unit cluster **25 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave108.log` + `/tmp/typecheck-wave108.log` + `/tmp/wave108-evidence.txt`） |
| wave-109 residual | 隔离仍 **BLOCKED**；sounds/first-launch/subscription-manager/project-scripts/session-grouping/run-control/tool-call residual；unit cluster **64 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave109.log` + `/tmp/typecheck-wave109.log` + `/tmp/wave109-evidence.txt`） |
| wave-110 residual | 隔离仍 **BLOCKED**；queue-store + runtime-feature-store + generated-ui + format + logger residual；unit cluster **71 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave110.log` + `/tmp/typecheck-wave110.log` + `/tmp/wave110-evidence.txt`） |
| wave-111 residual | 隔离仍 **BLOCKED**；file-workspace-utils + plan-utils + web-security + protected-paths residual；unit cluster **70 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave111.log` + `/tmp/typecheck-wave111.log` + `/tmp/wave111-evidence.txt`） |
| wave-112 residual | 隔离仍 **BLOCKED**；theme + wildcard + tool-category + path-canonical residual；unit cluster **46 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave112.log` + `/tmp/typecheck-wave112.log` + `/tmp/wave112-evidence.txt`） |
| wave-113 residual | 隔离仍 **BLOCKED**（wave-113 re-probe：admin=False / Sandbox missing / Docker missing；主机 package 1.0.14，**未**装机）；mention-parser + permission evaluate + classifier + approval-store + pending-edits + fuzzy-match residual；unit cluster **172 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave113.log` + `/tmp/typecheck-wave113.log` + `/tmp/wave113-evidence.txt` + `/tmp/nsis-probe-wave113.txt`） |
| wave-114 residual | 隔离仍 **BLOCKED**；runtime-policy + export + notifications + main fuzzy-match + terminal-command + command-risk residual；unit cluster **130 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave114.log` + `/tmp/typecheck-wave114.log` + `/tmp/wave114-evidence.txt`） |
| wave-115 residual | 隔离仍 **BLOCKED**；log-redaction + ssrf-guard + diff-parser + project-detector + app-settings + events residual；unit cluster **95 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave115.log` + `/tmp/typecheck-wave115.log` + `/tmp/wave115-evidence.txt`） |
| wave-116 residual | 隔离仍 **BLOCKED**；file-tree + web-security + IPC schemas (git/agents/codex) + ipc-guards residual；unit cluster **120 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave116.log` + `/tmp/typecheck-wave116.log` + `/tmp/wave116-evidence.txt`） |
| wave-117 residual | 隔离仍 **BLOCKED**（wave-117 re-probe：admin=False / Sandbox missing / Docker missing；**未**装机）；guarded-tools + protected-paths + ipc helpers + schemas (agentsId/plan/claude/workbench/git/updateMessage) residual；unit cluster **150 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave117.log` + `/tmp/typecheck-wave117.log` + `/tmp/wave117-evidence.txt` + `/tmp/nsis-probe-wave117.txt`） |
| wave-118 residual | 隔离仍 **BLOCKED**；format relative/duration edges + session-grouping search/sort + tool-call whitespace/persistence + theme font clamp residual；unit cluster **83 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave118.log` + `/tmp/typecheck-wave118.log` + `/tmp/wave118-evidence.txt`） |
| wave-119 residual | 隔离仍 **BLOCKED**；run-control string-throws/plan pause + first-launch key/case + queue-store update/cap/clear + subscription-manager reverse cleanup residual；unit cluster **53 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave119.log` + `/tmp/typecheck-wave119.log` + `/tmp/wave119-evidence.txt`） |
| wave-120 residual | 隔离仍 **BLOCKED**；permission defaults whitelist edges + path-canonical sensitive/escape + runtime-feature featureState precedence + toast ids/durations residual；unit cluster **58 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave120.log` + `/tmp/typecheck-wave120.log` + `/tmp/wave120-evidence.txt`） |
| wave-121 residual | 隔离仍 **BLOCKED**；permission-store dedupe/payload + attachments isolation + workspace-selection ref/timestamps + wildcard anchors + tool-category sets residual；unit cluster **68 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave121.log` + `/tmp/typecheck-wave121.log` + `/tmp/wave121-evidence.txt`） |
| wave-122 residual | 隔离仍 **BLOCKED**；skills-store narrow/uninstall toast + sounds volume clamp/disabled + generated-ui unknown-section/content merge residual；unit cluster **40 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave122.log` + `/tmp/typecheck-wave122.log` + `/tmp/wave122-evidence.txt`） |
| wave-123 residual | 隔离仍 **BLOCKED**（wave-123 re-probe：admin=False / Sandbox missing / Docker missing；**未**装机）；agent-mode-store clamp-persist/corrupt-json + workspace-store create-fallback/remove-noncurrent/git replace + plan-utils failed/locked/recent + diff-parser metadata/pre-header + project-detector Cargo `[package]` mid-file **fix** (line-scan) + Pipfile/pnpm-prefer + single-instance/main-window/crash dispose residual；unit cluster **111 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave123.log` + `/tmp/typecheck-wave123.log` + `/tmp/wave123-evidence.txt` + `/tmp/nsis-probe-wave123.txt`） |
| wave-124 residual | 隔离仍 **BLOCKED**；mutation-queue async reject/keyed error + logger null/undefined/console fallback + list-local-skills blank-line/empty-cache TTL + GeneratedUiTable compare/format + export toolCalls/empty-title + directives empty-path/compose contracts + plan-prompt 7-line + tab-defs padded-id residual；unit cluster **61 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave124.log` + `/tmp/typecheck-wave124.log` + `/tmp/wave124-evidence.txt`） |
| wave-125 residual | 隔离仍 **BLOCKED**（wave-125 re-probe：admin=False / Sandbox missing / Docker missing；**未**装机）；pi-status update/uninstall/missing-API/_setProgress + updater download/listener + GeneratedUiForm number defaults + chart pie/bar multi-series + project-scripts empty/cargo fallback + pi-api undefined + ssrf metadata/fe80 residual；unit cluster **98 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave125.log` + `/tmp/typecheck-wave125.log` + `/tmp/wave125-evidence.txt` + `/tmp/nsis-probe-wave125.txt`） |
| wave-126 residual | 隔离仍 **BLOCKED**（wave-126 re-probe：admin=False / Sandbox missing / Docker missing；**未**装机）；notifications preference/options + log-redaction circular/api-key + web-security openExternal/nav + fuzzy-match(main+renderer) + file-workspace-utils mode/shell/git + classifier git-hooks/pnpm + pending-edits cache + approval-store writeDiff/unknown + terminal-command force-push + ipc partition + path-canonical pem/sqlite/.ssh + formatDuration nullish residual；unit cluster **307 PASS**（13 files）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave126.log` + `/tmp/typecheck-wave126.log` + `/tmp/wave126-evidence.txt` + `/tmp/nsis-probe-wave126.txt`） |
| wave-127 residual | 隔离仍 **BLOCKED**；theme/subscription/session-grouping/sounds/toast/first-launch/workspace-selection/mention/run-control/local-file-protocol/tool-call/permission residual；unit cluster **199 PASS**（12 files）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave127.log` + `/tmp/typecheck-wave127.log` + `/tmp/wave127-evidence.txt`） |
| wave-128 residual | 隔离仍 **BLOCKED**（wave-128 re-probe：admin=False / Sandbox missing / Docker missing；**未**装机）；attachments max-20 + single-instance secondary/primary + mutation-queue serial/keyed + tray-icon candidates + queue-store agent_end-after-error/extension_error + generated-ui non-object/content-alone + project-scripts multi-segment residual；unit cluster **81 PASS**（7 files）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave128.log` + `/tmp/typecheck-wave128.log` + `/tmp/wave128-evidence.txt` + `/tmp/nsis-probe-wave128.txt`） |
| wave-129 residual | 隔离仍 **BLOCKED**（wave-129 re-probe：admin=False / Sandbox missing / Docker missing；**未**装机）；plan-store think-strip/step-cap/phases/goal-cleared + agent optimistic-drop/local-sort + session rename/setDefault + settings rightRail/fontSize-raw + skills blank-search/install-rethrow/toggle-toast + export HTML-escape/roles/tags residual；unit cluster **130 PASS**（6 files）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave129.log` + `/tmp/typecheck-wave129.log` + `/tmp/wave129-evidence.txt` + `/tmp/nsis-probe-wave129.txt`） |
| wave-130 residual | 隔离仍 **BLOCKED**（wave-130 re-probe：admin=False / Sandbox missing / Docker missing；**未**装机）；updater install-throw/cleanup/hydrate-missing + workspace explicit-id/remove-first/clearError + pi-status update/cancel/listeners + runtime-feature clamp-build/clearError/IpcError + permission defaults dir/* + pi-api delete residual；unit cluster **109 PASS**（6 files）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave130.log` + `/tmp/typecheck-wave130.log` + `/tmp/wave130-evidence.txt` + `/tmp/nsis-probe-wave130.txt`） |
| wave-131 residual | 隔离仍 **BLOCKED**（wave-131 re-probe：admin=False / Sandbox missing / Docker missing；**未**装机）；permission evaluate last-match/~/expand/disabled-* + wildcard empty/space-star/case + terminal terraform/npm-publish + ipc null/nested + diff-parser empty/prefer-diff + ssrf loopback-allow/data-blob residual；unit cluster **118 PASS**（6 files）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave131.log` + `/tmp/typecheck-wave131.log` + `/tmp/wave131-evidence.txt` + `/tmp/nsis-probe-wave131.txt`） |
| wave-132 residual | 隔离仍 **BLOCKED**（wave-132 re-probe：admin=False / Sandbox missing / Docker missing；**未**装机）；web-security http-open/blob-block/same-origin-nav + log-redaction Error/Buffer/non-exact-keys + path-canonical .aws/.gnupg/.npmrc + local-file-protocol cert/cred-dir 403/404 + notifications enabled-string/canNotify/body-omit + classifier iex/sc/reg/System32/pnpm-filter residual；unit cluster **194 PASS**（6 files）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave132.log` + `/tmp/typecheck-wave132.log` + `/tmp/wave132-evidence.txt` + `/tmp/nsis-probe-wave132.txt`） |
| wave-133 residual | 隔离仍 **BLOCKED**（wave-133 re-probe：admin=False / Sandbox missing / Docker missing；**未**装机）；diagnostics sessionStats/basename/nolog + agent-modes longHorizon-off/registry/workflow-flags/plan-empty-bash + crash-diagnostics renderer-dispose/nullish + overlay negative-origin/NaN-width + agent-mode-store fallback/runtime-clamp residual；unit cluster **63 PASS**（5 files）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave133.log` + `/tmp/typecheck-wave133.log` + `/tmp/wave133-evidence.txt` + `/tmp/nsis-probe-wave133.txt`） |
| wave-134 residual | 隔离仍 **BLOCKED**（wave-134 re-probe：admin=False / Sandbox missing / Docker missing；**未**装机）；main-window-options prefs-fresh/darwin-keys + mimocode maxMode-off/dream-distill/plan-tools + window-lifecycle quit-close/restore-not-min/no-overlay + updater download-error/progress-100/getState-clone + sqlite blank-search/limit/workspace/thinking residual；unit cluster **54 PASS**（5 files）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave134.log` + `/tmp/typecheck-wave134.log` + `/tmp/wave134-evidence.txt` + `/tmp/nsis-probe-wave134.txt`） |
| wave-135 residual | 隔离仍 **BLOCKED**（wave-135 re-probe：admin=False / Sandbox missing / Docker missing；**未**装机）；first-launch 1/0/throw-fallback + theme normalize/editor-diff/applyFont/system-light + sounds volume-clamp/disable-skip + subscription LIFO/nullish/non-fn-throw + toast duration-override/default-error + permission enqueue-idempotent/respond-throw residual；unit cluster **113 PASS**（6 files）；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave135.log` + `/tmp/typecheck-wave135.log` + `/tmp/wave135-evidence.txt` + `/tmp/nsis-probe-wave135.txt`） |
| wave-136 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / host 1.0.13）；IME D-003 useInputShortcuts composition guard + multi-display settings clamp + crash inject matrix + shortcuts overrides + fts residual；unit **91 PASS**（5 files）；typecheck **PASS**；product IME Enter guard；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave136.log` + `/tmp/wave136-evidence.txt`） |
| wave-137 residual | 隔离 re-probe 仍 **BLOCKED**；memory/paths + insert-reminders + usage-aggregation + session-path residual；unit **109 PASS**（4 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave137.log` + `/tmp/wave137-evidence.txt`） |
| wave-138 residual | 隔离 re-probe 仍 **BLOCKED**；task-id + agent-info + tool-category + protected-paths + plan-prompt residual；unit **107 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave138.log` + `/tmp/wave138-evidence.txt`） |
| wave-139 residual | 隔离 re-probe 仍 **BLOCKED**；window.ipc + TaskOverview pure + mutation-queue soak + export roles + tray residual；unit **61 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave139.log` + `/tmp/wave139-evidence.txt`） |
| wave-140 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / host 1.0.13；**未**装机）；formatPromptFailure 403/rate/cause/empty + git createBranch/checkout/original/changedFiles + preload invoke/send allowlist + memory/describeImages + command-risk reg/npm-g/clean/pipe + fuzzy CJK/segment residual；desktop unit **108 PASS**（4 files）+ shared command-risk **49 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave140-desktop.log` + `/tmp/unit-wave140-shared.log` + `/tmp/wave140-evidence.txt` + `/tmp/nsis-probe-wave140.txt`） |
| wave-141 residual | 隔离 re-probe 仍 **BLOCKED**；pi-package-adapter catalog/search/install + pending-edits autoApprove/cache/evict + workspace-selection long-list/tie + single-instance listener/restore-error residual；unit **67 PASS**（4 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave141.log` + `/tmp/wave141-evidence.txt` + `/tmp/nsis-probe-wave141.txt`） |
| wave-142 residual | 隔离 re-probe 仍 **BLOCKED**；generated-ui v1/clone/plainText + tool-call batch/args-alias + run-control plan-toast/ipcError + ipc-guards legacy/tabs residual；desktop unit **60 PASS**（3 files）+ shared ipc-guards **15 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave142-desktop.log` + `/tmp/unit-wave142-shared.log` + `/tmp/wave142-evidence.txt` + `/tmp/nsis-probe-wave142.txt`） |
| wave-143 residual | 隔离 re-probe 仍 **BLOCKED**；mention-parser whitespace/CJK/multi-@ + project-scripts nullish/unicode + settings-bridge score/enabled/reconcile + app-settings dream/workflow residual；desktop unit **45 PASS**（3 files）+ shared app-settings **13 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave143-desktop.log` + `/tmp/unit-wave143-shared.log` + `/tmp/wave143-evidence.txt` + `/tmp/nsis-probe-wave143.txt`） |
| wave-144 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；session-grouping tags/lazy/cycle/depth/group-order + queue-store turn/auto_retry/blank-tool/queue_update cap + toast duration-0/tone-defaults/cap-mix + export toolCalls/html-escape/json-meta + mutation-queue empty/reject-chain residual；desktop unit **102 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave144-unit.log` + `/tmp/wave144-typecheck.log` + `/tmp/wave144-evidence.txt` + `/tmp/nsis-probe-wave144.txt`）— **scoreboard 不通胀** |
| wave-145 residual | 隔离 re-probe 仍 **BLOCKED**；terminal-command del/rmdir/reg + logger stackless/non-fn-bridge + ipc helpers context/parse-throw + first-launch idempotent/empty + events lifecycle/queue residual；desktop unit **61 PASS**（4 files）+ shared events **11 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave145-unit-desktop.log` + `/tmp/wave145-unit-shared.log` + `/tmp/wave145-evidence.txt` + `/tmp/nsis-probe-wave145.txt`）— **scoreboard 不通胀** |
| wave-146 residual | 隔离 re-probe 仍 **BLOCKED**；ipc partition branded/params/empty-strings + tool-call-normalization empty/non-array + pi-api reassign/null + command-risk del/rmdir/kubectl/terraform/whitespace residual；desktop unit **27 PASS**（3 files）+ shared command-risk **52 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave146-unit-desktop.log` + `/tmp/wave146-unit-shared.log` + `/tmp/wave146-evidence.txt` + `/tmp/nsis-probe-wave146.txt`）— **scoreboard 不通胀** |
| wave-147 residual | 隔离 re-probe 仍 **BLOCKED**；tray-icon 6-candidate order/asar.unpacked + sounds invalid-json/preserve/audio-throw + notifications helper titles/disabled/constructor residual；desktop unit **54 PASS**（3 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave147-unit.log` + `/tmp/wave147-typecheck.log` + `/tmp/wave147-evidence.txt` + `/tmp/nsis-probe-wave147.txt`）— **scoreboard 不通胀** |
| wave-148 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；setup 1.0.11–13 有 artifact 但 **禁止** host 装机）；attachments mime/size/per-ws-cap + permission respondValue-throw/setMode-no-piAPI/boolean + ssrf IMDS-IPv6/RFC1918-allow/scheme residual；desktop unit **96 PASS**（3 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave148-unit.log` + `/tmp/wave148-evidence.txt` + `/tmp/nsis-probe-wave148.txt`）— **scoreboard 不通胀** |
| wave-149 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；agent-info hardPermission-empty/list-dual-disable/plan-no-ws/no-toolAllowlist + theme localStorage-throw/min-CSS-ladder/matchMedia-missing + evaluate bare-~/restore-edit/synthetic-ask/merge-empty + agent-mode-store normalize/null-ws/featureState-vs-longHorizon + format toDate/duration/relative residual；desktop unit **158 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave149.log` + `/tmp/typecheck-wave149.log` + `/tmp/wave149-evidence.txt` + `/tmp/nsis-probe-wave149.txt`）— **scoreboard 不通胀** |
| wave-150 residual | 隔离 re-probe 仍 **BLOCKED**；tool-category normalize/exact-fileRead/mode-required + wildcard mixed-sep/brace-escape/?-slash + subscription void/re-ensure/empty-array + agent-modes goal-clone/normalize/plan-gate/subagents residual；desktop unit **85 PASS**（4 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave150.log` + `/tmp/typecheck-wave150.log` + `/tmp/wave150-evidence.txt` + `/tmp/nsis-probe-wave150.txt`）— **scoreboard 不通胀** |
| wave-151 residual | 隔离 re-probe 仍 **BLOCKED**；defaults multi-whitelist/env.example-order/external-ask + workspace-selection empty/zero-active/tie-first + project-scripts no-escape/case-manager + events auto_retry_ok/empty-queue residual；desktop unit **45 PASS**（3 files）+ shared events **13 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave151-desktop.log` + `/tmp/unit-wave151-shared.log` + `/tmp/wave151-evidence.txt` + `/tmp/nsis-probe-wave151.txt`）— **scoreboard 不通胀** |
| wave-152 residual | 隔离 re-probe 仍 **BLOCKED**；main-window-options independent-chrome/titleBarStyle + fuzzy-match underscore/empty/subsequence (main+renderer) + command-risk sudo/mkfs/git-reset/terraform-auto-approve/trim residual；desktop unit **46 PASS**（3 files）+ shared command-risk **55 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave152-desktop.log` + `/tmp/unit-wave152-shared.log` + `/tmp/wave152-evidence.txt` + `/tmp/nsis-probe-wave152.txt`）— **scoreboard 不通胀** |
| wave-153 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；run-control null-agentId/blank-Error/missing-agentsAbort/activeExecution-clear/abort-recover + overlay small-workArea/fractional-origin/omit-size/primary-fallback/destroy/refresh + tool-call contentIndex/status-alias/Date-revive/output-prefer + terminal-command sudo/mkfs/terraform-auto-approve + app-settings workflow-over-compose/empty-defaults residual；desktop unit **84 PASS**（4 files）+ shared app-settings **17 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave153-desktop.log` + `/tmp/unit-wave153-shared.log` + `/tmp/wave153-evidence.txt` + `/tmp/nsis-probe-wave153.txt`）— **scoreboard 不通胀** |
| wave-154 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；log-redaction cookie/auth/secret-keys/nested-sk + ssrf empty/ftp/ws/169.254-port/fe80-band + mutation-queue sync-throw/keyed-no-write/concurrent-20 + tool-call-normalization modern-only/empty-object/siblings + first-launch 1-0/TRUE-fallback/write-canonical residual；desktop unit **132 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave154-desktop.log` + `/tmp/wave154-evidence.txt` + `/tmp/nsis-probe-wave154.txt`）— **scoreboard 不通胀** |
| wave-155 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；web-security empty/malformed/cross-origin/blocked-shell + evaluate fromConfig-order/multi-ruleset/disabled-star/EDIT_TOOLS + wildcard metachar/multi-*/space-star + sounds bounds/defaults/persist + generated-ui v3→v1/content-append/markdown-v2 residual；desktop unit **127 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave155-desktop.log` + `/tmp/wave155-evidence.txt` + `/tmp/nsis-probe-wave155.txt`）— **scoreboard 不通胀** |
| wave-156 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；export 你-AI-roles/usage-gate/json-omit-optional + tray-icon first-match/cwd-fallback/checkedPaths + notifications missing-API/denied/toggle + defaults empty-whitelist/nested-env/sibling-prefix residual；desktop unit **79 PASS**（4 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave156-desktop.log` + `/tmp/wave156-evidence.txt` + `/tmp/nsis-probe-wave156.txt`）— **scoreboard 不通胀** |
| wave-157 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；mention-parser cursor0/path-separators/empty-query/pure-resolve + runtime-policy git-segments/git.exe-cmd-wrapper/plan-Plan-reason/sessionPermissions-override + crash-diagnostics dispose-idempotent/multi-child-gone/renderer-dispose residual；desktop unit **105 PASS**（3 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave157-desktop.log` + `/tmp/wave157-evidence.txt` + `/tmp/nsis-probe-wave157.txt`）— **scoreboard 不通胀** |
| wave-158 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；format day-month-boundaries/duration-sec-boundaries + session-grouping depth-cap/cycle-depth2/orphan-drop/parent-before-child + fts-query quote-separator/empty-null/FTS5-ops + fuzzy-match prefix/CJK-segment/backslash residual；desktop unit **122 PASS**（4 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave158-desktop.log` + `/tmp/wave158-evidence.txt` + `/tmp/nsis-probe-wave158.txt`）— **scoreboard 不通胀** |
| wave-159 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；command-risk dd-if/git-clean-f/npm-uninstall-g/irm-iwr-pipes + tool-category network-regex/fileWrite-shell/empty-extension + main fuzzy-match renderer-parity + logger circular/throw-console/JSON + terminal-command high-risk-wrapper residual；desktop unit **68 PASS**（4 files）+ shared command-risk **58 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave159-desktop.log` + `/tmp/unit-wave159-shared.log` + `/tmp/wave159-evidence.txt` + `/tmp/nsis-probe-wave159.txt`）— **scoreboard 不通胀** |
| wave-160 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；ssrf metadata-case/RFC1918-allow/IMDS-IPv6/malformed + web-security external-http-only/file-mixed-deny/dev-localhost-http-only/same-origin + ipc partition primitives/partial-err + events lifecycle/auto_retry_start/compaction reasons residual；desktop unit **108 PASS**（3 files）+ shared events **16 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave160-desktop.log` + `/tmp/unit-wave160-shared.log` + `/tmp/wave160-evidence.txt` + `/tmp/nsis-probe-wave160.txt`）— **scoreboard 不通胀** |
| wave-161 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；app-settings defaultMode-normalize/nested-flags/composeWorkflow-as-workflow + theme resolveTheme/font-ladder/editor-floor + wildcard backslash-win32/empty/space-star residual；desktop unit **37 PASS**（2 files）+ shared app-settings **20 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave161-desktop.log` + `/tmp/unit-wave161-shared.log` + `/tmp/wave161-evidence.txt` + `/tmp/nsis-probe-wave161.txt`）— **scoreboard 不通胀** |
| wave-162 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；log-redaction Bearer-sk-api_key/Error-circular-MaxDepth + mutation-queue async-reject-advance/multi-key/nullish-results + sounds volume-clamp/NaN-null/disable-play residual；desktop unit **80 PASS**（3 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave162-desktop.log` + `/tmp/wave162-evidence.txt` + `/tmp/nsis-probe-wave162.txt`）— **scoreboard 不通胀** |
| wave-163 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；notifications enabled-true-only/no-API-denied/helpers-noop + defaults doom_loop-ask/question-deny/read-non-env/win-whitelist + tray-icon 6-candidates/parent/grandparent residual；desktop unit **65 PASS**（3 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave163-desktop.log` + `/tmp/wave163-evidence.txt` + `/tmp/nsis-probe-wave163.txt`）— **scoreboard 不通胀** |
| wave-164 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；path-canonical workspace-root/sibling-prefix/ENOTDIR-climb + evaluate disabled-empty/ask-allow/perm-wildcard/both-match/merge() + export HTML-escape/toolCalls/usage-0/role-labels + task-id skip-malformed/non-numeric-suffix/multi-digit/invalid-parent residual；desktop unit **112 PASS**（4 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave164-desktop.log` + `/tmp/wave164-evidence.txt` + `/tmp/nsis-probe-wave164.txt`）— **scoreboard 不通胀** |
| wave-165 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；session-path trailing-dot/dots-only/lowercase/unicode/hash-16 + tool-call alias-prefer/blank-index/status-map/array-filter + directives-pure PLAN_FILE_INFO/compose-docs/BUILD_SWITCH + protected-paths id_ecdsa/outside-first/escape/db.bak residual；desktop unit **99 PASS**（4 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave165-desktop.log` + `/tmp/wave165-evidence.txt` + `/tmp/nsis-probe-wave165.txt`）— **scoreboard 不通胀** |
| wave-166 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；classifier shell-empty/extra-high-risk/create-patch/unknown + insert-reminders plan→build-exists/planMode-off/compose-whitespace/build-only + plan-prompt write-only-under-plans + fts-query underscore/hyphen/operator-null residual；desktop unit **168 PASS**（4 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave166-desktop.log` + `/tmp/wave166-evidence.txt` + `/tmp/nsis-probe-wave166.txt`）— **scoreboard 不通胀** |
| wave-167 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；pending-edits reject-clear/review-missing/unique-ids + judge-prompt empty-special/insufficient-evidence/Zod-strip + agent-info longHorizon-off/mode-gates/hardPermission-over-session + pi-api falsy-as-is residual；desktop unit **95 PASS**（4 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave167-desktop.log` + `/tmp/wave167-evidence.txt` + `/tmp/nsis-probe-wave167.txt`）— **scoreboard 不通胀** |
| wave-168 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；generated-ui null-oversized/upsert-envelope/content-fallback/clone/plain-kv-action + paths resolveProjectId-trailing/walk-hidden-skip + guarded-tools policy-restore/builtin-names residual；desktop unit **117 PASS**（3 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave168-desktop.log` + `/tmp/wave168-evidence.txt` + `/tmp/nsis-probe-wave168.txt`）— **scoreboard 不通胀** |
| wave-169 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；memory-path-guard non-memory/root-CC/isInside/sibling-paths + run-control missing-piAPI/dual-fail/string-object-errors + reconcile empty/skip-fingerprint/upsert-skip residual；desktop unit **101 PASS**（3 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave169-desktop.log` + `/tmp/wave169-evidence.txt` + `/tmp/nsis-probe-wave169.txt`）— **scoreboard 不通胀** |
| wave-170 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；file-scanner maxDepth/dist-coverage/Thumbs/maxResults/missing-root + importer nested/roles/current-outdated/empty-list/missing-meta + interceptor autoApprove/empty-path/path-alias/turn_end-dedupe/unifiedDiff + task-registry empty-list/archive/eventSummary-flow + markdown-index delete-unknown/empty-body/scope+type residual；desktop unit **114 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave170-desktop.log` + `/tmp/wave170-evidence.txt` + `/tmp/nsis-probe-wave170.txt`）— **scoreboard 不通胀** |
| wave-171 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；list-local-skills truncate-100/CRLF/non-dir + plan-file-service corrupt-null/basename-escape/complete-dest + agent-modes shell-git-mkdir-case + builtin-skillhub fields/tar-safe + logger empty-arg/debug-fallback residual；desktop unit **97 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave171-desktop.log` + `/tmp/wave171-evidence.txt` + `/tmp/nsis-probe-wave171.txt`）— **scoreboard 不通胀** |
| wave-173 residual | 隔离 re-probe **BLOCKED**（admin=False / Sandbox missing / docker missing / host 1.0.13）；unit residual diagnostics/session-path/tray-icon/workspace-selection **67 PASS** + shared ipc-guards **19 PASS**；typecheck **PASS** — **K-004/014/015 仍 BLOCKED**（`/tmp/wave173-evidence.txt`） |
| wave-174 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；updater equal/prerelease/stable-over-pre + ENOTFOUND/blank normalize + window-lifecycle beginQuit-no-tray/ensureTray-after-quit/close-while-hidden + main-window-options unknown-platform/prefs/darwin-isolation + partition brand-only/wrong-brand/unicode + project-scripts whitespace managers/script spaces/manager matrix residual；desktop unit **75 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave174-evidence.txt` + `/tmp/nsis-probe-wave174.txt`）— **scoreboard 不通胀** |
| wave-175 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；file-tree maxDepth/maxEntries clamps + empty-ext + file-root + first-launch whitespace tokens/key isolation/numeric fallthrough + subscription-manager throwing setup/unsub throw LIFO + isSubscribed snapshot + crash-diagnostics null payloads/double-attach/wrong events + single-instance double primary/secondary never restore/restore arity residual；desktop unit **93 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave175-evidence.txt` + `/tmp/nsis-probe-wave175.txt`）— **scoreboard 不通胀** |
| wave-176 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；command-risk reg/kubectl/npm publish/Remove-Item Force + whitespace/comment-prefix + session-grouping whitespace query/messagesLoaded/activityTime + sounds invalid JSON/volume0/partial settings + web-security ftp/blob/ws/malformed/non-loopback + terminal-command draft registry residual；desktop unit **112 PASS**（4 files）+ shared command-risk **61 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave176-evidence.txt` + `/tmp/nsis-probe-wave176.txt`）— **scoreboard 不通胀** |
| wave-177 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；formatRelative future/sub-minute + formatDuration end/invalid/negative + toDate reject objects + logger bigint/circular/bridge throw/non-function log + protected-paths .config/gcloud + pem/p12/keystore/jks/token-names + .gcloud + notifications permission denied/default/short-circuit granted residual；desktop unit **130 PASS**（4 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave177-evidence.txt` + `/tmp/nsis-probe-wave177.txt`）— **scoreboard 不通胀** |
| wave-178 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；export empty-session markdown + tags/summary JSON + markdown 你/AI labels + run-control missing workspace+agent no toast + IpcError stop fallback + blank Error.message→未知错误 + fuzzy-match whitespace query not trimmed + CJK ordered-char + longer-query fail + log-redaction nested apiKey/token/password + Bearer comma/semicolon + primitives residual；desktop unit **124 PASS**（4 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave178-evidence.txt` + `/tmp/nsis-probe-wave178.txt`）— **scoreboard 不通胀** |
| wave-179 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；ssrf-guard 169.254 edges/https+port + IPv6 fe80/fec0 + query/hash/userinfo metadata + mutation-queue missing-key seed + double-reject + nested-run no-await-deadlock + main fuzzy-match whitespace/CJK/longer-query parity + tool-call output-over-result + non-object input ignore + nested trim id/name + array/function reject residual；desktop unit **141 PASS**（4 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave179-evidence.txt` + `/tmp/nsis-probe-wave179.txt`）— **scoreboard 不通胀** |
| wave-180 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；app-settings workflow-over-compose + maxMode partial merge + undefined defaults isolation + events thinking_level/tool_execution_end/session_info/extension_error + ipc-guards all SETTINGS tabs + params:undefined product + legacy string pair + theme Infinity/clamp/max ladder + getInitialTheme tokens + mention-parser oversized/negative cursor + CJK resolve + full-width space residual；desktop unit **50 PASS**（2 files）+ shared-types **125 PASS**（4 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave180-evidence.txt` + `/tmp/nsis-probe-wave180.txt`）— **scoreboard 不通胀** |
| wave-181 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；path-canonical nested existing/missing multi-segment + readLinkTarget missing + empty target reject + web-security data/mailto/file reject + same-origin case + mixed file↔http deny + 127.0.0.1 flag + session-grouping depth cap 4 + cycle depth 2 + preview/summary match + file-tree dirs-before-files + DEFAULT_IGNORES + maxEntries floor residual；desktop unit **115 PASS**（4 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave181-evidence.txt` + `/tmp/nsis-probe-wave181.txt`）— **scoreboard 不通胀** |
| wave-182 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；workspace-selection empty/singleton/immutability + lastActiveAt over createdAt + single-instance secondary exit(0)/no-quit + primary double emit + sounds volume clamp + disabled play + empty storage defaults + ipc null/undefined ok + array code/fallback err + function ok + err ref stable + first-launch writeBoolFlag + mark idempotent + TRUE/FALSE uppercase residual；desktop unit **123 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave182-evidence.txt` + `/tmp/nsis-probe-wave182.txt`）— **scoreboard 不通胀** |
| wave-183 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；permission wildcard CJK/? + regex literal $/{/^ + nullish str/* vs null pattern throw + tool-category internal spaces + CORE vs fileWrite/shell set delta + PlanWrite underscore + evaluate fromConfig empty + disabled later * deny/allow vs evaluate last match + default ask shape + window-lifecycle requestQuit once + restore null/destroyed no-op + double attach stacks close + overlay bounds +Infinity product path + 1×1 margin 14 + Math.round .5 residual；desktop unit **132 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave183-evidence.txt` + `/tmp/nsis-probe-wave183.txt`）— **scoreboard 不通胀** |
| wave-184 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；runtime-policy powershell/pwsh -Command git + compose empty immutable + extensions off plan_write + plan reason over shell-off + defaults .envrc vs .env.* + empty whitelist + trailing slash key + command-risk terraform/dd/del/rmdir/chmod/curl-pipe + crash-diagnostics dispose idempotent + double-attach + non-object renderer details + main-window-options win32=linux shape + darwin frame/titleBar + prefs unfrozen residual；desktop unit **128 PASS**（4 files）+ shared-types **64 PASS**（1 file）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave184-evidence.txt` + `/tmp/nsis-probe-wave184.txt`）— **scoreboard 不通胀** |
| wave-185 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；pending-edits list cache invalidation + edit old/new_string + classifier high-risk paths + pnpm/yarn multi-tool + nullish name/args `" {}"` + 2>&1 vs awk system + mimocode composeModeSupported false + workflowSupported false + subagents off empty systemAgents + notifications exact true only + missing key default true + disabled send no-op residual；desktop unit **179 PASS**（4 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave185-evidence.txt` + `/tmp/nsis-probe-wave185.txt`）— **scoreboard 不通胀** |
| wave-186 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；formatDuration ms/s/m ladder + formatRelative hour/day/>30d + isNumberOrUndefined/NaN + generated-ui content blank/card empty/concat + normalize nulls + title-only plain + updater download idle skip + install before downloaded no quitAndInstall + initial state releasePageUrl residual；desktop unit **101 PASS**（3 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave186-evidence.txt` + `/tmp/nsis-probe-wave186.txt`）— **scoreboard 不通胀** |
| wave-187 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；logger Error stack/no-stack + empty extra + console.debug + project-scripts double-run prefix + Windows path script names + undefined/null scriptName → `"undefined"`/`"null"` + protected-paths home root only without workspace + outside-workspace first + .env/id_* case-insensitive + isPathInside exact/sibling-prefix + ipc-guards brand short-circuit + params ref + primitives reject + settings tab pad reject + events turn_end/agent_end optional + message_update text_delta + compaction_end residual；desktop unit **75 PASS**（3 files）+ shared-types **46 PASS**（2 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave187-evidence.txt` + `/tmp/nsis-probe-wave187.txt`）— **scoreboard 不通胀** |
| wave-188 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；terminal-command terraform destroy/import + curl\|pwsh + rm -fr/--force + fuzzy empty query + first indexOf + underscore vs segment + subscription void setup + LIFO array unsub + agent-modes BUILD_SWITCH plan→build only + trim + empty shell reject + ssrf localhost/RFC1918 allow + exact metadata host + non-http reject residual；desktop unit **154 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave188-evidence.txt` + `/tmp/nsis-probe-wave188.txt`）— **scoreboard 不通胀** |
| wave-189 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；renderer fuzzy empty/first-indexOf/underscore + log-redaction x-api-key/api-key/secret + object key case + Date identity + export usage markdown + string timestamps JSON + HTML escape title/body + pi-api empty/frozen refs + app-settings null defaults + partial nested + workflow shallow-merge retains default knobs + ref isolation residual；desktop unit **104 PASS**（4 files）+ shared-types **25 PASS**（1 file）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave189-evidence.txt` + `/tmp/nsis-probe-wave189.txt`）— **scoreboard 不通胀** |
| wave-190 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；theme editor/diff floors + resolveTheme tokens + tray null path + cwd index5 + asar.unpacked index1 + ipc brand bare IpcError + success ref/primitives + first-launch missing/empty fallback + overwrite/idempotent mark + mutation-queue serial order + keyed multi-key isolation residual；desktop unit **119 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave190-evidence.txt` + `/tmp/nsis-probe-wave190.txt`）— **scoreboard 不通胀** |
| wave-191 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；localfile null workspace + .env reject + traversal + web-security blank/ws reject + file same-path only + path-canonical sibling-prefix + .env deny + sounds volume 0/1 + NaN→null JSON + malformed JSON defaults residual；desktop unit **130 PASS**（4 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave191-evidence.txt` + `/tmp/nsis-probe-wave191.txt`）— **scoreboard 不通胀** |
| wave-192 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；file-tree protected omit + maxEntries truncate + file/dir kinds + session-grouping parent/sibling sort + empty groups + messagesLoaded false skips think + tool-call success/executed/pausing + isError prefer + revive timestamps + persist times + git deleted/added/staged-only + empty changed + -z delete/rename + mention cursor0/empty/@span/whitespace + diagnostics dir logPath + sessionStats zeros override residual；desktop unit **165 PASS**（6 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave192-evidence.txt` + `/tmp/nsis-probe-wave192.txt`）— **scoreboard 不通胀** |
| wave-193 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；format duration empty/in-progress/negative + relative justNow/future + toDate identity/Invalid + notifications canNotify gates + helper titles + permission short-circuit + undefined Notification + workspace-selection equal-tie keep first + createdAt fallback + singleton identity + generated-ui upsert envelope + key_value/progress plain + equal content + maxBytes reject + single-instance primary true no exit + secondary exit(0) + restore throw forwards residual；desktop unit **172 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave193-evidence.txt` + `/tmp/nsis-probe-wave193.txt`）— **scoreboard 不通胀** |
| wave-194 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；export toolCalls details + zero usage + tags/summary JSON + HTML 用户/AI roles + run-control plan pausing/paused + dual-fail toast + skip markPausing + protected-paths .ssh/.aws/.kube + token/pem/sqlite + .config/gcloud + pi-api array/function as-is + reassignment + ssrf AWS IMDS IPv6 + uppercase host + empty/malformed + terminal curl\|sh + git --force word form not -f + chmod 777/dd if= + chown not high residual；desktop unit **209 PASS**（6 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave194-evidence.txt` + `/tmp/nsis-probe-wave194.txt`）— **scoreboard 不通胀** |
| wave-195 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；tray index0/2/3/4 candidates + crash renderer dispose + uncaught origin + child-process-gone + ipc array/function ok + legacy code+fallback err + first-launch 1/0 tokens + false key still first + write true/false + mutation sync/async reject advances + keyed transform + updater available/progress/not-available events residual；desktop unit **158 PASS**（6 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave195-evidence.txt` + `/tmp/nsis-probe-wave195.txt`）— **scoreboard 不通胀** |
| wave-196 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；log-redaction empty/sk-/Error stack + nested Buffer + path-canonical missing workspace + .ssh reject + command-risk git -f normal/--force high + id_ed25519 + irm\|pwsh + fuzzy case-insensitive + ordered camel + out-of-order fail + subscription empty-array + re-ensure + web-security malformed + 0.0.0.0/::1 deny + sounds preserve fields + missing key defaults + logger circular + console.error fallback residual；desktop unit **224 PASS**（7 files）；shared-types command-risk **66 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave196-evidence.txt` + `/tmp/nsis-probe-wave196.txt`）— **scoreboard 不通胀** |
| wave-197 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；agent-modes compose dual-flag + plan→build BUILD_SWITCH + plan read tools/git + events tool_execution_update/message_start/end + usage/context/custom + app-settings enabled false nested + dream interval + composeWorkflow legacy + diagnostics omit sessions/logPath + versions/details copy + format minute boundary + isNumberOrUndefined/isValidTimestamp + protected-paths secrets.txt/private.key/credentials.yml + isPathInside parent/trailing-sep + theme apply light/dark + getInitialTheme token + font clamp floors + overlay fractional negative workArea + zero workArea residual；desktop unit **193 PASS**（6 files）；shared-types events+app-settings **50 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave197-evidence.txt` + `/tmp/nsis-probe-wave197.txt`）— **scoreboard 不通胀** |
| wave-198 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；ssrf localhost/LAN allow + 169.254 block + fe80 variants + tool-call empty toolCalls new object + modern-only identity + workspace lastActiveAt 0 + null fallback + terminal git clean/npm publish/kubectl/reg + sessionMatches case/empty + group filter empty workspaces + notifications canNotify gates + helpers no-op when disabled + ipc-guards wrong-brand legacy + SETTINGS tabs endpoints residual；desktop unit **208 PASS**（6 files）；shared-types ipc-guards **27 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave198-evidence.txt` + `/tmp/nsis-probe-wave198.txt`）— **scoreboard 不通胀** |
| wave-199 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；git ahead/behind + default branch + gitDiff/commit/createBranch + export HTML escape + string timestamps + toolCalls details + omit tags/summary + generated-ui table/form/chart/callout/metric + column defaults + mention nearest @ + empty query + email-like + resolve splice + tool-call status aliases + reviveDate + normalizeToolCalls filters + isError preference + mutation-queue serial concurrent + keyed throw + sync/async return + window-lifecycle restore minimized + ensureTray reuse + beginQuit clears tray + mimocode build-only defaults + memory nested + maxMode unsupported + workflow inherits composeModeSupported + localfile .ssh 403 + net.fetch 404 + URL-encoded path residual；desktop unit **284 PASS**（9 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave199-evidence.txt` + `/tmp/nsis-probe-wave199.txt`）— **scoreboard 不通胀** |
| wave-200 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；main-window platform tokens + chrome key matrix + project-scripts coerce + manager matrix + pi-api primitives/null + live reassignment + ipc code/fallback partials + wrong-brand non-string + single-instance secondary no listener + double register last-wins fake + crash double attach dispose isolation + run-control task toast + agentsAbort only + empty ids + command-risk empty/mkfs/checkout -- ./publish/reg/uninstall + terminal mirrors + tray null six paths + prefer first residual；desktop unit **223 PASS**（9 files）；shared-types command-risk **68 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave200-evidence.txt` + `/tmp/nsis-probe-wave200.txt`）— **scoreboard 不通胀** |
| wave-201 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；web-security same-origin + file same-path + external schemes + log-redaction sensitive keys + Bearer/Cookie + circular/bigint + subscription void/array LIFO + cleanup idle + protected-paths .aws/.kube/.docker + pem/sqlite + outside workspace + diagnostics sessionStats preference + storageRoot basename + logPath missing + logger Error stack + piAPI throw fallback + debug + path-canonical allow nested + .env.local deny + ipc-guards factory params + SETTINGS tabs exact order residual；desktop unit **236 PASS**（7 files）；shared-types ipc-guards **29 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave201-evidence.txt` + `/tmp/nsis-probe-wave201.txt`）— **scoreboard 不通胀** |
| wave-202 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；agent-modes shell metachar reject + filePath/relativePath aliases + plan_write empty + registry defaults + session-grouping favorite false parent/child + thinking when loaded + multi-session sort + ssrf metadata case/https/port + userinfo public allow + 0.0.0.0/::1 allow + file scheme reject + command-risk reset --hard/clean -f/del /q + kubectl/terraform/dd + format null end→now + invalid end 进行中 + isNumberOrUndefined NaN/Infinity + workspace-selection empty/equal-tie + newer lastActiveAt + protected-paths .gnupg/.azure/.gcloud + id_rsa/known_hosts/.pypirc/pfx/jks + sibling-prefix + web-security allowDevLocalhost false + malformed empty + log-redaction null/Date pass-through + MaxDepth + sk- shapes + tool-call-normalization identity non-array + legacy id/name prefer residual；desktop unit **382 PASS**（9 files）；shared-types command-risk **70 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave202-evidence.txt` + `/tmp/nsis-probe-wave202.txt`）— **scoreboard 不通胀** |
| wave-203 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；theme system token + string font parseInt + applyTheme system matchMedia + clamp return + first-launch TRUE/FALSE/yes/no fallback + mark upgrades false→true + fuzzy first mid-token vs hyphen segment + empty-text/single-char (main+renderer) + terminal-command reset/clean/del/dd + kubectl/terraform ladder + sounds clamp 1.5/99/-0.01 + partial JSON undefined fields + notifications String(boolean) exact true + Chinese helper titles + subscription nullish array LIFO + double cleanup + app-settings defaultMode coerce + partial planMode merge + events tool_execution_start/queue/auto_retry/tool_execution_end residual；desktop unit **254 PASS**（8 files）；shared-types **54 PASS**（2 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave203-evidence.txt` + `/tmp/nsis-probe-wave203.txt`）— **scoreboard 不通胀** |
| wave-204 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；classifier shell-rc/.pi/System32 high + pipe sh/bash/dd edit + git fetch/pull/stash/tag read + bare git/npm edit + BASH/SHELL lowercased + >>/awk print> + pending-edits getByToolCallId after approve/remove + autoApprove independent + list newest-first + queue-store agent_end clears autoRetry + blank tool update + items cap 12 + empty queue_update + extension_error reason/whitespace + approval-store clearChanges rejects waiters + timeoutMs 0 + multi-line write diff + edit basename + helpers schema fail skip + updater/piDriver stringify + import schema reject + mention mid-token/path-like + mutation-queue fail-not-poison/keyed multi-step/independent queues + ipc partition arrays/bools + empty-string err + project-scripts special/empty + pi-api nested ref/symbol/bigint + path-canonical outside deny/missing leaf allow/.env deny + ipc-guards brand short-circuit + params undefined + SETTINGS tabs 10 residual；desktop unit **405 PASS**（11 files）；shared-types ipc-guards **32 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave204-evidence.txt` + `/tmp/nsis-probe-wave204.txt`）— **scoreboard 不通胀** |
| wave-205 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；main-window win32/linux frameless + darwin hiddenInset + perf backgroundThrottling + single-instance restore ref + secondary exit order + multi second-instance + tray checkedPaths order + cwd last-hit + exists first-stop + crash uncaught origin/error + renderer dispose independent + double dispose + diagnostics sessions messageCount + empty/missing zero + log read/omit + logger bool/num + circular + non-function log + Error no-stack + schemas terminal bounds + Task parentId/status + Goal/Subagent strict + toolCall status + thinking levels + PlanUpdate/archive residual；desktop unit **253 PASS**（7 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave205-evidence.txt` + `/tmp/nsis-probe-wave205.txt`）— **scoreboard 不通胀** |
| wave-206 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；command-risk curl\|sh/iwr\|iex/chmod 777 + force-with-lease + Remove-Item secrets + terraform import + protected-paths .npmrc/.netrc case + sibling/escape + ssrf exact metadata only + bare metadata host allow + localhost allow by design + web-security same-origin allowDevLocalhost false + file/about/javascript deny + log-redaction Bearer/Cookie + nested APIKEY non-mutating + overlay margin 14 + round-then-place + tool-call nested toolCall + arguments input + output>result + partial contentIndex + persistence strip args/result residual；desktop unit **288 PASS**（6 files）；shared-types command-risk **72 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave206-evidence.txt` + `/tmp/nsis-probe-wave206.txt`）— **scoreboard 不通胀** |
| wave-207 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；window-lifecycle restore without beforeShow + close after requestQuit no preventDefault + hasTray/isQuitting initial + localfile bare id_rsa/known_hosts/secrets.db 403 + .kube/.docker dirs + nested ordinary src 200 + mimocode planModeSupported false drops plan tools + workflowSupported overrides compose false + dream/distill unsupported + tool strip memory/history/task/actor + updater whitespace releaseNotes null + progress defaults 0 + downloaded 100% + app-update.yml/string network errors + dispose clears listeners + agent-modes normalize unknown/LH-off + goalSlashCommands clone isolation + plan→build empty body + generated-ui v1 title-only + form kind:text + timeline + null cells residual；desktop unit **175 PASS**（6 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave207-evidence.txt` + `/tmp/nsis-probe-wave207.json`）— **scoreboard 不通胀** |
| wave-208 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；export HTML title XSS escape + empty meta 0 + markdown usage defaults 0 + user **你** + HTML role/escape + session-grouping depth cap 4 + cycle depth 2 + favorite first + empty ws drop + case-insensitive match + run-control IpcError.fallback toast + plan dual-fail 暂停执行失败 + missing stop IPC + format toDate/ISO/ms + multi-minute duration + workspace-selection solo/createdAt-only + generic field preserve + tray-icon null-when-none + first-wins residual；desktop unit **255 PASS**（6 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave208-evidence.txt` + `/tmp/nsis-probe-wave208.json`）— **scoreboard 不通胀** |
| wave-209 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；sounds enable preserves volume + invalid JSON defaults + volume 0/1 stick + notifications canNotify permission+enabled + send no-op default + exact true/false store + first-launch missing fallback + 1/0 tokens + mark idempotent + theme resolve light/dark + garbage→system + editor/diff floors + CSS vars + terminal-command curl\|sh/chmod 777/force-with-lease draft + reg delete/npm -g draft + subscription ensure idempotent + void setup re-ensure residual；desktop unit **213 PASS**（6 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave209-evidence.txt` + `/tmp/nsis-probe-wave209.json`）— **scoreboard 不通胀** |
| wave-210 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；pi-api boolean/function pass-through + delete→undefined + project-scripts unknown/null/undefined manager→npm run + unicode/path-like script names + ipc partition 0/"" success + brand-only IpcError err + string code+fallback extras err + fuzzy renderer case-insensitive full/camel ordered 25 + reverse 0 + fuzzy main unicode ordered/mid-token/longer-query + ipc-guards null/array/string reject + ipcError params + SETTINGS first/last model/about + app-settings partial workflow maxDepth merge + undefined clone no shared ref residual；desktop unit **125 PASS**（5 files）；shared-types **65 PASS**（2 files）；combined **190 PASS**（7 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave210-evidence.txt` + `/tmp/nsis-probe-wave210.json`）— **scoreboard 不通胀** |
| wave-211 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；logger piAPI.log throw→console + null/undefined stringify + empty args + mention win path/unicode + whitespace-after-@ kill + empty query resolve + log-redaction bigint/symbol/function/Buffer + Error sk- message/stack + x-api-key + ssrf fe80/fe9/fea/feb block + fe70 allow + data/blob/ws reject + trailing-dot host + path-canonical .ssh/id_rsa/.npmrc deny + ordinary nested allow + empty/.. deny + tool-call null id/name + args>input>arguments + output>result + non-array empty + command-risk empty/ws normal + sudo/mkfs/npm publish + rm -rf/-fr + git clean -fd residual；desktop unit **296 PASS**（6 files）；shared-types command-risk **74 PASS**；combined **370 PASS**（7 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave211-evidence.txt` + `/tmp/nsis-probe-wave211.json`）— **scoreboard 不通胀** |
| wave-212 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；tool-category network regex compound + CORE ls/list/find + http not core + protected-paths .env variants/token-secret filenames + .ssh/.docker/.kube + outside workspace + single-instance secondary no listener + primary restore + crash child-process-gone + dispose stops events + format 0ms/45.0s/1m + invalid end 进行中 + events discriminant agent/turn + tool_execution_update/queue_update residual；desktop unit **196 PASS**（5 files）；shared-types events **27 PASS**；combined **223 PASS**（6 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave212-evidence.txt` + `/tmp/nsis-probe-wave212.json`）— **scoreboard 不通胀** |
| wave-213 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；main-window-options unknown platform frameless + fresh objects + diagnostics sessionStats prefer + clone details/versions + storageRoot basename + missing log empty + tray-icon null 6 paths + cwd-only last + workspace-selection lastActiveAt 0 not nullish + run-control empty ids no toast + successful stop true + defaults .env ask + write/edit/bash allow residual；desktop unit **186 PASS**（6 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave213-evidence.txt` + `/tmp/nsis-probe-wave213.json`）— **scoreboard 不通胀** |
| wave-214 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；agent-modes plan file_write/file_edit + nested plan md + path escape block + subshell/backtick deny + compose dual workflow flags + runtime-policy git after &/newline + filterActiveTools normalize + frozen permissions + session-grouping thinking match + parent-before-child + workspace order + terminal iwr/irm|iex + git reset/clean draft + first-launch storage throw fallback + theme cascade tokens + watchSystemTheme + subscription independent managers residual；desktop unit **290 PASS**（7 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave214-evidence.txt` + `/tmp/nsis-probe-wave214.json`）— **scoreboard 不通胀** |
| wave-215 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；evaluate multi-ruleset last-wins + ~/ $HOME expand + EDIT_TOOLS star-deny + non-star keep + fts quote-split tokens + token order + punctuation null + session-path stable/48-frag/reject separators + sounds volume clamp + partial JSON + play no-op disabled + notifications exact-true only + notify titles + Notification undefined denied + export markdown toolCalls/usage + HTML escape + JSON ISO residual；desktop unit **232 PASS**（6 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave215-evidence.txt` + `/tmp/nsis-probe-wave215.json`）— **scoreboard 不通胀** |
| wave-216 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；wildcard multiline/s + trailing space-star + literal dots + web-security custom schemes + IPv4 http(s) external + same-origin hash/default ports + will-navigate file query + memory-path-guard checkpoint-writer sessions {checkpoint,notes} only（MEMORY.md denied）+ projects MEMORY + tasks + task-bound own TID + mutation-queue object identity/undefined keyed/seq+concurrent + generated-ui maxBytes reject + slice-then-sanitize unknown drop → maxSections-1 + command-risk del /s /q high + terraform apply w/o auto-approve normal + git checkout -- . high + npm uninstall w/o -g normal residual；desktop unit **227 PASS**（5 files）；shared-types command-risk **76 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave216-evidence.txt` + `/tmp/nsis-probe-wave216.json`）— **scoreboard 不通胀** |
| wave-217 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；git-service unstage restore --staged + diff --staged + batch add abort protected + createBranch space reject + checkout invalidArgs fallback/params + status staged-only ignore + protected-paths .pypirc/id_*.pub/known_hosts + .gnupg/.azure/.gcloud + isPathInside equal/escape + fuzzy main/renderer segment 75 vs space/_ 50 + ordered-char order + formatDuration negative 0s/omitted end now + toDate bool reject + ssrf 169.254/16 + metadata case + private allow + log-redaction Date/null/api-key aliases + Error no-stack + app-settings null defaults + plan/compose mode + workflow wins composeWorkflow residual；desktop unit **330 PASS**（7 files）；shared-types app-settings **33 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave217-evidence.txt` + `/tmp/nsis-probe-wave217.json`）— **scoreboard 不通胀** |
| wave-218 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；path-canonical missing nested leaf + readLinkTarget missing + .env.local deny + tool-call status aliases success/executed/failed/executing + persistence drop args/result + empty id trim + nested toolCall + mention space-after-@ kill + last @ + resolve span + logger console.debug + non-fn piAPI.log + Error no-stack + events auto_retry/compaction/session_info/thinking + ipc-guards brand/legacy + SETTINGS tabs unique + workspace-selection empty/single/tie + lastActiveAt fallback residual；desktop unit **217 PASS**（5 files）；shared-types **65 PASS**（2 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave218-evidence.txt` + `/tmp/nsis-probe-wave218.json`）— **scoreboard 不通胀** |
| wave-219 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；defaults doom_loop/question/.env.example/config.env ask + whitelist external + tool-category fileWrite/shell/plan_write + main-window darwin hiddenInset + win/linux frameless + tray first-existing/null-6 + terminal del/kubectl/terraform/curl draft + session-path stable/charset + diagnostics sessionStats prefer/clone residual；desktop unit **207 PASS**（7 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave219-evidence.txt` + `/tmp/nsis-probe-wave219.json`）— **scoreboard 不通胀** |
| wave-220 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；sounds volume clamp/invalid JSON/default 0.5 + notifications exact-true + Chinese titles + canNotify + first-launch mark/1-0 vs yes/True + single-instance secondary exit/primary second-instance + export toolCalls/usage defaults/title quotes vs body &<> + command-risk dd/mkfs/reg/rmdir/chmod/terraform import/force push residual；desktop unit **207 PASS**（5 files）；shared-types command-risk **78 PASS**；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave220-evidence.txt` + `/tmp/nsis-probe-wave220.json`）— **scoreboard 不通胀** |
| wave-221 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；agent-modes goal clone/longHorizon-off/plan_write.mkdir/git status vs commit/network deny + theme round clamp/non-finite/invalid storage/matchMedia missing + session-grouping empty-query/messagesLoaded/depth-cap/favorite + subscription ensure idempotent/re-sub + crash multi-renderer isolate + localfile parent-escape/space path/404 residual；desktop unit **228 PASS**（6 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave221-evidence.txt` + `/tmp/nsis-probe-wave221.json`）— **scoreboard 不通胀** |
| wave-222 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；window-lifecycle restore/minimized/beforeShow/beginQuit/tray singleton + overlay bounds clamp/size keep/permission main + run-control plan pause toast + pi-api null/delete + ipc nested brand success + mimocode master-off checkpoint-writer/planModeSupported residual；desktop unit **177 PASS**（6 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave222-evidence.txt` + `/tmp/nsis-probe-wave222.json`）— **scoreboard 不通胀** |
| wave-223 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；wildcard ?/* path normalize/regex escape + runtime-policy git wrappers/plan shell deny/plan_write mode-required + evaluate permission wildcards/~/$HOME/EDIT_TOOLS last-wins + fts underscore/hyphen/quote/CJK + classifier iex/bcdedit/schtasks/reg/format + high paths + preview trim residual；desktop unit **292 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave223-evidence.txt` + `/tmp/nsis-probe-wave223.json`）— **scoreboard 不通胀** |
| wave-224 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；judge-prompt multiline/VerdictSchema + memory-path-guard non-memory/projectId/CC/dream tasks + pending-edits clear/cache/track fields + task-id other-session/null parent/not-found + paths nested progress/.. reject residual；desktop unit **205 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave224-evidence.txt` + `/tmp/nsis-probe-wave224.json`）— **scoreboard 不通胀** |
| wave-225 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；web-security open-handler deny+openExternal http only + navigate/redirect same-origin + updater releaseNotes array/checking→not-available + events message_update/queue/tool_execution + approval RiskLevel/DeferredEdit residual；desktop unit **82 PASS**（2 files）；shared-types **34 PASS**（2 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave225-evidence.txt` + `/tmp/nsis-probe-wave225.json`）— **scoreboard 不通胀** |
| wave-226 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；directives create vs edit/BUILD_SWITCH lift/compose docsDir windows+empty + session-path only-dots→session/case hash/userData join + approval-bridge resolve-false/double-resolve/select method + plan-prompt 7-line/plan_write residual；desktop unit **81 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave226-evidence.txt` + `/tmp/nsis-probe-wave226.json`）— **scoreboard 不通胀** |
| wave-227 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；reconcile delete-fail skip/size reindex/body trim + IpcError params/legacy + plan-execution DONE/PLAN_DONE + spawn-handler distill wire/empty lastAssistantText ?? + first-launch true-1/false-0 residual；desktop unit **51 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave227-evidence.txt` + `/tmp/nsis-probe-wave227.json`）— **scoreboard 不通胀** |
| wave-228 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；settings-nav unique tabs/case search/AND + plan-sync empty normalize/user skip + session-summary sinceMs/empty transcript + asset-inventory multi-ext sourcePath residual；desktop unit **27 PASS**（4 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave228-evidence.txt` + `/tmp/nsis-probe-wave228.json`）— **scoreboard 不通胀** |
| wave-229 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；plan-utils CRLF/preferred executed fallthrough/terminal status + file-scanner depth0/.pi-desktop/release + settings-bridge reconcile-off miss/floor0 residual；desktop unit **54 PASS**（3 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave229-evidence.txt` + `/tmp/nsis-probe-wave229.json`）— **scoreboard 不通胀** |
| wave-230 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；claude empty/outside path + codex cwd-empty/empty-jsonl + registry listAll/listSpawnable + pi-packages setQuery/install refresh/remove fail residual；desktop unit **51 PASS**（4 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave230-evidence.txt` + `/tmp/nsis-probe-wave230.json`）— **scoreboard 不通胀** |
| wave-231 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；native-session-fork pure slice/normalize/timestamp + skillhub missing fields/non-array + memory-service empty search/listRecent limit + pi-driver detectSync residual；desktop unit **35 PASS**（4 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave231-evidence.txt` + `/tmp/nsis-probe-wave231.json`）— **scoreboard 不通胀** |
| wave-232 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；task-service unknown ws/all-completed/default-agent list + sdk outer-first resolve/reset rethrow + session-summary combined filters/empty transcript/Date order residual；desktop unit **35 PASS**（3 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/wave232-evidence.txt` + `/tmp/nsis-probe-wave232.json`）— **scoreboard 不通胀** |
| wave-233 | residual unit depth | desktop 106 PASS / 5 files; typecheck PASS; fuzzy/fts/theme/list-local-skills/checkpoint product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-21 20:32 UTC |
| wave-234 | residual unit depth | desktop 161 PASS / 5 files; typecheck PASS; wildcard/tool-category/defaults/toast/task-id product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-21 20:38 UTC |
| wave-235 | residual unit depth | desktop 104 PASS / 4 files; typecheck PASS; attachments/skillhub/main-window/mutation-queue product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-21 23:57 UTC |
| wave-236 | residual unit depth | desktop 131 PASS / 3 files; typecheck PASS; evaluate/runtime-policy/first-launch product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 00:02 UTC |
| wave-237 | residual unit depth | desktop 96 PASS / 3 files; typecheck PASS; markdown-index/memory-service/shortcuts product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 00:07 UTC |
| wave-238 | residual unit depth | desktop 161 PASS / 3 files; typecheck PASS; format/mention-parser/logger product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 00:10 UTC |
| wave-239 | residual unit depth | desktop 96 PASS / 3 files; typecheck PASS; tool-call/pi-status-store/updater-store product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 00:16 UTC |
| wave-240 | residual unit depth | desktop 67 PASS / 2 files + shared-types 185 PASS / 5 files; typecheck PASS; runtime-feature/insert-reminders/command-risk/app-settings product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 00:21 UTC |
| wave-241 | residual unit depth | desktop 87 PASS / 3 files + shared-types events 33 PASS; typecheck PASS; session-path/agent-mode/approval-diff/events product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 00:23 UTC |
| wave-242 | residual unit depth | desktop 76 PASS / 3 files + shared-types ipc-guards 38 PASS; typecheck PASS; fork-pure/agent-info/plan-prompt/ipc-guards product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 00:27 UTC |
| wave-243 | residual unit depth | desktop 193 PASS / 4 files; typecheck PASS; ssrf-guard/fts-query/fuzzy-match/wildcard product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 00:32 UTC |
| wave-244 | residual unit depth | desktop 157 PASS / 4 files; typecheck PASS; tool-category/memory-path-guard/theme/defaults product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 08:38 UTC |
| wave-245 | residual unit depth | desktop 197 PASS / 4 files; typecheck PASS; classifier/pending-edits/permission-store/skills-store product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 08:44 UTC |
| wave-246 | residual unit depth | desktop 125 PASS / 3 files + shared-types command-risk 82 PASS; typecheck PASS; log-redaction/subscription-manager/terminal-command/command-risk product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 08:48 UTC |
| wave-247 | residual unit depth | desktop 213 PASS / 4 files; typecheck PASS; evaluate/runtime-policy/protected-paths/workspace-selection product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 08:52 UTC |
| wave-248 | residual unit depth | desktop 158 PASS / 4 files; typecheck PASS; main-window-options/tray-icon/single-instance/web-security product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 00:58 UTC |
| wave-249 | residual unit depth | desktop 138 PASS / 4 files; typecheck PASS; path-canonical/local-file-protocol/crash-diagnostics/window-lifecycle product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 01:00 UTC |
| wave-250 | residual unit depth | desktop 95 PASS / 4 files; typecheck PASS; diagnostics/updater/desktop-overlay/sqlite-session product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 01:03 UTC |
| wave-251 | residual unit depth | desktop 156 PASS / 3 files + shared-types app-settings 37 PASS; typecheck PASS; agent-modes/mimocode/ssrf/app-settings product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 01:06 UTC |
| wave-252 | residual unit depth | desktop wildcard 43 PASS + shared-types 79 PASS / 3 files; typecheck PASS; wildcard/ipc-guards/events/approval product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 01:10 UTC |
| wave-253 | residual unit depth | desktop 66 PASS / 2 files + shared-types command-risk 84 PASS; typecheck PASS; command-risk/git-service/guarded-tools product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 01:13 UTC |
| wave-254 | residual unit depth | desktop 161 PASS / 4 files; typecheck PASS; fts-query/file-scanner/format/fuzzy-match product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 01:16 UTC |
| wave-255 | residual unit depth | desktop 131 PASS / 4 files; typecheck PASS; theme/terminal-command/first-launch/mention-parser product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 01:21 UTC |
| wave-256 | residual unit depth | desktop 180 PASS / 4 files; typecheck PASS; export/tool-call/session-grouping/project-scripts product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 01:27 UTC |
| wave-257 | residual unit depth | desktop 174 PASS / 4 files; typecheck PASS; sounds/notifications/ipc/logger product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 01:29 UTC |
| wave-258 | residual unit depth | desktop 155 PASS / 4 files; typecheck PASS; mutation-queue/subscription-manager/pi-api/protected-paths product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 01:33 UTC |
| wave-259 | residual unit depth | desktop 196 PASS / 4 files; typecheck PASS; workspace-selection/web-security/log-redaction/generated-ui product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 01:36 UTC |
| wave-260 | residual unit depth | desktop 196 PASS / 2 files + shared-types 76 PASS / 2 files; typecheck PASS; ssrf-guard/classifier/events/app-settings product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 01:40 UTC |
| wave-261 | residual unit depth | desktop 109 PASS / 3 files + shared-types command-risk 86 PASS; typecheck PASS; path-canonical/single-instance/main-window-options/command-risk product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 01:44 UTC |
| wave-262 | residual unit depth | desktop 104 PASS / 3 files + shared-types approval 6 PASS; typecheck PASS; tray-icon/window-lifecycle/mimocode-runtime-port/approval product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 01:50 UTC |
| wave-263 | residual unit depth | desktop 129 PASS / 4 files + shared-types ipc-guards 42 PASS; typecheck PASS; local-file/crash-diagnostics/diagnostics/overlay/ipc-guards product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 01:58 UTC |
| wave-264 | residual unit depth | desktop 234 PASS / 5 files; typecheck PASS; list-local-skills/fts-query/evaluate/runtime-policy/agent-modes product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 02:06 UTC |
| wave-265 | residual unit depth | desktop 153 PASS / 4 files + shared-types command-risk 88 PASS; typecheck PASS; memory-path-guard/file-scanner/guarded-tools/updater/command-risk product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 02:11 UTC |
| wave-266 | residual unit depth | desktop 144 PASS / 4 files + shared-types events 39 PASS; typecheck PASS; tool-category/wildcard/defaults/theme/events product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 02:16 UTC |
| wave-267 | residual unit depth | desktop 190 PASS / 5 files + shared-types app-settings 41 PASS; typecheck PASS; terminal-command/first-launch/mention-parser/project-scripts/session-grouping/app-settings product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 02:21 UTC |
| wave-268 | residual unit depth | desktop 182 PASS / 4 files + shared-types approval 8 PASS; typecheck PASS; sounds/notifications/ipc/logger/approval product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 02:27 UTC |
| wave-269 | residual unit depth | desktop 194 PASS / 5 files; typecheck PASS; git-service/export/tool-call/fuzzy-match/sqlite-session-repository product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 02:34 UTC |
| wave-270 | residual unit depth | desktop 189 PASS / 5 files; typecheck PASS; tool-call-normalization/run-control/mutation-queue/fuzzy-match/subscription-manager product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 02:38 UTC |
| wave-271 | residual unit depth | desktop 304 PASS / 5 files; typecheck PASS; ssrf-guard/web-security/log-redaction/protected-paths/generated-ui product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 02:42 UTC |
| wave-272 | residual unit depth | desktop 68 PASS / 2 files + shared-types 218 PASS / 4 files; typecheck PASS; path-canonical/pi-api/ipc-guards/events/app-settings/command-risk product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 02:46 UTC |
| wave-273 | residual unit depth | desktop 191 PASS / 5 files; typecheck PASS; diagnostics/crash-diagnostics/tray-icon/overlay/fts-query product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 02:48 UTC |
| wave-274 | residual unit depth | desktop 244 PASS / 5 files; typecheck PASS; memory-path-guard/file-scanner/evaluate/list-local-skills/runtime-policy product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 02:58 UTC |
| wave-275 | residual unit depth | desktop 209 PASS / 5 files; typecheck PASS; tool-category/wildcard/defaults/agent-modes/guarded-tools product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 03:02 UTC |
| wave-276 | residual unit depth | desktop 243 PASS / 4 files + shared-types 92 PASS / 1 file; typecheck PASS; protected-paths/path-canonical/ssrf-guard/fts-query/command-risk product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 03:06 UTC |
| wave-277 | residual unit depth | desktop 225 PASS / 5 files; typecheck PASS; log-redaction/web-security/tray-icon/diagnostics/theme product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 03:09 UTC |
| wave-278 | residual unit depth | desktop 167 PASS / 4 files + shared-types 43 PASS / 1 file; typecheck PASS; mutation-queue/fuzzy-match/subscription-manager/events product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 03:13 UTC |
| wave-279 | residual unit depth | desktop 89 PASS / 2 files + shared-types 101 PASS / 3 files; typecheck PASS; ipc-guards/app-settings/approval/crash-diagnostics/generated-ui product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 03:18 UTC |
| wave-280 | residual unit depth | desktop 208 PASS / 5 files; typecheck PASS; first-launch/terminal-command/mention-parser/overlay/project-scripts product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 03:20 UTC |
| wave-281 | residual unit depth | desktop 273 PASS / 5 files; typecheck PASS; sounds/notifications/session-grouping/tool-call/run-control product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 03:24 UTC |
| wave-282 | residual unit depth | desktop 207 PASS / 5 files; typecheck PASS; export/sqlite-session-repository/git-service/format/main-window-options product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 03:30 UTC |
| wave-283 | residual unit depth | desktop 181 PASS / 5 files; typecheck PASS; single-instance/local-file-protocol/list-local-skills/evaluate/updater product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 03:38 UTC |
| wave-284 | residual unit depth | desktop 238 PASS / 4 files + shared-types 94 PASS / 1 file; typecheck PASS; wildcard/defaults/ssrf-guard/protected-paths/command-risk product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 03:41 UTC |
| wave-285 | residual unit depth | desktop 247 PASS / 5 files; typecheck PASS; web-security/log-redaction/path-canonical/fts-query/file-scanner product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 03:45 UTC |
| wave-286 | residual unit depth | desktop 195 PASS / 4 files + shared-types 12 PASS / 1 file; typecheck PASS; agent-modes/tool-category/memory-path-guard/theme/approval product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 03:48 UTC |
| wave-287 | residual unit depth | desktop 163 PASS / 3 files + shared-types 92 PASS / 2 files; typecheck PASS; runtime-policy/tray-icon/diagnostics/events/app-settings product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 03:55 UTC |
| wave-288 | residual unit depth | desktop 178 PASS / 4 files + shared-types 48 PASS / 1 file; typecheck PASS; fuzzy-match/mutation-queue/subscription-manager/crash-diagnostics/ipc-guards product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 04:01 UTC |
| wave-289 | residual unit depth | desktop 202 PASS / 5 files; typecheck PASS; first-launch/terminal-command/mention-parser/overlay-policy/workspace-selection product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 04:05 UTC |
| wave-290 | residual unit depth | desktop 233 PASS / 5 files; typecheck PASS; window-lifecycle/project-scripts/format/session-grouping/theme product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 04:08 UTC |
| wave-291 | residual unit depth | desktop 229 PASS / 5 files; typecheck PASS; tool-call-normalization/task-id/sounds/notifications/tool-call product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 04:12 UTC |
| wave-292 | residual unit depth | desktop 111 PASS / 5 files; typecheck PASS; plan-prompt/judge-prompt/directives-pure/project-detector/agent-info product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 04:15 UTC |
| wave-293 | residual unit depth | desktop 57 PASS / 5 files; typecheck PASS; session-path/IpcError/tab-defs/settings-nav/native-session-fork product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 04:19 UTC |
| wave-294 | residual unit depth | desktop 43 PASS / 5 files; typecheck PASS; plan-execution/DiffViewer/GeneratedUiTable/GeneratedUiForm/DateGrouped pure product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 04:28 UTC |
| wave-295 | residual unit depth | desktop 182 PASS / 5 files; typecheck PASS; plan-utils/ssrf-guard/diff-parser/file-tree/project-detector product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 04:34 UTC |
| wave-296 | residual unit depth | desktop 209 PASS / 4 files + shared-types 97 PASS / 1; typecheck PASS; command-risk/format/ipc/terminal-command/workspace-selection product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 04:39 UTC |
| wave-297 | residual unit depth | desktop 260 PASS / 5 files; typecheck PASS; tool-category/evaluate/protected-paths/wildcard/theme product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 04:44 UTC |
| wave-298 | residual unit depth | desktop 313 PASS / 5 files; typecheck PASS; classifier/runtime-policy/log-redaction/plan-prompt/fuzzy-match product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 04:47 UTC |
| wave-299 | residual unit depth | desktop 237 PASS / 5 files; typecheck PASS; agent-info/session-grouping/directives/path-canonical/first-launch product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 04:51 UTC |
| wave-300 | residual unit depth | desktop 162 PASS / 5 files; typecheck PASS; usage-aggregation/settings-nav/file-workspace/tool-call/defaults product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 04:59 UTC |
| wave-301 | residual unit depth | desktop 110 PASS / 5 files; typecheck PASS; native-session-fork/task-id/session-path/IpcError/tool-call-normalization product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 05:03 UTC |
| wave-302 | residual unit depth | desktop 157 PASS / 5 files; typecheck PASS; web-security/main-window-options/project-scripts/tab-defs/judge-prompt product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 05:05 UTC |
| wave-303 | residual unit depth | desktop 145 PASS / 4 files + shared-types 14 PASS / 1; typecheck PASS; desktop-overlay/single-instance/mention-parser/pi-driver-detect/approval product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 05:09 UTC |
| wave-304 | residual unit depth | desktop 95 PASS / 5 files; typecheck PASS; diff-parser/GeneratedUiChart/project-detector/plan-utils/GeneratedUiTable product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 05:26 UTC |
| wave-305 | residual unit depth | desktop 72 PASS / 5 files; typecheck PASS; DiffViewer/DateGrouped/GeneratedUiForm/plan-execution/file-tree product-aligned; NSIS still BLOCKED (admin=False) | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 | 2026-07-22 05:30 UTC |
| wave-172 residual | 隔离 re-probe 仍 **BLOCKED**（admin=False / Sandbox missing / Docker missing；**未**装机）；theme getDiff/getInitialFont/system-data-theme + single-instance lock→listener order + localfile empty-ws/sibling-prefix/unicode + mimocode planModeSupported/master-off-tools/checkpoint-writer + format 29/30-day residual；desktop unit **107 PASS**（5 files）；typecheck **PASS**；**K-004/014/015 仍 BLOCKED**（`/tmp/unit-wave172-desktop.log` + `/tmp/wave172-evidence.txt` + `/tmp/nsis-probe-wave172.txt`）— **scoreboard 不通胀** |

相对 2026-07-18 报告，单元/集成层保持全绿；wave-6 全量 suite 以 postfix 残差已绿为前提重跑，刷新历史 100/5/2/6 口径；唯一 fail 为 a11y 双按钮 strict-mode，已定点修并 7/7 复验。

## 全量 Electron E2E scoreboard（wave-6，权威）

日志：`/tmp/e2e-full-wave6-20260721-040535.log`（镜像 zcode exec `call-473bb480-…-126-stdout.log`）  
命令：`pnpm --filter @pi-desktop/desktop e2e:build`  
口径：**不**等同于矩阵 174 全绿；suite 内含 skip / env-blocked；**禁止**把 116 suite-pass 解读为 174 全绿。

| 结果 | 数量 | 说明 |
|---|---|---|
| passed | **116** | Playwright list 结果（含 retries 后通过项） |
| failed（首轮 full suite） | **1** | `command-palette-callbacks`：有 workspace 时切换终端（strict mode 双按钮） |
| flaky | **0** | 本波 full suite 无 flaky 报告 |
| skipped | **6** | long-horizon-live + deep-interactive（`RUN_*` 未开） |

### wave-6 唯一 fail → 修复 → 残差复验

| Spec | 首轮 | 根因 | 修复 | 残差复验 |
|---|---|---|---|---|
| `command-palette-callbacks` 切换终端 | FAIL | TerminalPanel 空态 CTA「+ 新建终端」与 tab-bar `aria-label="新建终端"` 的「+」同时匹配 `/新建终端/` | E2E 改精确 `name: '+ 新建终端'` | **7/7 PASS**（`/tmp/e2e-palette-residual-wave6.log`） |

有效口径（full suite + residual）：**suite 自动化主路径绿；1 fail 已闭环**。仍 **6 skipped**（live provider 门禁）。

## 历史 scoreboard（postfix，已 superseded）

日志：`/tmp/e2e-full-postfix.log`（wave-6 前）  
**100 passed / 5 failed / 2 flaky / 6 skipped** — 5 fail 已在早波 residual 消化（deep-use / m6 / programmer / settings-pi-config-sync / visual-audit）；仅作时间线保留，**不以本段为当前 suite 权威数字**。

### 首轮失败 → 修复 → 残差复验

| Spec | 首轮 | 根因 | 修复 | 残差复验 |
|---|---|---|---|---|
| `deep-use-surface-audit` | FAIL | goal 种子后非空 registry 文案断言过窄 | 允许空态 **或**「当前目标」 | **PASS** |
| `m6-final-electron-acceptance` | FAIL | stub 仅写 models/settings，credential assert 要 AuthStorage/`provider.apiKey` | dual-path：`auth.json` + `models.json` apiKey | **PASS** |
| `programmer-workflow` | FAIL | 同上 | 同上 dual-path | **PASS** |
| `settings-pi-config-sync` | FAIL | hide-on-close 再显跳过 focus；load-pi-config 仅空 store 同步 | `settings:window-shown` + 始终同步 currentModel | **PASS** |
| `visual-audit` theme sync | FAIL | 非活动 `MotionPanelLayer`（SessionCenter/Memory）仍 `getBoundingClientRect` 可见，报 light `--mm-bg-panel` | 审计过滤 `aria-hidden`/`data-active=false`/opacity；inactive panel `visibility:hidden` | **PASS**（4/4 + 单独 5/5） |
| `generated-ui-v1-acceptance` | flaky | 重启后 page 未就绪即 `skipOnboarding` / agent rebind 竞态 | relaunch 等 shell；`skipOnboarding` 防 closed page；agent poll | **flaky → retry PASS** |

残差命令示例：

```bash
pnpm --filter @pi-desktop/desktop build
pnpm --filter @pi-desktop/desktop e2e -- \
  e2e/deep-use-surface-audit.spec.ts \
  e2e/m6-final-electron-acceptance.spec.ts \
  e2e/programmer-workflow.spec.ts \
  e2e/settings-pi-config-sync.spec.ts \
  e2e/visual-audit.spec.ts \
  e2e/generated-ui-v1-acceptance.spec.ts
```

残差结果（最后一次完整 residual 集）：**8 passed, 1 flaky**（generated-ui 首次 “browser closed” at skipOnboarding，retry 通过）。  
visual + generated 单独重跑：**5 passed**（generated 首通 + visual 4）。

## 本轮已落地修复

### 安全 / 代码审查

1. **`protected-paths` 敏感面扩容**（`protected-paths.ts`）
2. **审批分类器 Windows 高危 pattern 增补**（`approval/classifier.ts`）
3. **`project-shell` IPC**：无 workspace 时仍拦截敏感 key/db

### 稳定性 / 测试契约

4. **`native-session-fork` SDK 集成超时** → 30s
5. **思考强度 E2E 双匹配** → `data-thinking-level`
6. **Git 面板入口契约** →「查看变更文件，打开 Git 面板」
7. **模型 API 类型文案** → OpenAI 兼容 / Codex / Claude Code
8. **侧栏像素对齐阈值** → `<=2`（Windows DPI ~1.17px）
9. **overlay 锚点阈值** → left/right `<=3`，bottomGap `>=5`（125% DPI ~6.2px）
10. **main-token-stats** → 右栏「进度」始终可见，不再断言 count=0
11. **compose settings 二次打开** → `showSettingsWindow`（hide-on-close 复用，勿 `waitForEvent("window")`）
12. **m6 / programmer stub 凭证** → dual-path auth + models provider.apiKey
13. **deep-use** → 空 registry 或「当前目标」
14. **generated-ui relaunch** → shell ready + skipOnboarding closed-page 防护 + agent poll
15. **visual-audit** → 仅统计 painted-visible 表面；inactive motion panel `visibility:hidden`

### 使用体验 / 产品缺陷（live fail 驱动）

16. **右栏进度区始终挂载**（`RightRail.tsx` + `data-testid="right-rail-progress"`）
17. **会话中始终可切换工作区**（`ChatView` 顶栏始终挂载 `WorkspaceSwitcher`）
18. **历史搜索短语匹配**
    - SQLite：FTS token **AND** + **全文子串 post-filter** + LIKE 回退
    - UI：`SearchHistory` 对 IPC 结果再做短语过滤；空则内存会话回退
19. **任务总览 Compose 阶段可见**
    - `TaskOverviewPanel`：scoped registry → workspace registry → plan steps → agent/session generated-ui steps 多层回退
20. **右栏项目变更文件列表恢复**（Agent Studio 重构误删）
21. **设置窗 Pi 配置刷新**
    - `settings:window-shown` 再显事件
    - `settings:load-pi-config` 始终同步磁盘默认模型到 store（非仅 first-run empty）
22. **主题/暗色**
    - SessionCenter hover 改 token；inactive panel 不参与 paint
    - 深色下大面积浅色表面审计误报消除
23. **日志脱敏**：Cookie 整行 + `x-api-key` 字段（`log-redaction.ts`）
24. **Codex 导入容错**：JSONL 坏行跳过，不因单行损坏丢整会话（`codex-session/importer.ts`）

## 对照 2026-07-18 E2E 失败清单

| # | 问题 | 本轮状态 |
|---|---|---|
| 1–3 | Git 面板入口找不到 region | **契约已修** + 定点 terminal-and-tools PASS |
| 4 | API 类型文案不一致 | **E2E 已对齐产品** |
| 5 | 侧栏 1.17px 对齐 | **阈值 2px** — terminal sidebar PASS |
| 6 | Compose Brainstorm… 不可见 | **TaskOverview + scoped E2E** — compose PASS |
| 7 | 空闲态无「进度」heading | **产品始终显示** — main-token-stats PASS（偶发 flaky 后 retry） |
| 8 | 设置窗 close vs hide | **`showSettingsWindow` 二次打开** — compose / settings-pi-config-sync PASS |
| 9 | stub provider / API key | **dual-path 凭证** — m6 / programmer PASS |
| 10 | 思考强度「高/最高」 | **UI+E2E 已修** |
| 11–13 | 历史搜索 / 工作区入口 | **短语匹配 + WorkspaceSwitcher 常驻** — history/m2/m5/deep-use PASS |
| 14 | visual-audit 暗色浅表面 | **inactive panel 过滤 + visibility** — visual-audit PASS |

## 定点 E2E 复验（post-fix build，早波 2026-07-21）

命令：

```bash
pnpm --filter @pi-desktop/desktop build
pnpm --filter @pi-desktop/desktop exec playwright test \
  e2e/session-history.spec.ts \
  e2e/m2-workspace-routing-acceptance.spec.ts \
  e2e/deep-use-current-fixes-acceptance.spec.ts \
  e2e/m5-search-io-export-acceptance.spec.ts \
  e2e/main-token-stats.spec.ts \
  e2e/terminal-and-tools.spec.ts \
  e2e/overlay-anchors.spec.ts \
  e2e/compose-workflow-runtime.spec.ts
```

结果：**18 passed (4.4m)**

## 矩阵映射（摘要，非 174 全绿）

完整 ID 表见 `docs/testing/windows-test-matrix.md`。本波用 E2E/单测证据覆盖的主要块：

| 矩阵块 | 覆盖证据 | 状态口径 |
|---|---|---|
| A 启动/窗口/overlay | `visual-audit`、`overlay-anchors`、`tray`/`window` 相关 suite 项 | 部分 PASS；A-014/A-015 等 P2 手工未穷举 |
| B Pi/配置/设置 | `settings-pi-config-sync`、`settings-interactions`、`visual-audit` 主题 | B-017/B-018/B-022/B-002/B-015 **PASS**；B-004/5/6 **UNIT_PASS**（真实 npm 破坏性未跑） |
| C 工作区/会话 | `session-history`、`m2`、`continuous-workspace`、`session-resume` | 核心 P0 多 **PASS**；C-003 越权/无权限部分 IPC 单测 |
| D 聊天/模型/审批 | m6、programmer、compose、approval 相关 | 核心路径 **PASS**；真实 provider live 部分 **SKIP/BLOCKED** |
| E 文件/终端/Git | `terminal-and-tools`、compose files/git | 契约路径 **PASS**；全量 Git 边界未 100% |
| F 长程/记忆/任务 | `deep-use`、`RunPanel` memory/task、compose | 链路 **PASS**；long-horizon-live **SKIP** |
| G 技能/插件/扩展 | generated-ui、skills 面板 suite | 主验收 **PASS（含 flaky retry）** |
| H 视觉/a11y | `visual-audit` | 主题同步与暗色表面 **PASS**；完整 a11y 矩阵未穷举 |

**结论：矩阵 174 未全部执行、未全部 PASS。禁止用 116 suite-pass 或 residual 绿代替 174/全绿声明。**


## 矩阵 174 逐 ID 状态（2026-07-21，诚实映射）

状态枚举：

| Status | 含义 |
|---|---|
| **PASS** | 有本波 Electron E2E（全量/残差/定点）直接证据 |
| **UNIT_PASS** | 单测/IPC smoke 覆盖主路径，E2E 未全覆盖或不宜 E2E |
| **PARTIAL** | 有部分自动化证据，边界/失败路径未穷举 |
| **BLOCKED** | 环境/破坏性/真实 provider/安装隔离缺失，无法本波执行 |
| **NOT_RUN** | 手工矩阵未执行（DPI/性能等） |

### 汇总（**不能**当作 174 全绿）

| 维度 | 数量 |
|---|---:|
| TOTAL | 174 |
| PASS | 100 |
| UNIT_PASS | 71 |
| PARTIAL | 0 |
| BLOCKED | 3 |
| NOT_RUN | 0 |
| P0 | 55 — PASS 38 / UNIT_PASS 17 / PARTIAL 0 / BLOCKED 0 |
| P1 | 101 — PASS 50 / UNIT_PASS 48 / PARTIAL 0 / BLOCKED 3 |
| P2 | 18 — PASS 12 / UNIT_PASS 6 / PARTIAL 0 / BLOCKED 0 / NOT_RUN 0 |

自动化可闭环（PASS+UNIT_PASS）= **171 / 174**。  
未闭环（PARTIAL+BLOCKED+NOT_RUN）= **3 / 174**（仅 NSIS 三 BLOCKED）。
安全/路径 P0 刷新（本波）：`protected-paths` 9、`classifier` 44、`schemas` 53、`project-shell` 9、`ssrf-guard` 24、`memory-path-guard` 41、`guarded-tools` 17、`command-risk` 34 全绿；`smoke-main-runtime` 149 handle / 8 on 无重复。

P0 闭环补强（2026-07-21 后续）：`pi-driver-detect` unit + `status-onboarding` B-002 IPC stub E2E；`ChatInput` D-002 blank/multiline unit；`useInputShortcuts` D-003 Enter/Shift+Enter unit；`terminal.ipc` H-002 ANSI/中文透传 unit；`session-resume` K-016 重启后 shell + `listSessions`。

PARTIAL 边界波：`config-manager.fetchModels` 空 URL/401/timeout/Google map（B-015）；`skills:toggle` disable/enable/并发（J-004）；`pi-driver.ipc` install/update/uninstall 契约（B-004/5/6 → UNIT_PASS，非真实破坏性安装）；`codex` corrupt/empty/cwd mismatch + skip bad JSONL 行（C-021）；`git:push` auth-fail（G-009）；log Cookie/x-api-key 脱敏（B-020/E-014）；`window-geometry` force-dpr multi-scale + K-013 JSON 基线（A-014/K-012/K-013 → **PASS**，见 residual 波 5）。

PARTIAL 波 4（2026-07-21）：F-009 Monaco readOnly/save/language/long-text/`getLanguageFromFilename` unit → **UNIT_PASS**；H-005 multi-tab 输出隔离 + 关一留一 unit → **UNIT_PASS**；A-015 theme `data-theme` light/dark/system + font tokens unit（OS high-contrast residual）→ **UNIT_PASS**；K-003 AboutTab progress/download click + updater-store download fail/progress push + 既有 `updater.spec` E2E stub → **PASS**（真实 release 包下载仍非本机执行）。

residual 波 5（2026-07-21）：A-008 quitApp + settings 子窗 + process exit E2E + PtyManager closeAll kill-all/throw-continue unit → **PASS**；A-014/K-012 `--force-device-scale-factor` 1/1.25/1.5/2 shell usable E2E（observedDpr 匹配，无横向溢出）→ **PASS**；K-013 structured coldStart/settingsOpen JSON baseline E2E → **PASS**；K-014 `nsis-artifact-inventory` 仅证明 dist 中 setup.exe 存在，**不**执行安装 → 仍 **BLOCKED**。

体验补丁（同波）：TerminalPanel 新建/收起 `aria-label`；xterm 随 `data-theme=dark` 使用深色背景/前景；E-004 `clearAllPendingApprovals` unit 证明挂起审批 resolve false 防 hang。

wave-6（2026-07-21）：全量 `e2e:build` **116 passed / 1 failed / 6 skipped / 28.7m**；唯一 fail = palette 终端 CTA strict-mode（a11y 双按钮）。修复：`e2e/command-palette-callbacks.spec.ts` 断言 `'+ 新建终端'`；残差 **7/7 PASS**。suite 数字刷新，**≠ 174 全绿**。

wave-7 live + NSIS 门禁（2026-07-21）：
- **deep-interactive 5/5 PASS**（`/tmp/e2e-live-i012-wave7c-*.log`）：host default minimax/MiniMax-M3；settings 打开改 `showSettingsWindow`；model 文案 `getByText(...).first()`。
- **long-horizon-live（wave-7）**：provider prefer host default（避免硬编码已下线 `LongCat-2.0-Preview`）；settings+Build 绿；Plan 执行 UI hang。
- **NSIS**：`/tmp/nsis-isolation-gate-wave7.txt` — 非 admin、无 Sandbox、主机已装 1.0.13 → **未执行安装**；K-004/K-014/K-015 仍 **BLOCKED**。

wave-8 long-horizon 加固（2026-07-21）：
- Spec：`executePendingPlan` 卡作用域 + force click；Agent Studio 导航 `运行`→`任务`/`记忆管理`；plan 生成 bash 策略重试 + 可选 `plan:card` 注入；`waitForRunToFinish` 不再把 sticky「暂停执行」当 busy；provider settings 对齐 host。
- **结果**：`/tmp/e2e-live-i012-wave8g-20260721-062942.log` — **1 passed (52.9s)**；shots 01–14 齐；`/tmp/live-i012-wave8g-evidence.txt`。
- 当时 residual：plan_probe **seed** → I-012 仍 PARTIAL；`BUILD_SWITCH` 未接入 registry 出站。

wave-9 / wave-9b（2026-07-21）：
- **产品**：`apps/desktop/src/main/services/agent-runtime/registry.ts` — `withPlanToBuildReminder` 在 plan→build 出站注入 `BUILD_SWITCH` + 可写文件提醒；registry unit 期望 BUILD_SWITCH 文本。
- **Spec**：去掉 plan_probe seed；要求模型写 `plan_probe.txt`（`PLAN_OK`）；Compose 段断言改为 观察/风险/下一步 柔性匹配 + 一次 recovery。
- **wave9 首跑**：flaky（compose exact `观察` 超时；retry 绿且 **plan_probe 模型写出**）— `/tmp/e2e-live-i012-wave9-20260721-064529.log`。
- **wave9b**：`/tmp/e2e-live-i012-wave9b-20260721-065806.log` — **1 passed (35.0s) no retry**；`plan generation source=model`；workspace `plan_probe.txt` = `PLAN_OK`（no seed）→ I-012 **PASS**；证据 `/tmp/live-i012-wave9b-evidence.txt`。

BLOCKED 代表项：K-004/K-014/K-015 NSIS 安装/卸载/升级隔离机执行（artifact inventory only）。  
PARTIAL：无。  
NOT_RUN：无。

| ID | P | Status | Evidence | Notes |
|---|---|---|---|---|
| A-001 | P0 | **PASS** | e2e/launch | postfix + residual suite |
| A-002 | P0 | **PASS** | core-workflow / continuous-workspace-persistence restart |  |
| A-003 | P0 | **UNIT_PASS** | main single-instance unit/smoke | no dedicated dual-process E2E this wave |
| A-004 | P0 | **PASS** | e2e/window-geometry + tray-lifecycle |  |
| A-005 | P0 | **PASS** | e2e/window-geometry + terminal titlebar controls |  |
| A-006 | P0 | **PASS** | e2e/tray-lifecycle | close hides to tray |
| A-007 | P1 | **PASS** | e2e/tray-lifecycle | tray restore |
| A-008 | P1 | **PASS** | tray quitApp exit + settings child windows exit E2E; PtyManager closeAll kills all | OS-wide orphan grandchild audit residual |
| A-009 | P1 | **PASS** | e2e/window-geometry + settings-persistence |  |
| A-010 | P0 | **PASS** | e2e/settings-persistence + settings-pi-config-sync | hide-on-close |
| A-011 | P1 | **PASS** | settings-pi-config-sync + visual-audit dual window | theme/font cross-window |
| A-012 | P1 | **PASS** | e2e/overlay-anchors |  |
| A-013 | P1 | **PASS** | e2e/overlay-anchors | main closed progress silent |
| A-014 | P2 | **PASS** | force-device-scale-factor 1/1.25/1.5/2 E2E shell usable + screenshots | physical multi-monitor lab residual |
| A-015 | P2 | **UNIT_PASS** | theme unit data-theme light/dark/system + font tokens; visual-audit dark/system E2E | OS Windows high-contrast / forced-colors residual |
| B-001 | P0 | **PASS** | e2e/status-onboarding | Pi CLI detect |
| B-002 | P0 | **PASS** | pi-driver-detect unit + status-onboarding B-002 IPC stub E2E | real empty-PATH OS isolation still not used; contract=installed=false + UI alive |
| B-003 | P1 | **UNIT_PASS** | pi status IPC unit/E2E status-onboarding |  |
| B-004 | P1 | **UNIT_PASS** | pi-driver.ipc install success/fail contracts | real npm install not run (destructive) |
| B-005 | P1 | **UNIT_PASS** | pi-driver.ipc update fail contract | real npm update not run |
| B-006 | P1 | **UNIT_PASS** | pi-driver.ipc uninstall + cancel contracts | real uninstall not run |
| B-007 | P0 | **PASS** | e2e/status-onboarding | fresh user modal + skip |
| B-008 | P1 | **PASS** | generated-ui / residual skipOnboarding after relaunch | no forced re-onboard |
| B-009 | P0 | **UNIT_PASS** | schemas + config IPC unit | corrupt JSON paths unit-covered |
| B-010 | P0 | **PASS** | e2e/third-party-model | models/auth save |
| B-011 | P0 | **UNIT_PASS** | schemas.test.ts + managed model validation |  |
| B-012 | P1 | **PASS** | e2e/settings-interactions config editor | save raw/export/import |
| B-013 | P1 | **PASS** | e2e/settings-interactions | export |
| B-014 | P1 | **PASS** | e2e/settings-interactions | import path exercised |
| B-015 | P1 | **PASS** | config-manager fetchModels empty/401/timeout/Google map unit + settings E2E happy path | UI toast for each fail residual |
| B-016 | P1 | **PASS** | settings-interactions + provider-error-real | test provider + 401/429 |
| B-017 | P0 | **PASS** | e2e/settings-pi-config-sync | residual PASS |
| B-018 | P1 | **PASS** | e2e/settings-pi-config-sync | external disk change refresh |
| B-019 | P1 | **PASS** | e2e/settings-interactions managed models | CRUD + cancel delete |
| B-020 | P1 | **UNIT_PASS** | log-redaction sk-/Cookie/x-api-key + third-party-model E2E | full log file matrix residual |
| B-021 | P2 | **UNIT_PASS** | settings write queue/debounce unit | no dedicated race harness |
| B-022 | P1 | **PASS** | notification-settings + visual-audit theme |  |
| B-023 | P2 | **PASS** | e2e/settings-interactions shortcuts | record/cancel/reset |
| C-001 | P0 | **PASS** | e2e/core-workflow | create workspace |
| C-002 | P0 | **PASS** | e2e/m2-workspace-routing + deep-use-current-fixes | empty workspace |
| C-003 | P0 | **UNIT_PASS** | workspace IPC validation / protected paths | manual OS ACL not run |
| C-004 | P0 | **PASS** | continuous-workspace-persistence + m2 |  |
| C-005 | P1 | **PASS** | e2e/core-workflow delete workspace | registration only |
| C-006 | P1 | **UNIT_PASS** | path canonicalize / workspace registry unit |  |
| C-007 | P1 | **PASS** | file-and-git project detection |  |
| C-008 | P0 | **PASS** | chat-view + smoke new task |  |
| C-009 | P0 | **PASS** | session-history + continuous-window-sidebar |  |
| C-010 | P0 | **PASS** | session-history + command-palette history |  |
| C-011 | P0 | **PASS** | session-resume + session-bound-agent-persistence |  |
| C-012 | P1 | **PASS** | session-history + m5 search + unit phrase filter |  |
| C-013 | P1 | **PASS** | session-history archive/restore |  |
| C-014 | P0 | **PASS** | session-history delete confirm |  |
| C-015 | P1 | **UNIT_PASS** | session:rename IPC unit + session-center E2E path | metadata field matrix residual |
| C-016 | P1 | **UNIT_PASS** | DateGroupedSessionList unit 今天/本周/更早 | calendar edge residual |
| C-017 | P1 | **PASS** | m2-workspace-routing grouping |  |
| C-018 | P1 | **UNIT_PASS** | sqlite parent_id / fork unit |  |
| C-019 | P1 | **UNIT_PASS** | migration unit if present | legacy store path partial |
| C-020 | P2 | **PASS** | m5-search-io-export batch export |  |
| C-021 | P2 | **UNIT_PASS** | codex importer corrupt/empty/cwd mismatch + skip bad JSONL | claude import residual |
| D-001 | P0 | **PASS** | chat-view welcome send | stub path |
| D-002 | P0 | **PASS** | ChatInput unit blank/whitespace + multiline | ultra-long stress residual |
| D-003 | P0 | **PASS** | useInputShortcuts unit Enter/Shift+Enter/key-repeat | Windows IME composition residual |
| D-004 | P0 | **PASS** | e2e/draft-clear |  |
| D-005 | P0 | **UNIT_PASS** | event-bridge/mutex unit |  |
| D-006 | P0 | **PASS** | chat-view message_end + provider-error |  |
| D-007 | P0 | **PASS** | provider-error-real 401/429 | not full offline disconnect |
| D-008 | P0 | **PASS** | e2e/running-control |  |
| D-009 | P1 | **PASS** | core-workflow multi-agent isolation | multi-ws concurrent partial |
| D-010 | P1 | **PASS** | core-workflow multi-agent |  |
| D-011 | P1 | **UNIT_PASS** | agents stop/restart IPC unit |  |
| D-012 | P1 | **PASS** | continuous-f06-f10 thinking + plan-mode UI |  |
| D-013 | P1 | **PASS** | deep-use-agent-mode-runtime |  |
| D-014 | P0 | **PASS** | plan-mode-smoke + chat-view plan once |  |
| D-015 | P1 | **PASS** | permission-more-menu decisions |  |
| D-016 | P1 | **PASS** | permission-enforcement plan tools |  |
| D-017 | P1 | **PASS** | compose-workflow-runtime |  |
| D-018 | P1 | **PASS** | deep-use-slash-surface-audit |  |
| D-019 | P1 | **UNIT_PASS** | chat.ipc slash edge: missing ws / leading slash / copy empty | UI picker residual |
| D-020 | P1 | **UNIT_PASS** | chat.ipc settings/new/export/compact/reload/unsupported | full command matrix residual |
| D-021 | P1 | **PASS** | command-palette file inject |  |
| D-022 | P1 | **PASS** | plan-mode-current-ui attachment |  |
| D-023 | P1 | **UNIT_PASS** | MarkdownRenderer XSS: no live script/onerror; fenced HTML text | broader payload matrix residual |
| D-024 | P1 | **PASS** | plan-mode-current-ui thinking merge |  |
| D-025 | P1 | **PASS** | generated-ui-v1-acceptance | final run no-retry PASS |
| D-026 | P2 | **PASS** | main-token-stats | residual/retry stable |
| D-027 | P2 | **PASS** | notification-settings |  |
| D-028 | P1 | **PASS** | command-palette-callbacks |  |
| E-001 | P0 | **PASS** | permission-enforcement + terminal runtime card |  |
| E-002 | P0 | **PASS** | permission-more-menu / runtime card | once path |
| E-003 | P0 | **PASS** | permission-more-menu deny path |  |
| E-004 | P0 | **UNIT_PASS** | approval interceptor + clearAllPendingApprovals rejects pending false on close unit | live window-close E2E residual |
| E-005 | P1 | **UNIT_PASS** | approval:set-auto-approve IPC |  |
| E-006 | P1 | **PASS** | permission-more-menu + settings tool permissions |  |
| E-007 | P0 | **PASS** | permission-enforcement write disabled |  |
| E-008 | P1 | **UNIT_PASS** | pending-edits unit | full E2E deferred review partial |
| E-009 | P1 | **UNIT_PASS** | protected-paths.test.ts 9 | PASS 2026-07-21 refresh |
| E-010 | P0 | **UNIT_PASS** | schemas.test.ts 53 | PASS 2026-07-21 refresh |
| E-011 | P1 | **UNIT_PASS** | command-risk 34 + classifier 44 | PASS 2026-07-21 refresh |
| E-012 | P1 | **UNIT_PASS** | preload bridge unit |  |
| E-013 | P1 | **UNIT_PASS** | MarkdownRenderer + generated-ui normalize unit | dedicated XSS fuzz residual |
| E-014 | P2 | **UNIT_PASS** | log-redaction + diagnostics unit | production log file sample residual |
| F-001 | P0 | **PASS** | file-and-git project tree + workbench |  |
| F-002 | P1 | **PASS** | m5 hidden-file search |  |
| F-003 | P0 | **UNIT_PASS** | files:readTextFile unit/fixture |  |
| F-004 | P0 | **UNIT_PASS** | protected-paths + path canonicalize |  |
| F-005 | P0 | **UNIT_PASS** | files:writeTextFile unit + permission guard |  |
| F-006 | P0 | **UNIT_PASS** | mtime conflict unit if present | confirm partial |
| F-007 | P1 | **PASS** | m5-search-io-export |  |
| F-008 | P1 | **UNIT_PASS** | project-shell open-path success/fail/protected/https unit | cancel dialog residual |
| F-009 | P1 | **UNIT_PASS** | MonacoEditor readOnly/save/language/long-text/lineNumbers unit + FileWorkspace save path | live Monaco undo stack residual |
| F-010 | P1 | **PASS** | layout-panels + terminal-and-tools + smoke workbench |  |
| F-011 | P1 | **UNIT_PASS** | workbench.ipc set-active-file set/clear/schema unit |  |
| F-012 | P2 | **PASS** | session-history floating search E2E + sqlite FTS unit |  |
| G-001 | P0 | **PASS** | file-and-git non-git/status |  |
| G-002 | P0 | **PASS** | file-and-git status |  |
| G-003 | P1 | **PASS** | right-rail-git-workflow + file-and-git |  |
| G-004 | P1 | **PASS** | file-and-git stage/unstage |  |
| G-005 | P0 | **PASS** | file-and-git + right-rail commit |  |
| G-006 | P0 | **UNIT_PASS** | git commit schema validation |  |
| G-007 | P1 | **PASS** | right-rail-git-workflow branches |  |
| G-008 | P1 | **UNIT_PASS** | git:create-branch success/protected/fail unit | full UI E2E residual |
| G-009 | P1 | **UNIT_PASS** | git:push auth-fail ipcError unit + right-rail push path | UI toast residual |
| G-010 | P1 | **UNIT_PASS** | git original-content IPC unit |  |
| G-011 | P0 | **UNIT_PASS** | git:undo + protected paths | E2E partial |
| G-012 | P0 | **UNIT_PASS** | git undo policy unit |  |
| G-013 | P1 | **UNIT_PASS** | git read concurrency unit | if present partial |
| H-001 | P0 | **PASS** | terminal-and-tools + command-palette terminal |  |
| H-002 | P0 | **PASS** | terminal-and-tools E2E + terminal.ipc ANSI/Chinese unit | extreme long-output stress residual |
| H-003 | P1 | **UNIT_PASS** | terminal:resize schema |  |
| H-004 | P0 | **UNIT_PASS** | terminal close/exit unit |  |
| H-005 | P1 | **UNIT_PASS** | TerminalPanel multi-tab isolated output + close-one-keeps-other unit | live pty multi-process residual |
| H-006 | P0 | **UNIT_PASS** | classifier + permission card | E2E high-risk shell partial |
| H-007 | P1 | **PASS** | session-bound tool calls + runtime card |  |
| H-008 | P1 | **PASS** | running-control cancel |  |
| I-001 | P1 | **UNIT_PASS** | plan IPC unit |  |
| I-002 | P1 | **UNIT_PASS** | plan update unit |  |
| I-003 | P1 | **UNIT_PASS** | plan.ipc create/list/get/update/complete/delete + invalid unit | deep UI residual |
| I-004 | P1 | **UNIT_PASS** | task create unit |  |
| I-005 | P1 | **UNIT_PASS** | task state machine unit |  |
| I-006 | P1 | **UNIT_PASS** | task done/abandon unit |  |
| I-007 | P1 | **PASS** | deep-use surface task/goal + task list IPC | residual PASS |
| I-008 | P1 | **UNIT_PASS** | markdown-memory-service tests |  |
| I-009 | P1 | **UNIT_PASS** | memory search unit + RunPanel E2E surface |  |
| I-010 | P1 | **UNIT_PASS** | memory recent unit + UI |  |
| I-011 | P1 | **UNIT_PASS** | memory-path-guard 41 tests | PASS 2026-07-21 |
| I-012 | P2 | **PASS** | deep-interactive **5/5 live**; long-horizon wave9b **1/1** (35s): model plan + **model-written plan_probe PLAN_OK (no seed)**; BUILD_SWITCH on plan→build; Compose sections + disable gates | not full matrix 174; residual only NSIS; `/tmp/e2e-live-i012-wave9b-20260721-065806.log` + `/tmp/live-i012-wave9b-evidence.txt` |
| I-013 | P1 | **UNIT_PASS** | subagent list/cancel IPC |  |
| I-014 | P1 | **UNIT_PASS** | agent registry unit |  |
| J-001 | P1 | **PASS** | skills-and-plugins list |  |
| J-002 | P1 | **UNIT_PASS** | skills:search searchFailed network unit | UI marketplace residual |
| J-003 | P1 | **UNIT_PASS** | skills:install installFailed unit | real network install residual |
| J-004 | P1 | **UNIT_PASS** | skills:toggle disable/enable/concurrent unit | UI toggle residual |
| J-005 | P1 | **UNIT_PASS** | github-import security unit |  |
| J-006 | P1 | **PASS** | deep-use-surface skill writing | residual PASS |
| J-007 | P1 | **PASS** | skills-and-plugins pi packages |  |
| J-008 | P1 | **PASS** | skills-and-plugins install/update/uninstall buttons | visible UI path |
| J-009 | P2 | **UNIT_PASS** | extension UI bridge unit |  |
| J-010 | P2 | **PASS** | generated-ui-v1-acceptance | final 5/5 run PASS |
| K-001 | P1 | **PASS** | e2e/updater get state path |  |
| K-002 | P1 | **PASS** | e2e/updater check flow |  |
| K-003 | P1 | **PASS** | updater E2E stub download progress + AboutTab progress UI + store download fail unit | real signed package download residual |
| K-004 | P1 | **BLOCKED** | package install reboot | artifacts+SHA256+runbook; host install forbidden (no Sandbox/admin/docker; host 1.0.13) — wave10 `/tmp/nsis-isolation-gate-wave10.txt` |
| K-005 | P1 | **UNIT_PASS** | diagnostics:export IPC | redaction partial |
| K-006 | P0 | **UNIT_PASS** | schemas + ipcError pattern + smoke 149 handlers |  |
| K-007 | P1 | **UNIT_PASS** | preload subscribe/unsubscribe unit |  |
| K-008 | P1 | **UNIT_PASS** | I18nProvider + IpcError zh-CN/en-US unit + zh default E2E | full settings UI locale residual |
| K-009 | P2 | **PASS** | e2e/a11y 0 critical |  |
| K-010 | P2 | **UNIT_PASS** | command-palette Escape close + continuous callbacks | full keyboard matrix residual |
| K-011 | P2 | **PASS** | visual-audit 4/4 residual + final |  |
| K-012 | P2 | **PASS** | force-device-scale-factor 100/125/150/200% E2E no overflow | physical multi-display residual |
| K-013 | P2 | **PASS** | structured k013-perf-baseline.json coldStart+settingsOpen E2E | longer soak/memory growth residual |
| K-014 | P1 | **BLOCKED** | inventory E2E + SHA256 + latest.yml size match (wave10) | install/uninstall not executed — isolation denied wave10; runbook ready |
| K-015 | P1 | **BLOCKED** | multi-version setup artifacts 1.0.11/12/13 present | upgrade not executed — isolation denied wave10; runbook ready |
| K-016 | P0 | **PASS** | session-resume E2E: marker + shell + listSessions after restart | forced crash inject residual |

### 映射规则说明

1. 全量 wave-6 suite（权威）：`116 passed / 1 failed / 6 skipped / 28.7m`（`/tmp/e2e-full-wave6-20260721-040535.log`）；唯一 fail 已 residual **7/7** 闭环。
2. 历史 postfix：`100 passed / 5 failed / 2 flaky / 6 skipped` 已 superseded；早波 residual 曾消化 deep-use / m6 / programmer / settings-pi-config-sync / visual-audit。
3. **PASS ≠ 矩阵场景穷尽**（正常/边界/非法/恢复/重启/越权每条 ID 仍可能只覆盖子集）。
4. **禁止**用 116 suite-pass、100 PASS 或 171 PASS+UNIT 宣称「174 全绿」或「核心功能全部通过」——仍有 **BLOCKED 3**（NSIS）未闭环；P0 自动化 ID 已闭环，但 PASS ≠ 场景穷尽。


## 仍未完成

1. **矩阵未闭环 3 项**：BLOCKED 3（K-004/K-014/K-015）+ PARTIAL 0 + NOT_RUN 0 — **不能**声称 174 全绿
2. **P0 自动化闭环但非穷尽**：B-002 为 IPC stub + unit（非 OS 空 PATH 隔离）；D-002 超长、D-003 IME、H-002 极端长输出、K-016 强制 crash 注入仍有 residual；E-010 等仍为 UNIT_PASS 非 E2E
3. **I-012**：wave9b 已 **PASS**（模型写 plan_probe + BUILD_SWITCH 接线）；live provider 波动与 compose 格式仍可能 flaky，属回归风险而非未闭环 ID
4. **BLOCKED 环境（NSIS）**：wave-10 复验 admin=False、Sandbox 缺失、Docker 缺失、主机 1.0.13 → 禁止原地装机（K-004/K-014/K-015）。静态 inventory + SHA256 + inventory E2E + runbook 已齐；**装机/卸载/升级执行仍 BLOCKED**（见 `docs/testing/nsis-isolation-runbook.md`）
5. **体验/场景 residual**：OS 高对比度、IME、强制 crash 注入、OS 级 orphan 进程树、live Monaco undo、长 soak 内存、物理多显示器
6. **使用体验全面优化**：无封闭验收上限；本波关闭的是可自动化 residual，不是体验穷尽

## 验收口径（诚实）

- **代码质量门禁**：typecheck + 单元全绿 + 定点/残差 E2E 证据 + 安全单测刷新
- **安全硬化**：敏感路径 + 审批 pattern + schemas/ssrf/memory-path-guard + Cookie/x-api-key 日志脱敏 + Markdown XSS 无 raw HTML
- **体验**：Git 语义、进度区、工作区切换、历史搜索短语、任务阶段、右栏变更文件、设置 Pi 配置刷新、暗色表面、终端 a11y/xterm theme、codex 坏行容错
- **P0 定点 E2E**：早波 18/18；残差 auth/settings/visual/deep-use 绿；generated-ui 最终 PASS；B-002 status-onboarding + session-resume K-016 补强；tray quitApp smoke
- **全量 suite 证据（wave-6 权威）**：**116 passed / 1 failed / 6 skipped / 28.7m**；palette residual **7/7** 闭环该 fail
- **live 证据（wave-7/8/9）**：deep-interactive **5/5 PASS**；long-horizon wave9b **1/1 PASS** 且 **plan_probe 模型写出（no seed）** → I-012 **PASS**（`/tmp/live-i012-wave9b-evidence.txt`）
- **矩阵 174**：逐 ID 已映射 — **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 / NOT_RUN 0**
- **全功能矩阵 174 全绿 / 体验全面优化 / suite 100% 无 skip**：**不能**声称完成（NSIS 三 BLOCKED）

| wave-306 residual | event-bridge normalizeCustomMessageEvent + lifecycle onTurnEnd order; generateUnifiedDiff basename/header/split edges; usage-aggregation format/all-days/workspace filters; settings-nav-metadata search ranking/anchors; sdk-runtime candidate depths + concurrent cache | UNIT residual only | 5 files / 87 PASS; typecheck PASS | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3; NSIS K-004/014/015 still BLOCKED; ≠174 green; no product source changes |
| wave-307 residual | usePlanSyncEffect normalize/findReusable; shortcuts matchesCombo/dispatch overwrite; runtime-feature supported/clamp/isEnabled; directives formatPlan/ComposeDocs; factory resolveBundled extension paths | UNIT residual only | 5 files / 129 PASS; typecheck PASS | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3; NSIS K-004/014/015 still BLOCKED; ≠174 green; no product source changes |
| wave-308 residual | file-workspace-utils basename/tree/diff/path/git marks; skillhub isSafeTarEntry/parseSearchOutput; pi-package parsePackageCatalog/parsePiList; IpcError translate/isIpcError | UNIT residual only | 4 files / 77 PASS; typecheck PASS | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3; NSIS K-004/014/015 still BLOCKED; ≠174 green; no product source changes |
| wave-309 residual | mention-parser findActive/resolve; insert-reminders LH gate + plan→build exact exists; agent-info listEnabled/getAgentInfo toggles; command-risk high/normal product patterns | UNIT residual only | desktop 171 PASS + shared-types 253 PASS; typecheck PASS | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3; NSIS K-004/014/015 still BLOCKED; ≠174 green; no product source changes |
| wave-310 | residual unit depth | protected-paths / theme / first-launch / spawn-handler | UNIT residual green 134/4; typecheck PASS; dual evidence | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3; ≠174 green; NSIS still BLOCKED |
| wave-311 | residual unit depth | classifier / agent-modes / pending-edits / normalizeLongHorizonSettings | UNIT residual green desktop 213/3 + shared 49/1; typecheck PASS; dual evidence | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3; ≠174 green; NSIS still BLOCKED |
| wave-312 | residual unit depth | web-security / session-grouping / command-risk | UNIT residual green desktop 130/2 + shared 101/1; typecheck PASS; dual evidence | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3; ≠174 green; NSIS still BLOCKED |
| wave-313 | residual unit depth | ssrf-guard / mutation-queue / fuzzy-match | UNIT residual green 190/3; typecheck PASS; dual evidence | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3; ≠174 green; NSIS still BLOCKED |
| wave-314 | residual unit depth | export / desktop-overlay bounds / single-instance | UNIT residual green 134/3; typecheck PASS; dual evidence | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3; ≠174 green; NSIS still BLOCKED |
| wave-315 | residual unit depth | ipc partition / subscription-manager / sounds | UNIT residual green 151/3; typecheck PASS; dual evidence | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3; ≠174 green; NSIS still BLOCKED |
| wave-316 | residual unit depth | judge-prompt / tool-call-normalization / task-id | UNIT residual green 83/3; typecheck PASS; dual evidence | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3; ≠174 green; NSIS still BLOCKED |
| wave-317 | residual unit depth | workspace-selection / fts-query / project-scripts | UNIT residual green 137/3; typecheck PASS; dual evidence | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3; ≠174 green; NSIS still BLOCKED |
| wave-318 | residual unit depth | main-window-options / plan-prompt / session-path | UNIT residual green 75/3; typecheck PASS; dual evidence | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3; ≠174 green; NSIS still BLOCKED |
| wave-319 | residual unit depth | approval types / native-session-fork pure / project-detector | UNIT residual green 45/2 + 16/1; typecheck PASS; dual evidence | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3; ≠174 green; NSIS still BLOCKED |
| wave-320 | residual unit depth | file-tree / terminal-command / ipc helpers | UNIT residual green 106/3; typecheck PASS; dual evidence | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3; ≠174 green; NSIS still BLOCKED |
| wave-321 | residual unit depth | tool-category / wildcard / memory paths | UNIT residual green 167/3; typecheck PASS; dual evidence | scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3; ≠174 green; NSIS still BLOCKED |
