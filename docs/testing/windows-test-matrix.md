# Pi Desktop Windows Test Matrix

测试对象：Pi Desktop `1.0.13`（当前工作区版本）  
测试平台：Windows 10/11，Electron 41.2.0，Node.js >= 22.19.0，pnpm 9。外部依赖：Pi CLI、Git、GitHub 网络、可用的模型 Provider。

## 使用方式

- `P0`：启动、消息收发、会话/工作区、文件安全、审批、终端、设置持久化等阻断性路径。
- `P1`：完整的 Git、计划/任务/记忆、技能/插件、模型配置、更新器和窗口生命周期。
- `P2`：视觉、可访问性、动效、诊断导出、导入/导出和异常恢复的补充路径。
- 执行结果写入 `docs/testing/windows-test-report-2026-07-18.md`，状态使用 `PASS`、`FAIL`、`BLOCKED`、`NOT_RUN`。
- 每条功能至少覆盖正常、空值/边界、非法输入、失败恢复、重复操作和重启/并发中的适用项；高风险文件、Shell、Git 和 IPC 还要覆盖越权路径。

## A. 启动、窗口与系统集成

| ID | P | 用例 | 预期 | 执行 |
|---|---|---|---|---|
| A-001 | P0 | 首次启动主进程 | Electron 启动无未捕获异常，主窗口可见，标题为 Pi Desktop，渲染器非空 | E2E `launch` |
| A-002 | P0 | 已存在用户数据时重启 | 应用恢复可用，不因坏的临时状态阻塞启动 | E2E + 手工 |
| A-003 | P0 | 第二次启动同一实例 | 单实例锁生效，现有窗口被激活，不创建重复主窗口 | 单测 + 手工 |
| A-004 | P0 | 主窗口最小化/恢复 | 窗口状态正确，恢复后内容与会话不丢失 | E2E `window-geometry` |
| A-005 | P0 | 主窗口最大化/还原 | 仅切换一次状态，尺寸/布局不溢出，`is-maximized` 返回一致 | E2E + IPC |
| A-006 | P0 | 主窗口关闭按钮 | 按产品约定隐藏到托盘或退出；无残留子进程和无响应窗口 | E2E `tray-lifecycle` |
| A-007 | P1 | 托盘恢复 | 关闭主窗口后托盘存在，点击恢复主窗口并聚焦 | E2E `tray-lifecycle` |
| A-008 | P1 | 托盘退出 | 退出菜单真正结束 Electron、pty、子窗口和监听器 | 手工/进程检查 |
| A-009 | P1 | 设置窗口独立打开 | 800x600 目标尺寸、独立 BrowserWindow、非模态，可多次打开而不重复创建 | E2E `window-geometry` |
| A-010 | P0 | 设置窗口关闭/再开 | 设置窗口销毁或隐藏符合约定，重新打开仍加载正确 tab | E2E `settings-persistence` |
| A-011 | P1 | 主窗口与设置窗口同时交互 | 两窗口 IPC 不串线，焦点与快捷键作用于正确窗口 | E2E + 手工 |
| A-012 | P1 | Desktop overlay 隐藏/显示 | 主窗口上下文切换后 overlay 锚点、可见性和进度提示正确 | E2E `overlay-anchors` |
| A-013 | P1 | 主窗口关闭时 overlay | 主窗口关闭后不错误展示进度提醒，重新打开后恢复正常 | E2E `overlay-anchors` |
| A-014 | P2 | 窗口拖动区域和 DPI 缩放 | 100%、125%、150% 缩放下标题栏可拖动、按钮可点击、无裁剪 | 手工矩阵 |
| A-015 | P2 | 高对比度/系统深色模式 | 颜色、文字和焦点仍可辨识，不依赖系统主题造成不可读 | 手工 |

## B. 首次启动、Pi 状态与配置

| ID | P | 用例 | 预期 | 执行 |
|---|---|---|---|---|
| B-001 | P0 | 检测 PATH 中已安装 Pi CLI | 状态显示已安装、版本可读，不显示错误 onboarding | E2E `status-onboarding` |
| B-002 | P0 | Pi CLI 缺失 | 显示可行动的安装/配置提示，主 UI 不崩溃 | E2E `status-onboarding` IPC stub + unit `pi-driver-detect` |
| B-003 | P1 | 刷新 Pi 状态 | 状态变化事件只更新一次，旧状态不覆盖新状态 | 单测/IPC |
| B-004 | P1 | 安装 Pi CLI 成功/失败 | 进度、成功结果和失败错误均可见，取消后状态回收 | IPC + 手工 |
| B-005 | P1 | Pi CLI 更新成功/失败 | 版本刷新，失败有可读错误，不破坏当前配置 | IPC + 手工 |
| B-006 | P1 | Pi CLI 卸载 | 卸载成功后状态变为未安装；取消/权限失败可恢复 | IPC + 手工 |
| B-007 | P0 | 首次用户 onboarding | 新用户出现 onboarding；完成、跳过、关闭后的后续启动行为稳定 | E2E `status-onboarding` |
| B-008 | P1 | onboarding 重复打开 | 已完成 onboarding 的用户不会被强制重复引导 | E2E + 重启 |
| B-009 | P0 | 读取 models/auth/settings 配置 | 文件不存在、为空、JSON 损坏、权限不足均返回结构化错误，不抛到 renderer | 单测/IPC |
| B-010 | P0 | 保存配置合法值 | 写入正确文件，保留未修改字段，重新加载结果一致 | E2E `third-party-model` |
| B-011 | P0 | 保存配置非法值 | Zod/业务校验拒绝空 provider、坏 URL、非法模型类型，磁盘不被破坏 | 单测/IPC |
| B-012 | P1 | 原始配置保存 | JSON 文本保存前校验，换行/Unicode/敏感字段处理正确 | IPC + 文件断言 |
| B-013 | P1 | 配置导出 | 导出文件生成，内容完整，路径取消时无异常副作用 | E2E `settings-interactions` |
| B-014 | P1 | 配置导入合法/坏 JSON | 合法导入生效；坏 JSON 有错误且原配置保留 | E2E + IPC |
| B-015 | P1 | fetch models 网络成功/超时/非 2xx | 成功列表正确，超时/网络失败不冻结窗口且给出可重试反馈 | E2E/网络模拟 |
| B-016 | P1 | provider test 成功/鉴权失败/超时 | 结果明确区分成功、鉴权、网络和格式错误 | E2E `settings-interactions` |
| B-017 | P0 | 默认模型设置 | 设置默认模型后聊天选择器、Pi 配置和重启后状态一致 | E2E `settings-pi-config-sync` |
| B-018 | P1 | 外部修改 Pi 配置后重开设置 | 设置摘要和主窗口模型列表刷新，不显示旧缓存 | E2E `settings-pi-config-sync` |
| B-019 | P1 | 受管模型新增/编辑/删除 | 表单校验、保存、取消、删除确认和删除后列表更新正确 | E2E `settings-interactions` |
| B-020 | P1 | Anthropic/API key 等 provider 字段 | 输入显示、保存、读取和错误反馈符合 provider 规则，敏感值不出现在日志 | E2E + 日志审计 |
| B-021 | P2 | 配置文件并发写入 | 两次保存串行化，最终 JSON 不被截断/交叉覆盖 | 集成/并发 |
| B-022 | P1 | 主题、语言、通知、侧栏/右栏设置 | 立即生效，重新打开和重启仍保持，所有主面板同步 | E2E `notification-settings`/`visual-audit` |
| B-023 | P2 | 快捷键录制/取消/单项重置/全部重置 | 冲突、空快捷键、Escape 取消均可处理，默认值可恢复 | E2E `settings-interactions` |

## C. 工作区与会话中心

| ID | P | 用例 | 预期 | 执行 |
|---|---|---|---|---|
| C-001 | P0 | 创建工作区 | 目录选择后创建/注册 workspace，切换到该工作区且路径正确 | E2E `core-workflow` |
| C-002 | P0 | 空目录工作区 | 空目录可进入，显示空态，不要求已有 Git/项目文件 | E2E `m2-workspace-routing` |
| C-003 | P0 | 非目录路径/无权限目录 | 拒绝并提示，当前工作区保持不变 | IPC + 手工 |
| C-004 | P0 | 工作区切换 | 当前 workspace、会话、终端、Git 和模型上下文全部切换 | E2E `continuous-workspace-persistence` |
| C-005 | P1 | 删除工作区 | 删除注册关系不误删用户目录；当前工作区有安全的回退 | E2E + 文件断言 |
| C-006 | P1 | 工作区重复添加/大小写路径 | 去重和路径规范化一致，Windows 大小写/尾斜杠不产生重复项 | 单测/IPC |
| C-007 | P1 | 工作区检测项目类型 | Git、Node、Python、Go 等探测结果准确；未知目录仍可打开 | 单测 `project-detector` |
| C-008 | P0 | 新建会话 | 新会话获得唯一 ID，绑定当前 workspace/agent，输入区清空 | E2E `chat-view` |
| C-009 | P0 | 会话列表/摘要 | 列表按预期分组、排序，标题/时间/消息数量正确 | E2E `session-history` |
| C-010 | P0 | 会话打开/历史跳转 | 从列表、搜索、命令面板打开目标会话，消息和滚动定位正确 | E2E `session-history` |
| C-011 | P0 | 会话持久化重启 | 消息、usage、工具调用、卡片和 agent 绑定重启后仍可恢复 | E2E `session-resume`/`session-bound-agent-persistence` |
| C-012 | P1 | 会话搜索空/大小写/中文/特殊字符 | 搜索结果准确；无结果空态；不因正则字符崩溃 | E2E + 单测 |
| C-013 | P1 | 会话归档/恢复 | 归档项从主列表移入 archived，恢复回原分组 | E2E `session-history` |
| C-014 | P0 | 删除会话及取消确认 | 取消不删除；确认只删除目标会话，当前会话有安全回退 | E2E `session-history` |
| C-015 | P1 | 会话元数据编辑 | 标题、置顶、标签等合法修改持久化；空标题使用规则化回退 | IPC/UI |
| C-016 | P1 | 日期分组 | 今天/昨天/本周/本月/更早边界按本地时区正确归组，跨午夜稳定 | 单测 + 手工 |
| C-017 | P1 | 工作区分组 | workspace 分组可折叠，跨工作区会话不串组 | E2E `m2-workspace-routing` |
| C-018 | P1 | 会话树结构/分叉 | parent_id、分支/恢复消息加载正确，删除父节点不破坏可读历史 | SQLite 集成 |
| C-019 | P1 | 旧 electron-store 数据迁移 | 旧格式可读取/迁移，重复迁移幂等，损坏记录隔离 | 单测/迁移 |
| C-020 | P2 | 批量导出 | 选中、多选、全选、无选、目标路径取消、超大文本和 Unicode 均正确 | E2E `m5-search-io-export` |
| C-021 | P2 | Claude/Codex 会话导入 | 扫描、导入、重复、格式坏、路径越权、取消选择均有明确结果 | IPC/fixture |

## D. 聊天、Pi Agent、模式与流式运行

| ID | P | 用例 | 预期 | 执行 |
|---|---|---|---|---|
| D-001 | P0 | 发送普通文本 | 用户消息立即出现，Pi 流式事件逐步渲染，完成后状态收敛 | E2E `chat-view` |
| D-002 | P0 | 空白/超长/多行消息 | 空白不提交；多行按预期；超长有界面或后端保护，不冻结 | 单测 ChatInput blank/multiline |
| D-003 | P0 | Enter/Shift+Enter | Enter 提交，Shift+Enter 换行，Windows IME/组合输入不误提交 | 单测 useInputShortcuts；IME residual |
| D-004 | P0 | 发送中清空草稿 | 提交立即清空输入框，后台请求继续且不丢消息 | E2E `draft-clear` |
| D-005 | P0 | 流式 text_delta + turn_end 并发 | 消息顺序、最终文本和 loading 状态稳定，无重复/截断 | 单测/集成 |
| D-006 | P0 | provider message_end 错误 | 错误在聊天中可见且可操作，不被泛化为空响应 | E2E `chat-view` |
| D-007 | P0 | 网络断开/Provider 超时 | 当前消息状态可恢复，重试或新消息可用，窗口不死锁 | E2E `provider-error` |
| D-008 | P0 | 停止生成 | abort 只影响目标 workspace/session，状态、输入框、历史正确 | E2E `running-control` |
| D-009 | P1 | 多工作区并发会话 | 消息和事件按 workspace 路由，互不串流 | E2E `core-workflow` |
| D-010 | P1 | 多 agent 创建/切换/隔离 | agent 列表、消息、权限、thinking 设置互不污染 | E2E `core-workflow` |
| D-011 | P1 | agent restart/stop/abort | 每种终止语义正确，重复调用幂等，运行态回收 | IPC/集成 |
| D-012 | P1 | thinking level 切换 | 可选值生效，事件/显示一致，非法值拒绝 | IPC/单测 |
| D-013 | P1 | 模式切换 chat/plan/compose/build | 模式状态、工具集、输入提示和请求上下文同步，不泄漏内部包装文本 | E2E `deep-use-agent-mode-runtime` |
| D-014 | P0 | Plan 模式单次提交 | 仅发送一次 `/plan`，计划卡出现后选择区可用 | E2E `plan-mode-smoke` |
| D-015 | P1 | Plan 批准/拒绝/修改/取消 | 每个 decision 只消费一次，错误 decision 不改变计划 | IPC/E2E |
| D-016 | P1 | Plan 工具限制 | plan 模式移除 mutation 工具，保留 read/plan_write，always 权限不能覆盖 | E2E `permission-enforcement` |
| D-017 | P1 | Compose workflow | workflow 状态、子 agent、artifact 路径、回退模式和失败重试正确 | E2E `compose-workflow-runtime` |
| D-018 | P1 | Slash 命令列表 | 内置、技能、扩展命令去重、排序、搜索正确；不支持命令明确隐藏/报错 | E2E `deep-use-slash-surface-audit` |
| D-019 | P1 | Slash 命令参数 | 参数缺失、空格、引号、Unicode 和超长输入正确传递 | 单测/E2E |
| D-020 | P1 | 内置命令 reload/new/session 等 | 每个命令只触发目标动作，失败返回结构化结果 | IPC/E2E |
| D-021 | P1 | Mention/file reference | 路径解析、空格、中文、点文件、越工作区路径均正确处理并在发送前展示 | E2E `command-palette-callbacks` |
| D-022 | P1 | Attachment | 添加、删除、重复、不可读文件和计划模式上下文正确 | E2E `plan-mode-current-ui` |
| D-023 | P1 | Markdown/GFM/code block | 表格、代码、链接、长行、HTML/raw 策略渲染正确且不执行脚本 | 单测/E2E/a11y |
| D-024 | P1 | thinking block 合并 | 相邻 thinking block 合并，计数准确，流式更新不跳动 | E2E `plan-mode-current-ui` |
| D-025 | P1 | 生成 UI 卡片 | 历史卡片、运行时卡片、未知 action、白名单 action、重放和共存均安全 | E2E `generated-ui-v1-acceptance` |
| D-026 | P2 | Token/usage 统计 | 当前会话、header、settings 统计一致；空值/大数/跨重启正确 | E2E `main-token-stats` |
| D-027 | P2 | 通知 | 完成/错误/后台运行通知遵循开关，关闭通知不发送；重启保持 | E2E `notification-settings` |
| D-028 | P1 | 全局快捷键/命令面板 | Ctrl+K 打开，Escape 关闭，cmd/file/history 模式切换和 callback 正确 | E2E `command-palette-callbacks` |

## E. 审批、权限、安全与人工确认

| ID | P | 用例 | 预期 | 执行 |
|---|---|---|---|---|
| E-001 | P0 | high-risk tool call | 工具执行前出现审批请求，磁盘无提前修改 | E2E `permission-enforcement` |
| E-002 | P0 | approve once | 仅当前请求放行，执行结果/文件 review 可见 | E2E |
| E-003 | P0 | deny | 工具不执行，agent 收到拒绝结果，会话仍可继续 | E2E |
| E-004 | P0 | approval 超时/窗口关闭 | 请求回收为拒绝或可恢复状态，不挂起 agent | 单测/手工 |
| E-005 | P1 | auto-approve 开关 | 开关事件同步主进程；关闭后立即恢复审批 | IPC/E2E |
| E-006 | P1 | permission mode 切换 | read/edit/high-risk/always 等模式只影响允许范围，持久化规则正确 | E2E `permission-more-menu` |
| E-007 | P0 | write disabled guard | 写操作在触盘前被拦截，文件内容与 mtime 不变 | E2E `permission-enforcement` |
| E-008 | P1 | deferred edit review | 延迟编辑可查看、逐项/全部应用、取消和重复应用幂等 | 单测/E2E |
| E-009 | P1 | protected paths | `.git`、配置、数据库、工作区外路径和系统路径不能被越权编辑/删除 | 单测/IPC |
| E-010 | P0 | IPC 参数注入 | 所有高风险 IPC 对类型、空串、超长、相对路径、UNC 路径、`..` 做校验 | schema 单测 |
| E-011 | P1 | Shell command risk classifier | normal/read/edit/high-risk 分类覆盖引号、管道、&&、大小写和危险 Git 变体 | shared 单测 |
| E-012 | P1 | renderer API 暴露面 | preload 仅暴露 typed API，不暴露 Node/Electron 原始对象，事件取消订阅有效 | preload 单测 |
| E-013 | P1 | CSP/XSS | markdown、文件名、错误文本、provider 返回内容中的脚本不执行 | E2E/a11y/security |
| E-014 | P2 | 日志脱敏 | API key、token、密码、路径敏感片段不落日志和诊断导出 | 单测/日志审计 |

## F. 文件、搜索、编辑器与 Workbench

| ID | P | 用例 | 预期 | 执行 |
|---|---|---|---|---|
| F-001 | P0 | 文件树扫描 | 目录、文件、空目录、深层目录、排序和展开正确 | 单测/E2E |
| F-002 | P1 | 文件树过滤 | `.git`、node_modules 等按规则隐藏；点文件在需要的搜索路径可见 | E2E `m5-search-io-export` |
| F-003 | P0 | 读取文本文件 | UTF-8、BOM、中文、CRLF、空文件和大文件正确；二进制不误当文本 | IPC/fixture |
| F-004 | P0 | 工作区外读取 | 拒绝绝对越权、`..`、符号链接绕过和 UNC 路径 | IPC/security |
| F-005 | P0 | 写入文本文件 | 新建、覆盖、目录不存在、权限失败、Unicode 和换行正确 | IPC/E2E |
| F-006 | P0 | expectedMtime 冲突 | 文件被外部修改时拒绝覆盖并提示冲突，原文件保留 | IPC/集成 |
| F-007 | P1 | 文件搜索 | 大小写、中文、空查询、特殊字符、点文件、limit 边界和取消正确 | E2E `m5-search-io-export` |
| F-008 | P1 | 文件选择/打开/reveal | 取消选择无副作用，目标路径受保护，系统打开失败可恢复 | IPC/UI |
| F-009 | P1 | Monaco 编辑器 | 打开、修改、保存、撤销/重做、只读、语法高亮和长文本不崩溃 | 单测/E2E |
| F-010 | P1 | Workbench 二级 tabs | files/git/terminal 切换保持上下文，面板高度和折叠状态正确 | E2E `layout-panels` |
| F-011 | P1 | active file | 选择文件后右栏/编辑器/聊天引用同步，切换 workspace 不串文件 | IPC/E2E |
| F-012 | P2 | 搜索历史 | 保存、删除、清空、重复查询去重和重启保持正确 | E2E/单测 |

## G. Git 工作流

| ID | P | 用例 | 预期 | 执行 |
|---|---|---|---|---|
| G-001 | P0 | 非 Git 目录 status | 返回明确的非 Git 状态，Git panel 不崩溃 | E2E `file-and-git` |
| G-002 | P0 | clean/modified/untracked status | 分支、改动、未跟踪、删除和冲突状态准确 | E2E `file-and-git` |
| G-003 | P1 | diff unstaged/staged | diff 文本、文件列表、空 diff 和二进制提示正确 | E2E `right-rail-git-workflow` |
| G-004 | P1 | stage/unstage 单个/多个/空列表 | 状态正确更新，空列表为 no-op，不误改其他文件 | IPC/E2E |
| G-005 | P0 | commit 合法 message | commit 成功，日志和状态刷新 | E2E `file-and-git` |
| G-006 | P0 | commit 空/空白/超长/特殊 message | 拒绝空值，特殊字符不被 shell 注入，错误可见 | schema/IPC |
| G-007 | P1 | branch 列表/切换 | 当前分支、远端分支、无分支、非法分支名和工作区 dirty 状态正确 | E2E `right-rail-git-workflow` |
| G-008 | P1 | 创建分支 | 合法名成功，重复/非法/空名拒绝，不污染当前工作区 | IPC/E2E |
| G-009 | P1 | push 成功/无远端/鉴权失败/冲突 | 结果可理解，不吞错误，不进入假成功状态 | E2E + mock |
| G-010 | P1 | original content | tracked、untracked、删除文件和路径越权结果正确 | IPC |
| G-011 | P0 | git undo tracked | 恢复 HEAD 内容，受保护路径和非目标文件不变 | E2E/IPC |
| G-012 | P0 | git undo untracked/modified | 按安全策略拒绝或仅删除允许的 untracked，提示准确 | 单测/IPC |
| G-013 | P1 | 并发 Git 读取 | status/diff/log 重复读取去重，结果不会互相覆盖 | 单测/集成 |

## H. Terminal 与工具调用

| ID | P | 用例 | 预期 | 执行 |
|---|---|---|---|---|
| H-001 | P0 | 创建终端 | 返回唯一 terminal id，cwd/workspace 绑定正确，输出可订阅 | E2E `terminal-and-tools` |
| H-002 | P0 | 输入命令/输出 | 输入、回显、ANSI、中文、换行和长输出正确 | E2E terminal-and-tools + unit terminal.ipc ANSI/中文 |
| H-003 | P1 | resize | cols/rows 边界校验，终端布局同步，不丢输出 | IPC/E2E |
| H-004 | P0 | 终端退出 | exit code 正确，订阅回调只触发一次，关闭后输入被拒绝 | 单测/E2E |
| H-005 | P1 | 多终端 tabs | 每个终端隔离，切换不串输出，关闭单个不影响其他 | E2E |
| H-006 | P0 | 危险 Shell 命令审批 | 命令执行前触发高风险确认，拒绝时不改文件 | E2E/分类器 |
| H-007 | P1 | 工具调用卡片 | read/edit/execute 状态、参数、结果、错误和重试显示正确 | E2E |
| H-008 | P1 | 工具长运行/取消 | 取消停止底层执行并更新 UI，重复取消幂等 | 集成 |

## I. 计划、任务、记忆与长周期运行

| ID | P | 用例 | 预期 | 执行 |
|---|---|---|---|---|
| I-001 | P1 | plan create/list/get | 合法数据可创建、查询、列表排序，缺失 workspace 被拒绝 | 单测/IPC |
| I-002 | P1 | plan update | 仅允许合法状态/字段修改，版本或并发冲突可识别 | 单测/集成 |
| I-003 | P1 | plan complete/delete | 完成与删除状态持久化，重复操作幂等，当前 plan 回收 | IPC/E2E |
| I-004 | P1 | task create/rename | 标题边界、重复 ID、非法输入处理正确 | 单测/IPC |
| I-005 | P1 | task start/block/unblock | 状态迁移符合状态机，非法迁移拒绝，时间戳/进度一致 | 单测/集成 |
| I-006 | P1 | task done/abandon | 终态不可误恢复，UI 和持久化一致 | IPC/E2E |
| I-007 | P1 | task list/get active | workspace/agent 过滤正确，空结果安全 | IPC/E2E |
| I-008 | P1 | memory write/index | Markdown 记忆写入、索引更新、重复记录和坏文件处理正确 | 单测 |
| I-009 | P1 | memory search | 空 query、中文、大小写、limit、无结果和损坏索引正确 | 单测/E2E |
| I-010 | P1 | memory recent | 时间排序、limit 边界、跨 workspace 隔离正确 | 单测/E2E |
| I-011 | P1 | memory path guard/reconcile | 工作区外路径、删除/重命名文件、索引重建安全且幂等 | 单测 |
| I-012 | P2 | long-horizon checkpoint/judge | checkpoint 恢复、judge 成功/失败/超时、重启续跑和重复评估正确 | 单测/E2E |
| I-013 | P1 | subagent list/cancel | 类型/实例列表隔离，取消未知/已终止实例返回安全结果 | IPC/E2E |
| I-014 | P1 | agent runtime registry | create/prompt/stop/restart/suppress 多次调用不泄漏实例 | 单测 |

## J. 技能、插件、Pi packages 与生成式 UI

| ID | P | 用例 | 预期 | 执行 |
|---|---|---|---|---|
| J-001 | P1 | 本地技能列表 | 无目录、空目录、坏 frontmatter、workspace 覆盖和重复 slug 均可处理 | IPC/E2E |
| J-002 | P1 | skills marketplace search | 空 query、中文、网络失败和分页/空结果正确 | E2E `skills-and-plugins` |
| J-003 | P1 | skill install/uninstall | 成功、已安装、版本冲突、网络失败和回滚正确 | E2E + 文件断言 |
| J-004 | P1 | skill toggle | enabled 状态持久化，聊天命令列表实时更新 | E2E |
| J-005 | P1 | GitHub import | 合法 URL、非 GitHub、私有/不存在仓库、路径注入和网络失败安全 | IPC/security |
| J-006 | P1 | write skill | 名称/内容校验，路径固定在技能目录，覆盖确认和 Unicode 正确 | IPC/E2E |
| J-007 | P1 | Pi package catalog | 搜索、刷新、空/坏 catalog 和缓存回退正确 | E2E `skills-and-plugins` |
| J-008 | P1 | package install/update/remove | 按钮状态、进度、失败回滚、重复点击和重启后列表正确 | E2E |
| J-009 | P2 | extension UI bridge | prompt/confirm/select/notify 等请求路由到正确窗口，取消和坏请求安全 | 单测/E2E |
| J-010 | P2 | generated UI whitelist | 只允许白名单组件/action，未知组件不执行脚本且保留可读 fallback | E2E `generated-ui-v1-acceptance` |

## K. 更新、诊断、国际化、无障碍和非功能

| ID | P | 用例 | 预期 | 执行 |
|---|---|---|---|---|
| K-001 | P1 | updater get state | 未启用、无网络、已是最新、发现更新的状态明确 | 单测/E2E |
| K-002 | P1 | updater check | 不阻塞主窗口，事件状态单调推进，重复 check 幂等 | E2E `updater` |
| K-003 | P1 | updater download | 进度/失败/取消/磁盘不足可见，下载不破坏当前版本 | E2E + mock |
| K-004 | P1 | updater install | 仅有可安装更新时允许，重启后安装流程完整，异常可回退 | 手工/打包环境 |
| K-005 | P1 | diagnostics export | 生成可读报告，包含必要运行信息但脱敏 token/key，路径取消安全 | IPC/E2E |
| K-006 | P0 | typed IPC error contract | 所有 handler 的失败均为结构化 `IpcError` 或明确结果，不出现未捕获 rejection | 单测/日志 |
| K-007 | P1 | 事件订阅/取消订阅 | renderer 销毁后无回调泄漏；同一事件不重复订阅 | 单测 |
| K-008 | P1 | zh-CN/en-US 切换 | 主要 UI、错误、空态、设置和动态文本均有翻译，不出现 key 泄漏 | E2E/截图 |
| K-009 | P2 | axe critical/serious | 主聊天页和命令面板无 critical/serious violation，键盘焦点可见 | E2E `a11y` |
| K-010 | P2 | 键盘导航 | Tab 顺序合理，Enter/Space 操作控件，Escape 关闭 modal/popover | 手工/E2E |
| K-011 | P2 | 视觉布局 | 主导航、左右栏、设置 tabs、终端、Git、空态和 loading 无重叠/裁剪 | E2E `visual-audit` |
| K-012 | P2 | 100/125/150/200% DPI | 文字、按钮、面板和窗口尺寸稳定，无横向溢出 | 手工截图矩阵 |
| K-013 | P2 | 性能基线 | 冷启动、首屏、长会话、长输出、大文件搜索和多终端不出现明显卡死/内存持续增长 | 手工/性能记录 |
| K-014 | P1 | 打包安装 | NSIS 安装、卸载、快捷方式、用户数据保留策略和首次启动正确 | `package` + 手工 |
| K-015 | P1 | 升级安装 | 旧版本升级后配置/会话保留，版本号、卸载和回滚策略正确 | 打包矩阵 |
| K-016 | P0 | 崩溃/异常恢复 | renderer/main/pty 异常有日志，重启后用户数据不损坏，会话可恢复 | E2E session-resume 重启后 shell+listSessions；强制 crash residual |

## 覆盖要求

1. P0 全部执行通过后才允许给出“核心功能通过”。
2. 每个 IPC handler 至少有一个成功用例、一个非法输入/错误用例；文件、Git、终端、审批还必须有工作区外/危险路径用例。
3. 真实 Provider、GitHub、Pi CLI、Windows 托盘、NSIS 安装和自动更新属于环境相关测试，无法在无凭据/无发布包时标记为 PASS，只能标记 `BLOCKED` 并记录原因。
4. 对失败项记录：ID、实际结果、复现步骤、日志/截图、严重等级、是否回归验证。

## 2026-07-21 执行映射（逐 ID 见 qa-progress，非 174 全绿）

证据源：`docs/testing/qa-progress-2026-07-21.md`（含 **174 行逐 ID 表**）。

- **wave-6 全量 suite（权威）**：`/tmp/e2e-full-wave6-20260721-040535.log` — **116 passed / 1 failed / 6 skipped / 28.7m**（`e2e:build`）。
- **wave-6 残差**：`command-palette-callbacks` **7/7 PASS**（修闭 full-suite 唯一 fail：终端 CTA strict-mode）。
- **wave-7 live**：deep-interactive **5/5 PASS**（minimax）；long-horizon 初版 settings+Build 绿、Plan hang。
- **wave-8 long-horizon**：`RUN_LONG_HORIZON_ACCEPTANCE=1` → **1 passed / 52.9s**（`/tmp/e2e-live-i012-wave8g-20260721-062942.log`）；当时 plan_probe **seed** → 曾 PARTIAL。
- **wave-9b long-horizon**：`RUN_LONG_HORIZON_ACCEPTANCE=1` → **1 passed / 35.0s**（`/tmp/e2e-live-i012-wave9b-20260721-065806.log`）；**plan source=model**；**plan_probe 模型写出 PLAN_OK（no seed）** + BUILD_SWITCH plan→build → I-012 **PASS**（`/tmp/live-i012-wave9b-evidence.txt`）。
- **wave-76 residual**：PlanCard actions/options + DesktopOverlay progress stop focus-visible；unit cluster **24**；NSIS still **BLOCKED**（`/tmp/wave76-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-75 residual**：CommandCard/PermissionRequestStack/MessageBubble/GeneratedUiCard/CommandPalette focus-visible；unit cluster **63**；NSIS still **BLOCKED**（`/tmp/wave75-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-74 residual**：GitPanel change/branch/commit + FileWorkspace tree/toolbar + TaskProgressPanel rows focus-visible；unit cluster **77**；NSIS still **BLOCKED**（`/tmp/wave74-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-73 residual**：GitRailControls row/popover + SessionCenter row/delete + MiniMaxCodeLayout floating toggle focus-visible；unit cluster **33**；NSIS still **BLOCKED**（`/tmp/wave73-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-72 residual**：RightRail browse/file/diff/expand + ModelSelector trigger/options focus-visible；unit cluster **21**；NSIS still **BLOCKED**（`/tmp/wave72-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-71 residual**：SessionRow select/actions/menu/confirm + UsageTab RangeButton chips focus-visible；unit cluster **6**；NSIS still **BLOCKED**（`/tmp/wave71-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-70 residual**：AboutTab updater/diagnostics + ManagedModelsPanel row/dialog focus-visible；unit cluster **13**；NSIS still **BLOCKED**（`/tmp/wave70-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-69 residual**：SkillsPanel tabs/dialogs + PiPackagesMarketplace catalog/install + AppearanceTab theme cards focus-visible；unit cluster **12**；NSIS still **BLOCKED**（`/tmp/wave69-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-68 residual**：TerminalPanel toolbar + MySkills uninstall dialog + InstalledAddons actions focus-visible；unit cluster **16**；NSIS still **BLOCKED**（`/tmp/wave68-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-67 residual**：SettingsNav a11y assert；mention-parser edges；SettingsContent close/done + SwitchControl + ToolPermissions preset focus-visible；unit cluster **39**；NSIS still **BLOCKED**（`/tmp/wave67-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-66 residual**：PersistenceBanner + RecentWorkspaces focus-visible a11y；unit cluster **8**；NSIS still **BLOCKED**（`/tmp/wave66-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-65 residual**：SearchHistory pure (`sliceMatchWindow`/`compareSearchResults`) + close/result a11y；NSIS re-probe still **BLOCKED**；unit **7**（`/tmp/wave65-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-64 residual**：protected-paths credential edges；PiStatusPanel install/cancel/error + dialog a11y；terminal-command/crash-diagnostics expand；unit cluster **19**；NSIS still **BLOCKED**（`/tmp/wave64-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-63 residual**：WorkspaceSwitcher menu a11y + `basename` pure；ProgressReminderToast stop focus-visible；ssrf/web-security edges；unit cluster **44**；NSIS still **BLOCKED**（`/tmp/wave63-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-62 residual**：skillhub path-guard fix + pure expand；WorkspaceNoticeBanner a11y；format/fuzzy/main-window/tray/workspace-selection expand；unit cluster **59**；NSIS still **BLOCKED**（`/tmp/wave62-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-61 residual**：native-session-fork pure helpers **7**；NSIS re-probe still **BLOCKED**（`/tmp/wave61-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-60 residual**：useTaskProgress pure/hook **7** + PLAN_DIRECTIVE **2**（**9**）；NSIS still **BLOCKED**（`/tmp/wave60-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-59 residual**：ClaudeSessionImporter **5** + tool-call normalize expand + findActiveSlashCommand；cluster **17**；NSIS still **BLOCKED**（`/tmp/wave59-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-58 residual**：VirtualizedMessageList **3** + GeneratedUiChart pure/UI **4**（`buildChartOption`）；NSIS still **BLOCKED**（`/tmp/wave58-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-57 residual**：NSIS re-probe still **BLOCKED**；UX PiConfigEditor/ShortcutsSettings a11y；unit **6**（`/tmp/wave57-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-56 residual**：DiffViewer `splitHunkLines` pure/UI **6**；FoldRow keyboard a11y；NSIS still **BLOCKED**（`/tmp/wave56-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-55 residual**：ProjectGroupedSessionList UX + unit **3**；session-grouping depth/activity/group expand；cluster w/ wave-54 **11**；NSIS still **BLOCKED**（`/tmp/wave55-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-54 residual**：DateGroupedSessionList pure/UI **4**（injectable `now` + local-calendar）；group focus-visible；NSIS still **BLOCKED**（`/tmp/wave54-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-53 residual**：GeneratedUiForm pure/UI **4** + MiniMaxCodeTitleBar **2**；NSIS still **BLOCKED**（`/tmp/wave53-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-52 residual**：GeneratedUiTable pure/UI **4** + SessionExport recheck **3**；NSIS still **BLOCKED**（`/tmp/wave52-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-51 residual**：NSIS re-probe still **BLOCKED**；UX SessionExportDialog a11y；unit ModelSelector/MotionPanelLayer/CustomMessageCard/SessionExport **10**（`/tmp/wave51-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-50 residual**：UX ShortcutsCheatsheet close a11y；unit ShortcutsCheatsheet/SessionRow **6**；NSIS still **BLOCKED**（`/tmp/wave50-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-49 residual**：unit SettingsNav **3** + Popover **3**（**6**）；NSIS still **BLOCKED**（`/tmp/wave49-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-48 residual**：unit session-path **3** + AnimatedCollapse **3**（**6**）；NSIS still **BLOCKED**（`/tmp/wave48-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-47 residual**：UX Button/ErrorBoundary/ThinkingBlock a11y；unit **9**；NSIS still **BLOCKED**（`/tmp/wave47-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-46 residual**：unit theme **7** + directives-pure **6** + judge-prompt **4**（**17**）；NSIS still **BLOCKED**（`/tmp/wave46-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-45 residual**：unit ApprovalPanel **3** + LongHorizonTab **3**；UX ApprovalPanel bulk focus-visible；NSIS still **BLOCKED**（`/tmp/wave45-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-44 residual**：export TaskOverviewPanel pure mappers；UX ChangeApprovalCard a11y；unit pure/ChangeApprovalCard **8**；NSIS still **BLOCKED**（`/tmp/wave44-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-43 residual**：unit MemoryPanel **3**；NSIS still **BLOCKED**（`/tmp/wave43-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-42 residual**：UX Settings ModelSelector button a11y；unit ModelSelector/Appearance/Permissions **5**；NSIS still **BLOCKED**（`/tmp/wave42-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-41 residual**：UX SkillCreateDropdown menu a11y；unit **2**；NSIS re-probe still **BLOCKED**（`/tmp/wave41-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-40 residual**：unit TopTabBar **2** + UsageStatsPanel **1**；NSIS still **BLOCKED**（`/tmp/wave40-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-39 residual**：UX FileChangeItem a11y + RecentWorkspaces formatTimeAgo；unit FileChangeItem/RecentWorkspaces **6**（cluster 13 w/ wave-38）；NSIS still **BLOCKED**（`/tmp/wave39-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-38 residual**：UX ResizablePanel a11y + SkillCard focus；unit ResizablePanel/RunPanel/SkillCard **7**；NSIS still **BLOCKED**（`/tmp/wave38-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-37 residual**：shared-types ipc-guards **4**（isSettingsWindowTab + ipcError）；suite **47**；NSIS still **BLOCKED**（`/tmp/wave37-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-36 residual**：unit list-local-skills **4** + desktop-overlay **1** + pi-api **2** + claude/codex sessions **2**（**9**）；nsis honesty **4/4**；security/memory refresh；NSIS re-probe still **BLOCKED**（`/tmp/wave36-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-89 residual**：tool-category + wildcard + fts-query residual；unit **32 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave89-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-90 residual**：NSIS re-probe still **BLOCKED**；runtime-policy + guarded-tools + memory-path-guard + protected-paths + Markdown XSS residual；unit **148 PASS**；typecheck PASS；NSIS honesty E2E **4/4**（`/tmp/wave90-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-91 residual**：shared-types command-risk/app-settings/ipc-guards + directives-pure residual；unit shared **61 PASS** + directives **9 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave91-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-92 residual**：NSIS re-probe still **BLOCKED**；pending-edits + approval-bridge + PtyManager residual；unit **43 PASS**；typecheck PASS（`/tmp/wave92-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-93 residual**：session-path + log-redaction + ipc helpers residual；unit **42 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave93-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-94 residual**：NSIS re-probe still **BLOCKED**；web-security + Task/Goal/Subagent schemas residual；unit **85 PASS**；typecheck PASS（`/tmp/wave94-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-95 residual**：permission/approval/toast store + event-bridge residual；unit **54 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave95-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-96 residual**：attachments/agent-mode store + ssrf-guard residual；unit **55 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave96-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-97 residual**：agent/workspace/updater stores + window-lifecycle + tray-icon + local-file-protocol unit **82 PASS**；typecheck PASS；NSIS re-probe still **BLOCKED** （`/tmp/wave97-evidence.txt`）— **≠ 装机 PASS**；scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**。
- **wave-98 residual**：crash-diagnostics/workspace-selection/main-window-options/diagnostics/single-instance/overlay bounds unit **42 PASS**；typecheck PASS；NSIS still **BLOCKED**（）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-99 residual**：desktop-overlay/updater/workbench/diagnostics/agents/window IPC unit **35 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave99-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-100 residual**：settings-window/packages/claude-codex/git-undo/pi-driver IPC unit **38 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave100-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-101 residual**：sqlite-session-repository + mimocode-runtime-port + workspace.ipc + config.ipc unit **39 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave101-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-102 residual**：settings/skills/project-shell/plan/terminal IPC unit **66 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave102-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-103 residual**：files.ipc + list-local-skills + updater service + skills-store unit **44 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave103-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-104 residual**：permission defaults edges + agent-mode clamp/normalize + attachments cap/dup + pi-status throw/idle + desktop-overlay/updater/diagnostics IPC unit **59 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave104-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-105 residual**：export JSON/escape/toolCalls + notifications API edges + terminal-command risk variants + plan-prompt + checkpoint empty + pi-api unit **21 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave105-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-106 residual**：mutation-queue multi-key + session-path fallback/truncate + GeneratedUi pure edges + date-group boundaries unit **28 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave106-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-107 residual**：extension-ui-bridge resolve/always/plan-parse + DiffViewer fold threshold/multi-run + useDebouncedSave unmount/path unit **16 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave107-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-108 residual**：globals focus-visible/reduced-motion + tab-defs near-miss + partition incomplete shapes + fuzzy underscore/empty + IPC channel inventory unit **25 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave108-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-109 residual**：sounds corrupt/AudioContext/complete tones + first-launch unknown/`1`/`0` + subscription cleanup re-ensure + project-scripts colon names + sessionMatches empty/meta/thinking + sort favorites + run-control missing stop/combined errors + tool-call status aliases unit **64 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave109-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-110 residual**：queue-store turn/tool/cap/ignore + runtime-feature missing API/throw + generated-ui null/clone/plain + format relative/duration edges + logger stringify unit **71 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave110-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-111 residual**：file-workspace-utils path/size/git overwrite + plan-utils strip/preferred/cancelled + web-security origin/localhost + protected-paths config/credentials/isPathInside unit **70 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave111-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-112 residual**：theme matchMedia/font CSS + wildcard multi-star + tool-category network regex + path-canonical missing-leaf/create unit **46 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave112-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-113 residual**：NSIS re-probe still **BLOCKED**（admin=False / Sandbox missing / Docker missing；未主机装机）；mention-parser email/cursor/win-path + evaluate expand/empty/later-allow + classifier 2>&1/case/yarn + approval-store timeout/diff basename + pending-edits cache/toolCallId + fuzzy full-text/indexOf unit **172 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave113-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-114 residual**：runtime-policy shell-off/clone/freeze + export string-ts/escape asymmetry + notifications localStorage/ctor + main fuzzy residual + terminal-command pipe/wipe + command-risk destroy/secrets unit **130 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave114-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-115 residual**：log-redaction cookie/stack + ssrf fe70/http:/// + diff multi-file/hunk defaults + project-detector lockfile/bun/gradle + app-settings mode/merge + events residual unit **95 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave115-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-116 residual**：file-tree sort/clamp/noisy + web-security external/file-case + schemas git log tuple/agents/codex + ipc-guards params residual unit **120 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave116-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-117 residual**：NSIS re-probe still **BLOCKED**；guarded-tools non-string path/null params + bash empty/shell-off + id_rsa deny + policy controller identity + protected-paths gcloud-pair/secrets variants/trailing-sep + helpers short-circuit/dual-prefix + schemas agentsId/claude/plan/workbench/gitBranches/updateMessage residual unit **150 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave117-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-118 residual**：format 59min/23h/29d/duration edges + session-grouping case/trim/tags + tool-call whitespace/result→output + theme clamp 12..20 residual unit **83 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave118-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-119 residual**：run-control non-Error throws/plan pause-not-paused/stream-end + first-launch key overwrite/case + queue-store update/cap6/clear + subscription reverse cleanup residual unit **53 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave119-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-120 residual**：defaults empty-whitelist/case/prefix-sibling + path-canonical sensitive-missing/escape + runtime-feature featureState>longHorizon + toast unique-id/durations residual unit **58 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave120-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-121 residual**：permission-store dedupe/order/payload + attachments cross-ws isolation + workspace-selection object-ref/mixed timestamps + wildcard double-space/anchors + tool-category trim/core residual unit **68 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave121-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-122 residual**：skills-store non-array narrow/uninstall toast + sounds clamp/disabled/AudioContext + generated-ui drop-unknown/merge-content residual unit **40 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave122-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-123 residual**：NSIS re-probe still **BLOCKED**；agent-mode clamp-persist/corrupt-json + workspace create-fallback/remove-noncurrent/git replace + plan-utils failed/locked/recent + diff-parser metadata/pre-header + project-detector Cargo mid-file **fix** (line-scan) + Pipfile/pnpm-prefer + single-instance/main-window/crash dispose residual unit **111 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave123-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-124 residual**：mutation-queue async reject/keyed error + logger null/undefined/console + list-local-skills blank/empty-cache + GeneratedUiTable compare/format + export toolCalls/empty-title + directives empty-path/compose + plan-prompt 7-line + tab-defs padded residual unit **61 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave124-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-125 residual**：NSIS re-probe still **BLOCKED**；pi-status update/uninstall/missing-API + updater download/listener + GeneratedUiForm/Chart residual + project-scripts empty/cargo + pi-api undefined + ssrf metadata/fe80 residual unit **98 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave125-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-126 residual**：NSIS re-probe still **BLOCKED**；notifications/log-redaction/web-security/fuzzy-match/file-workspace-utils/classifier/pending-edits/approval-store/terminal-command/ipc/path-canonical/format residual unit **307 PASS**（13 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave126-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-127 residual**：theme/subscription/session-grouping/sounds/toast/first-launch/workspace-selection/mention/run-control/local-file-protocol/tool-call/permission residual unit **199 PASS**（12 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave127-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-128 residual**：NSIS re-probe still **BLOCKED**；attachments max-20 + single-instance secondary/primary + mutation-queue serial/keyed + tray-icon candidates + queue-store agent_end-after-error/extension_error + generated-ui non-object/content-alone + project-scripts multi-segment residual unit **81 PASS**（7 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave128-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-129 residual**：NSIS re-probe still **BLOCKED**；plan-store think-strip/step-cap/phases/goal-cleared + agent optimistic-drop/local-sort + session rename/setDefault + settings rightRail/fontSize-raw + skills blank-search/install-rethrow/toggle-toast + export HTML-escape/roles/tags residual unit **130 PASS**（6 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave129-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-130 residual**：NSIS re-probe still **BLOCKED**；updater install-throw/cleanup/hydrate-missing + workspace explicit-id/remove-first/clearError + pi-status update/cancel/listeners + runtime-feature clamp-build/clearError/IpcError + permission defaults dir/* + pi-api delete residual unit **109 PASS**（6 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave130-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-131 residual**：NSIS re-probe still **BLOCKED**；permission evaluate last-match/~/expand/disabled-* + wildcard empty/space-star/case + terminal terraform/npm-publish + ipc null/nested + diff-parser empty/prefer-diff + ssrf loopback-allow/data-blob residual unit **118 PASS**（6 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave131-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-132 residual**：NSIS re-probe still **BLOCKED**；web-security http-open/blob-block/same-origin-nav + log-redaction Error/Buffer/non-exact-keys + path-canonical .aws/.gnupg/.npmrc + local-file-protocol cert/cred-dir 403/404 + notifications enabled-string/canNotify/body-omit + classifier iex/sc/reg/System32/pnpm-filter residual unit **194 PASS**（6 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave132-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-133 residual**：NSIS re-probe still **BLOCKED**；diagnostics sessionStats/basename/nolog + agent-modes longHorizon-off/registry/workflow-flags/plan-empty-bash + crash-diagnostics renderer-dispose/nullish + overlay negative-origin/NaN-width + agent-mode-store fallback/runtime-clamp residual unit **63 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave133-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-134 residual**：NSIS re-probe still **BLOCKED**；main-window-options prefs-fresh/darwin-keys + mimocode maxMode-off/dream-distill/plan-tools + window-lifecycle quit-close/restore-not-min/no-overlay + updater download-error/progress-100/getState-clone + sqlite blank-search/limit/workspace/thinking residual unit **54 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave134-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-135 residual**：NSIS re-probe still **BLOCKED**；first-launch 1/0/throw-fallback + theme normalize/editor-diff/applyFont/system-light + sounds volume-clamp/disable-skip + subscription LIFO/nullish/non-fn-throw + toast duration-override/default-error + permission enqueue-idempotent/respond-throw residual unit **113 PASS**（6 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave135-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-136 residual**：NSIS re-probe still **BLOCKED**；IME D-003 useInputShortcuts isComposing/native/keyCode-229 + settings multi-display workArea clamp/negative-origin + crash inject child/renderer matrix + shortcuts override/editable + fts emoji/path/email residual unit **91 PASS**（5 files）；typecheck PASS；product IME Enter-submit guard locked；NSIS still **BLOCKED**（`/tmp/wave136-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-137 residual**：NSIS re-probe still **BLOCKED**；memory/paths separator-empty/parse-null + insert-reminders longHorizon/build-switch-default/charset + usage-aggregation streak/share/messageCount + session-path allowed/hash residual unit **109 PASS**（4 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave137-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-138 residual**：NSIS re-probe still **BLOCKED**；task-id session-scope/null-parent + agent-info omit-compose/unknown-mode + tool-category multiedit/network/core + protected-paths kube/docker/azure/env + plan-prompt structure residual unit **107 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave138-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-139 residual**：NSIS re-probe still **BLOCKED**；window.ipc native-unmax/null/multi-display-bounds + TaskOverview mapUi/file_list + mutation-queue long-soak-50/interleaved-reject + export roles/html/json-meta + tray prefer-first residual unit **61 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave139-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-140 residual**：NSIS re-probe still **BLOCKED**；formatPromptFailure 403/rate/cause/empty + git createBranch/checkout-missing/original/changedFiles-z + preload invoke/send allowlist + memory/describeImages + command-risk reg/npm-g/clean/pipe + fuzzy CJK/segment residual；desktop unit **108 PASS**（4 files）+ shared command-risk **49 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave140-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-141 residual**：NSIS re-probe still **BLOCKED**；pi-package-adapter catalog-entities/search/install-prefix + pending-edits autoApprove/list-cache/evict + workspace-selection long-list/tie + single-instance listener/restore-error residual unit **67 PASS**（4 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave141-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-142 residual**：NSIS re-probe still **BLOCKED**；generated-ui v1-fallback/clone/plainText + tool-call batch/args-alias/fallbackStatus + run-control plan-toast/ipcError-fallback/combined + ipc-guards legacy/tabs residual；desktop unit **60 PASS**（3 files）+ shared ipc-guards **15 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave142-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-143 residual**：NSIS re-probe still **BLOCKED**；mention-parser whitespace/CJK/multi-@ + project-scripts nullish/unicode + settings-bridge score-floor/enabled/reconcile + app-settings dream/distill/workflow residual；desktop unit **45 PASS**（3 files）+ shared app-settings **13 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave143-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-144 residual**：NSIS re-probe still **BLOCKED**；session-grouping tags/lazy/cycle/group-order + queue-store turn/auto_retry/blank-tool/queue_update-cap + toast duration-0/tone-defaults/cap-mix + export toolCalls/html-escape/json-meta + mutation-queue empty/reject-chain residual；desktop unit **102 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave144-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-145 residual**：NSIS re-probe still **BLOCKED**；terminal-command del/rmdir/reg + logger stackless/non-fn-bridge + ipc helpers context/parse-throw + first-launch idempotent/empty + events lifecycle/queue residual；desktop unit **61 PASS**（4 files）+ shared events **11 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave145-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-146 residual**：NSIS re-probe still **BLOCKED**；ipc partition branded/params/empty-strings + tool-call-normalization empty/non-array + pi-api reassign/null + command-risk del/rmdir/kubectl/terraform/whitespace residual；desktop unit **27 PASS**（3 files）+ shared command-risk **52 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave146-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-147 residual**：NSIS re-probe still **BLOCKED**；tray-icon 6-candidate order/asar.unpacked + sounds invalid-json/preserve/audio-throw + notifications helper titles/disabled/constructor residual；desktop unit **54 PASS**（3 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave147-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-148 residual**：NSIS re-probe still **BLOCKED**（artifacts 1.0.11–13 present, host install forbidden）；attachments mime/size/per-ws-cap + permission respondValue-throw/setMode-no-piAPI/boolean + ssrf IMDS-IPv6/RFC1918-allow/scheme residual；desktop unit **96 PASS**（3 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave148-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-149 residual**：NSIS re-probe still **BLOCKED**（admin=False / Sandbox missing / Docker missing；host install forbidden）；agent-info hardPermission-empty/list-dual-disable/plan-no-ws/no-toolAllowlist + theme localStorage-throw/min-CSS-ladder/matchMedia-missing + evaluate bare-~/restore-edit/synthetic-ask/merge-empty + agent-mode-store normalize/null-ws/featureState-vs-longHorizon + format toDate/duration/relative residual；desktop unit **158 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave149-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-150 residual**：NSIS re-probe still **BLOCKED**；tool-category normalize/exact-fileRead/mode-required + wildcard mixed-sep/brace-escape/?-slash + subscription void/re-ensure/empty-array + agent-modes goal-clone/normalize/plan-gate/subagents residual；desktop unit **85 PASS**（4 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave150-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-151 residual**：NSIS re-probe still **BLOCKED**；defaults multi-whitelist/env.example-order/external-ask + workspace-selection empty/zero-active/tie-first + project-scripts no-escape/case-manager + events auto_retry_ok/empty-queue residual；desktop unit **45 PASS**（3 files）+ shared events **13 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave151-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-152 residual**：NSIS re-probe still **BLOCKED**；main-window-options independent-chrome/titleBarStyle + fuzzy-match underscore/empty/subsequence (main+renderer) + command-risk sudo/mkfs/git-reset/terraform-auto-approve/trim residual；desktop unit **46 PASS**（3 files）+ shared command-risk **55 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave152-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-153 residual**：NSIS re-probe still **BLOCKED**（admin=False / Sandbox missing / Docker missing；host install forbidden）；run-control null-agentId/blank-Error/missing-agentsAbort/activeExecution-clear/abort-recover + overlay small-workArea/fractional-origin/omit-size/primary-fallback/destroy/refresh + tool-call contentIndex/status-alias/Date-revive/output-prefer + terminal-command sudo/mkfs/terraform-auto-approve + app-settings workflow-over-compose/empty-defaults residual；desktop unit **84 PASS**（4 files）+ shared app-settings **17 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave153-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-154 residual**：NSIS re-probe still **BLOCKED**；log-redaction cookie/auth/secret-keys/nested-sk + ssrf empty/ftp/ws/169.254-port/fe80-band + mutation-queue sync-throw/keyed-no-write/concurrent-20 + tool-call-normalization modern-only/empty-object/siblings + first-launch 1-0/TRUE-fallback/write-canonical residual；desktop unit **132 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave154-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-155 residual**：NSIS re-probe still **BLOCKED**；web-security empty/malformed/cross-origin/blocked-shell + evaluate fromConfig-order/multi-ruleset/disabled-star/EDIT_TOOLS + wildcard metachar/multi-*/space-star + sounds bounds/defaults/persist + generated-ui v3→v1/content-append/markdown-v2 residual；desktop unit **127 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave155-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-156 residual**：NSIS re-probe still **BLOCKED**；export 你-AI-roles/usage-gate/json-omit-optional + tray-icon first-match/cwd-fallback/checkedPaths + notifications missing-API/denied/toggle + defaults empty-whitelist/nested-env/sibling-prefix residual；desktop unit **79 PASS**（4 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave156-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-157 residual**：NSIS re-probe still **BLOCKED**；mention-parser cursor0/path-separators/empty-query/pure-resolve + runtime-policy git-segments/git.exe-cmd-wrapper/plan-Plan-reason/sessionPermissions-override + crash-diagnostics dispose-idempotent/multi-child-gone/renderer-dispose residual；desktop unit **105 PASS**（3 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave157-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-158 residual**：NSIS re-probe still **BLOCKED**；format day-month-boundaries/duration-sec-boundaries + session-grouping depth-cap/cycle-depth2/orphan-drop/parent-before-child + fts-query quote-separator/empty-null/FTS5-ops + fuzzy-match prefix/CJK-segment/backslash residual；desktop unit **122 PASS**（4 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave158-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-159 residual**：NSIS re-probe still **BLOCKED**；command-risk dd-if/git-clean-f/npm-uninstall-g/irm-iwr-pipes + tool-category network-regex/fileWrite-shell/empty-extension + main fuzzy-match renderer-parity + logger circular/throw-console/JSON + terminal-command high-risk-wrapper residual；desktop unit **68 PASS**（4 files）+ shared command-risk **58 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave159-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-160 residual**：NSIS re-probe still **BLOCKED**；ssrf metadata-case/RFC1918-allow/IMDS-IPv6/malformed + web-security external-http-only/file-mixed-deny/dev-localhost-http-only/same-origin + ipc partition primitives/partial-err + events lifecycle/auto_retry_start/compaction reasons residual；desktop unit **108 PASS**（3 files）+ shared events **16 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave160-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-161 residual**：NSIS re-probe still **BLOCKED**；app-settings defaultMode-normalize/nested-flags/composeWorkflow-as-workflow + theme resolveTheme/font-ladder/editor-floor + wildcard backslash-win32/empty/space-star residual；desktop unit **37 PASS**（2 files）+ shared app-settings **20 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave161-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-162 residual**：NSIS re-probe still **BLOCKED**；log-redaction Bearer-sk-api_key/Error-circular-MaxDepth + mutation-queue async-reject-advance/multi-key/nullish-results + sounds volume-clamp/NaN-null/disable-play residual；desktop unit **80 PASS**（3 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave162-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-163 residual**：NSIS re-probe still **BLOCKED**；notifications enabled-true-only/no-API-denied/helpers-noop + defaults doom_loop-ask/question-deny/read-non-env/win-whitelist + tray-icon 6-candidates/parent/grandparent residual；desktop unit **65 PASS**（3 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave163-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-164 residual**：NSIS re-probe still **BLOCKED**；path-canonical workspace-root/sibling-prefix/ENOTDIR-climb + evaluate disabled-empty/ask-allow/perm-wildcard/both-match/merge() + export HTML-escape/toolCalls/usage-0/role-labels + task-id skip-malformed/non-numeric-suffix/multi-digit/invalid-parent residual；desktop unit **112 PASS**（4 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave164-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-165 residual**：NSIS re-probe still **BLOCKED**；session-path trailing-dot/dots-only/lowercase/unicode/hash-16 + tool-call alias-prefer/blank-index/status-map/array-filter + directives-pure PLAN_FILE_INFO/compose-docs/BUILD_SWITCH + protected-paths id_ecdsa/outside-first/escape/db.bak residual；desktop unit **99 PASS**（4 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave165-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-166 residual**：NSIS re-probe still **BLOCKED**；classifier shell-empty/extra-high-risk/create-patch/unknown + insert-reminders plan→build-exists/planMode-off/compose-whitespace/build-only + plan-prompt write-only-under-plans + fts-query underscore/hyphen/operator-null residual；desktop unit **168 PASS**（4 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave166-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-167 residual**：NSIS re-probe still **BLOCKED**；pending-edits reject-clear/review-missing/unique-ids + judge-prompt empty-special/insufficient-evidence/Zod-strip + agent-info longHorizon-off/mode-gates/hardPermission-over-session + pi-api falsy-as-is residual；desktop unit **95 PASS**（4 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave167-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-168 residual**：NSIS re-probe still **BLOCKED**；generated-ui null-oversized/upsert-envelope/content-fallback/clone/plain-kv-action + paths resolveProjectId-trailing/walk-hidden-skip + guarded-tools policy-restore/builtin-names residual；desktop unit **117 PASS**（3 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave168-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-169 residual**：NSIS re-probe still **BLOCKED**；memory-path-guard non-memory/root-CC/isInside/sibling-paths + run-control missing-piAPI/dual-fail/string-object-errors + reconcile empty/skip-fingerprint/upsert-skip residual；desktop unit **101 PASS**（3 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave169-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-170 residual**：NSIS re-probe still **BLOCKED**；file-scanner maxDepth/dist-coverage/Thumbs/maxResults/missing-root + importer nested/roles/current-outdated/empty-list/missing-meta + interceptor autoApprove/empty-path/path-alias/turn_end-dedupe/unifiedDiff + task-registry empty-list/archive/eventSummary-flow + markdown-index delete-unknown/empty-body/scope+type residual；desktop unit **114 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave170-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-171 residual**：NSIS re-probe still **BLOCKED**；list-local-skills truncate-100/CRLF/non-dir + plan-file-service corrupt-null/basename-escape/complete-dest + agent-modes shell-git-mkdir-case + builtin-skillhub fields/tar-safe + logger empty-arg/debug-fallback residual；desktop unit **97 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave171-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-173 residual**：unit diagnostics/session-path/tray-icon/workspace-selection **67** + shared ipc-guards **19**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave173-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-174 residual**：NSIS re-probe still **BLOCKED**；updater equal/prerelease/stable-over-pre + ENOTFOUND/blank normalize + window-lifecycle beginQuit-no-tray/ensureTray-after-quit/close-while-hidden + main-window-options unknown-platform/prefs/darwin-isolation + partition brand-only/wrong-brand/unicode + project-scripts whitespace managers/script spaces/manager matrix residual；desktop unit **75 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave174-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-175 residual**：NSIS re-probe still **BLOCKED**；file-tree maxDepth/maxEntries clamps + empty-ext + file-root + first-launch whitespace tokens/key isolation/numeric fallthrough + subscription-manager throwing setup/unsub throw LIFO + isSubscribed snapshot + crash-diagnostics null payloads/double-attach/wrong events + single-instance double primary/secondary never restore/restore arity residual；desktop unit **93 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave175-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-176 residual**：NSIS re-probe still **BLOCKED**；command-risk reg/kubectl/npm publish/Remove-Item Force + whitespace/comment-prefix + session-grouping whitespace query/messagesLoaded/activityTime + sounds invalid JSON/volume0/partial settings + web-security ftp/blob/ws/malformed/non-loopback + terminal-command draft registry residual；desktop unit **112 PASS**（4 files）+ shared command-risk **61 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave176-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-177 residual**：NSIS re-probe still **BLOCKED**；formatRelative future/sub-minute + formatDuration end/invalid/negative + toDate reject objects + logger bigint/circular/bridge throw/non-function log + protected-paths .config/gcloud + pem/p12/keystore/jks/token-names + .gcloud + notifications permission denied/default/short-circuit granted residual；desktop unit **130 PASS**（4 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave177-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-178 residual**：NSIS re-probe still **BLOCKED**；export empty-session markdown + tags/summary JSON + markdown 你/AI labels + run-control missing workspace+agent no toast + IpcError stop fallback + blank Error.message→未知错误 + fuzzy-match whitespace query not trimmed + CJK ordered-char + longer-query fail + log-redaction nested apiKey/token/password + Bearer comma/semicolon + primitives residual；desktop unit **124 PASS**（4 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave178-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-179 residual**：NSIS re-probe still **BLOCKED**；ssrf-guard 169.254 edges/https+port + IPv6 fe80/fec0 + query/hash/userinfo metadata + mutation-queue missing-key seed + double-reject + nested-run no-await-deadlock + main fuzzy-match whitespace/CJK/longer-query parity + tool-call output-over-result + non-object input ignore + nested trim id/name + array/function reject residual；desktop unit **141 PASS**（4 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave179-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-180 residual**：NSIS re-probe still **BLOCKED**；app-settings workflow-over-compose + maxMode partial merge + undefined defaults isolation + events thinking_level/tool_execution_end/session_info/extension_error + ipc-guards all SETTINGS tabs + params:undefined product + legacy string pair + theme Infinity/clamp/max ladder + getInitialTheme tokens + mention-parser oversized/negative cursor + CJK resolve + full-width space residual；desktop unit **50 PASS**（2 files）+ shared-types **125 PASS**（4 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave180-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-181 residual**：NSIS re-probe still **BLOCKED**；path-canonical nested existing/missing multi-segment + readLinkTarget missing + empty target reject + web-security data/mailto/file reject + same-origin case + mixed file↔http deny + 127.0.0.1 flag + session-grouping depth cap 4 + cycle depth 2 + preview/summary match + file-tree dirs-before-files + DEFAULT_IGNORES + maxEntries floor residual；desktop unit **115 PASS**（4 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave181-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-182 residual**：NSIS re-probe still **BLOCKED**；workspace-selection empty/singleton/immutability + lastActiveAt over createdAt + single-instance secondary exit(0)/no-quit + primary double emit + sounds volume clamp + disabled play + empty storage defaults + ipc null/undefined ok + array code/fallback err + function ok + err ref stable + first-launch writeBoolFlag + mark idempotent + TRUE/FALSE uppercase residual；desktop unit **123 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave182-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-183 residual**：NSIS re-probe still **BLOCKED**；permission wildcard CJK/? + regex literal $/{/^ + nullish str/* vs null pattern throw + tool-category internal spaces + CORE vs fileWrite/shell set delta + PlanWrite underscore + evaluate fromConfig empty + disabled later * deny/allow vs evaluate last match + default ask shape + window-lifecycle requestQuit once + restore null/destroyed no-op + double attach stacks close + overlay bounds +Infinity product path + 1×1 margin 14 + Math.round .5 residual；desktop unit **132 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave183-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-184 residual**：NSIS re-probe still **BLOCKED**；runtime-policy powershell/pwsh -Command git + compose empty immutable + extensions off plan_write + plan reason over shell-off + defaults .envrc vs .env.* + empty whitelist + trailing slash key + command-risk terraform/dd/del/rmdir/chmod/curl-pipe + crash-diagnostics dispose idempotent + double-attach + non-object renderer details + main-window-options win32=linux shape + darwin frame/titleBar + prefs unfrozen residual；desktop unit **128 PASS**（4 files）+ shared-types **64 PASS**（1 file）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave184-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-185 residual**：NSIS re-probe still **BLOCKED**；pending-edits list cache invalidation + edit old/new_string + classifier high-risk paths + pnpm/yarn multi-tool + nullish name/args `" {}"` + 2>&1 vs awk system + mimocode composeModeSupported false + workflowSupported false + subagents off empty systemAgents + notifications exact true only + missing key default true + disabled send no-op residual；desktop unit **179 PASS**（4 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave185-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-186 residual**：NSIS re-probe still **BLOCKED**；formatDuration ms/s/m ladder + formatRelative hour/day/>30d + isNumberOrUndefined/NaN + generated-ui content blank/card empty/concat + normalize nulls + title-only plain + updater download idle skip + install before downloaded no quitAndInstall + initial state releasePageUrl residual；desktop unit **101 PASS**（3 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave186-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-187 residual**：NSIS re-probe still **BLOCKED**；logger Error stack/no-stack + empty extra + console.debug + project-scripts double-run prefix + Windows path script names + undefined/null scriptName → `"undefined"`/`"null"` + protected-paths home root only without workspace + outside-workspace first + .env/id_* case-insensitive + isPathInside exact/sibling-prefix + ipc-guards brand short-circuit + params ref + primitives reject + settings tab pad reject + events turn_end/agent_end optional + message_update text_delta + compaction_end residual；desktop unit **75 PASS**（3 files）+ shared-types **46 PASS**（2 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave187-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-188 residual**：NSIS re-probe still **BLOCKED**；terminal-command terraform destroy/import + curl|pwsh + rm -fr/--force + fuzzy empty query + first indexOf + underscore vs segment + subscription void setup + LIFO array unsub + agent-modes BUILD_SWITCH plan→build only + trim + empty shell reject + ssrf localhost/RFC1918 allow + exact metadata host + non-http reject residual；desktop unit **154 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave188-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-189 residual**：NSIS re-probe still **BLOCKED**；renderer fuzzy empty/first-indexOf/underscore + log-redaction x-api-key/api-key/secret + object key case + Date identity + export usage markdown + string timestamps JSON + HTML escape title/body + pi-api empty/frozen refs + app-settings null defaults + partial nested + workflow shallow-merge retains default knobs + ref isolation residual；desktop unit **104 PASS**（4 files）+ shared-types **25 PASS**（1 file）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave189-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-190 residual**：NSIS re-probe still **BLOCKED**；theme editor/diff floors + resolveTheme tokens + tray null path + cwd index5 + asar.unpacked index1 + ipc brand bare IpcError + success ref/primitives + first-launch missing/empty fallback + overwrite/idempotent mark + mutation-queue serial order + keyed multi-key isolation residual；desktop unit **119 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave190-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-191 residual**：NSIS re-probe still **BLOCKED**；localfile null workspace + .env reject + traversal + web-security blank/ws reject + file same-path only + path-canonical sibling-prefix + .env deny + sounds volume 0/1 + NaN→null JSON + malformed JSON defaults residual；desktop unit **130 PASS**（4 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave191-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-192 residual**：NSIS re-probe still **BLOCKED**；file-tree protected omit + maxEntries truncate + file/dir kinds + session-grouping parent/sibling sort + empty groups + messagesLoaded false skips think + tool-call success/executed/pausing + isError prefer + revive timestamps + persist times + git deleted/added/staged-only + empty changed + -z delete/rename + mention cursor0/empty/@span/whitespace + diagnostics dir logPath + sessionStats zeros override residual；desktop unit **165 PASS**（6 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave192-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-193 residual**：NSIS re-probe still **BLOCKED**；format duration empty/in-progress/negative + relative justNow/future + toDate identity/Invalid + notifications canNotify gates + helper titles + permission short-circuit + undefined Notification + workspace-selection equal-tie keep first + createdAt fallback + singleton identity + generated-ui upsert envelope + key_value/progress plain + equal content + maxBytes reject + single-instance primary true no exit + secondary exit(0) + restore throw forwards residual；desktop unit **172 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave193-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-194 residual**：NSIS re-probe still **BLOCKED**；export toolCalls details + zero usage + tags/summary JSON + HTML 用户/AI roles + run-control plan pausing/paused + dual-fail toast + skip markPausing + protected-paths .ssh/.aws/.kube + token/pem/sqlite + .config/gcloud + pi-api array/function as-is + reassignment + ssrf AWS IMDS IPv6 + uppercase host + empty/malformed + terminal curl|sh + git --force word form not -f + chmod 777/dd if= + chown not high residual；desktop unit **209 PASS**（6 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave194-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-195 residual**：NSIS re-probe still **BLOCKED**；tray index0/2/3/4 candidates + crash renderer dispose + uncaught origin + child-process-gone + ipc array/function ok + legacy code+fallback err + first-launch 1/0 tokens + false key still first + write true/false + mutation sync/async reject advances + keyed transform + updater available/progress/not-available events residual；desktop unit **158 PASS**（6 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave195-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-196 residual**：NSIS re-probe still **BLOCKED**；log-redaction empty/sk-/Error stack + nested Buffer + path-canonical missing workspace + .ssh reject + command-risk git -f normal/--force high + id_ed25519 + irm|pwsh + fuzzy case-insensitive + ordered camel + out-of-order fail + subscription empty-array + re-ensure + web-security malformed + 0.0.0.0/::1 deny + sounds preserve fields + missing key defaults + logger circular + console.error fallback residual；desktop unit **224 PASS**（7 files）；shared-types command-risk **66 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave196-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-197 residual**：NSIS re-probe still **BLOCKED**；agent-modes compose dual-flag + plan→build BUILD_SWITCH + plan read tools/git + events tool_execution_update/message_start/end + usage/context/custom + app-settings enabled false nested + dream interval + composeWorkflow legacy + diagnostics omit sessions/logPath + versions/details copy + format minute boundary + isNumberOrUndefined/isValidTimestamp + protected-paths secrets.txt/private.key/credentials.yml + isPathInside parent/trailing-sep + theme apply light/dark + getInitialTheme token + font clamp floors + overlay fractional negative workArea + zero workArea residual；desktop unit **193 PASS**（6 files）；shared-types events+app-settings **50 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave197-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-198 residual**：NSIS re-probe still **BLOCKED**；ssrf localhost/LAN allow + 169.254 block + fe80 variants + tool-call empty toolCalls new object + modern-only identity + workspace lastActiveAt 0 + null fallback + terminal git clean/npm publish/kubectl/reg + sessionMatches case/empty + group filter empty workspaces + notifications canNotify gates + helpers no-op when disabled + ipc-guards wrong-brand legacy + SETTINGS tabs endpoints residual；desktop unit **208 PASS**（6 files）；shared-types ipc-guards **27 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave198-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-199 residual**：NSIS re-probe still **BLOCKED**；git ahead/behind + default branch + gitDiff/commit/createBranch + export HTML escape + string timestamps + toolCalls details + omit tags/summary + generated-ui table/form/chart/callout/metric + column defaults + mention nearest @ + empty query + email-like + resolve splice + tool-call status aliases + reviveDate + normalizeToolCalls filters + isError preference + mutation-queue serial concurrent + keyed throw + sync/async return + window-lifecycle restore minimized + ensureTray reuse + beginQuit clears tray + mimocode build-only defaults + memory nested + maxMode unsupported + workflow inherits composeModeSupported + localfile .ssh 403 + net.fetch 404 + URL-encoded path residual；desktop unit **284 PASS**（9 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave199-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-200 residual**：NSIS re-probe still **BLOCKED**；main-window platform tokens + chrome key matrix + project-scripts coerce + manager matrix + pi-api primitives/null + live reassignment + ipc code/fallback partials + wrong-brand non-string + single-instance secondary no listener + double register last-wins fake + crash double attach dispose isolation + run-control task toast + agentsAbort only + empty ids + command-risk empty/mkfs/checkout -- ./publish/reg/uninstall + terminal mirrors + tray null six paths + prefer first residual；desktop unit **223 PASS**（9 files）；shared-types command-risk **68 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave200-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-201 residual**：NSIS re-probe still **BLOCKED**；web-security same-origin + file same-path + external schemes + log-redaction sensitive keys + Bearer/Cookie + circular/bigint + subscription void/array LIFO + cleanup idle + protected-paths .aws/.kube/.docker + pem/sqlite + outside workspace + diagnostics sessionStats preference + storageRoot basename + logPath missing + logger Error stack + piAPI throw fallback + debug + path-canonical allow nested + .env.local deny + ipc-guards factory params + SETTINGS tabs exact order residual；desktop unit **236 PASS**（7 files）；shared-types ipc-guards **29 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave201-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-202 residual**：NSIS re-probe still **BLOCKED**；agent-modes shell metachar + path aliases + plan_write empty + registry defaults + session-grouping favorite/parent-child + thinking loaded + multi-session sort + ssrf metadata case/https + 0.0.0.0/::1 + command-risk reset/clean/del/kubectl/terraform/dd + format null-end/进行中 + workspace empty/equal-tie + protected-paths gnupg/azure/gcloud/key material + web-security allowDevLocalhost false + log-redaction MaxDepth/sk- + tool-call-normalization identity/legacy prefer residual；desktop unit **382 PASS**（9 files）；shared-types command-risk **70 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave202-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-203 residual**：NSIS re-probe still **BLOCKED**；theme system/string font + first-launch tokens + fuzzy mid-token/hyphen parity + terminal reset/clean/kubectl/terraform + sounds clamp/partial JSON + notifications exact true + Chinese helpers + subscription nullish LIFO + app-settings defaultMode/planMode + events tool/queue/auto_retry residual；desktop unit **254 PASS**（8 files）；shared-types **54 PASS**（2 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave203-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-204 residual**：NSIS re-probe still **BLOCKED**；classifier shell-rc/.pi/System32 high + pipe mutation + multi-tool read whitelist + pending-edits lifecycle + queue-store cap/autoRetry/extension_error + approval-store clear/timeout0/diff + helpers schema/import + mention mid-token/path-like + mutation-queue independence + ipc partition + project-scripts empty + pi-api ref + path-canonical outside/.env + ipc-guards brand/params/tabs residual；desktop unit **405 PASS**（11 files）；shared-types ipc-guards **32 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave204-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-205 residual**：NSIS re-probe still **BLOCKED**；main-window chrome/perf + single-instance restore/exit order + tray path order + crash dispose isolation + diagnostics sessionStats/log + logger stringify/circular + schemas terminal/Task/Goal/Subagent/toolCall/thinking/Plan residual；desktop unit **253 PASS**（7 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave205-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-206 residual**：NSIS re-probe still **BLOCKED**；command-risk curl\|sh/iwr/chmod/force-with-lease/Remove-Item + protected-paths npmrc/netrc + ssrf exact-metadata/localhost-allow + web-security same-origin + log-redaction Bearer/nested keys + overlay bounds margin + tool-call nested/partial/persistence residual；desktop unit **288 PASS**（6 files）；shared-types command-risk **72 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave206-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-207 residual**：NSIS re-probe still **BLOCKED**；window-lifecycle restore/close-after-quit/hasTray + localfile id_rsa/known_hosts/secrets.db/.kube/.docker + mimocode planModeSupported/workflowSupported/dream-distill/tool-strip + updater releaseNotes/progress/downloaded/error/dispose + agent-modes normalize/goal clone/plan→build empty + generated-ui title-only/form/timeline residual；desktop unit **175 PASS**（6 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave207-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-208 residual**：NSIS re-probe still **BLOCKED**；export HTML XSS/usage defaults/roles + session-grouping depth/cycle/favorite/empty-ws + run-control IpcError/plan dual-fail/missing stop + format toDate/duration/iso + workspace-selection solo/createdAt + tray-icon null/first-wins residual；desktop unit **255 PASS**（6 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave208-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-209 residual**：NSIS re-probe still **BLOCKED**；sounds enable/volume/invalid JSON + notifications canNotify/send no-op + first-launch 1/0/idempotent + theme resolve/garbage/CSS vars + terminal curl\|sh/chmod/reg/npm-g draft + subscription ensure idempotent residual；desktop unit **213 PASS**（6 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave209-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-210 residual**：NSIS re-probe still **BLOCKED**；pi-api bool/fn/delete + project-scripts unknown manager/unicode + ipc partition 0/""/brand + fuzzy renderer/main parity + ipc-guards/SETTINGS anchors + app-settings workflow merge residual；desktop unit **125 PASS**（5 files）；shared-types **65 PASS**（2 files）；combined **190 PASS**（7 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave210-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-211 residual**：NSIS re-probe still **BLOCKED**；logger throw-fallback/null-args + mention win/unicode + log-redaction bigint/Error sk- + ssrf fe80/data/ws + path-canonical id_rsa/npmrc + tool-call args>input + command-risk sudo/rm -rf residual；desktop unit **296 PASS**（6 files）；shared-types command-risk **74 PASS**；combined **370 PASS**（7 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave211-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-212 residual**：NSIS re-probe still **BLOCKED**；tool-category network/CORE + protected-paths .env/secrets/ssh + single-instance secondary/primary + crash child-gone/dispose + format duration edges + events discriminant/queue residual；desktop unit **196 PASS**（5 files）；shared-types events **27 PASS**；combined **223 PASS**（6 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave212-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-213 residual**：NSIS re-probe still **BLOCKED**；main-window unknown platform + diagnostics sessionStats/clone + tray null/6-paths + workspace lastActiveAt 0 + run-control empty ids + defaults .env ask residual；desktop unit **186 PASS**（6 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave213-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-214 residual**：NSIS re-probe still **BLOCKED**；agent-modes plan writes/shell + runtime-policy git/filter/freeze + session-grouping thinking/parent + terminal iwr/git draft + first-launch throw fallback + theme cascade/watch + subscription managers residual；desktop unit **290 PASS**（7 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave214-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-215 residual**：NSIS re-probe still **BLOCKED**；evaluate multi-ruleset/~/HOME/EDIT_TOOLS + fts quote-split + session-path stable/48 + sounds clamp + notifications exact-true + export md/html/json residual；desktop unit **232 PASS**（6 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave215-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-216 residual**：NSIS re-probe still **BLOCKED**；wildcard multiline/trailing-star/dots + web-security schemes/IPv4/same-origin + memory-path-guard checkpoint allowlist + mutation-queue identity + generated-ui slice-then-drop maxSections-1 + command-risk del/terraform/git checkout residual；desktop unit **227 PASS**（5 files）；shared-types command-risk **76 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave216-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-217 residual**：NSIS re-probe still **BLOCKED**；git-service unstage/diff-staged/batch-protect/checkout invalidArgs + protected-paths pypirc/id_pub/gnupg + fuzzy segment/order parity + formatDuration negative/now + ssrf 169.254/16/metadata + log-redaction primitives/api-key + app-settings null/mode/workflow residual；desktop unit **330 PASS**（7 files）；shared-types app-settings **33 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave217-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-218 residual**：NSIS re-probe still **BLOCKED**；path-canonical missing leaf/.env.local + tool-call status aliases/persistence + mention last-@ + logger debug fallback + events auto_retry/compaction + ipc-guards brand/tabs + workspace-selection empty/tie residual；desktop unit **217 PASS**（5 files）；shared-types **65 PASS**（2 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave218-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-219 residual**：NSIS re-probe still **BLOCKED**；defaults doom_loop/env + tool-category write/shell/plan + main-window chrome + tray first-existing + terminal high-risk draft + session-path stable + diagnostics sessionStats residual；desktop unit **207 PASS**（7 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave219-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-220 residual**：NSIS re-probe still **BLOCKED**；sounds clamp/notifications exact-true/first-launch/single-instance/export escape product-aligned/command-risk residual；desktop unit **207 PASS**（5 files）；shared-types command-risk **78 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave220-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-221 residual**：NSIS re-probe still **BLOCKED**；agent-modes/theme/session-grouping/subscription-manager/crash-diagnostics/local-file-protocol residual；desktop unit **228 PASS**（6 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave221-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-222 residual**：NSIS re-probe still **BLOCKED**；window-lifecycle/overlay/run-control/pi-api/ipc/mimocode-runtime-port residual；desktop unit **177 PASS**（6 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave222-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-223 residual**：NSIS re-probe still **BLOCKED**；wildcard/runtime-policy/evaluate/fts-query/classifier residual；desktop unit **292 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave223-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-224 residual**：NSIS re-probe still **BLOCKED**；judge-prompt/memory-path-guard/pending-edits/task-id/paths residual；desktop unit **205 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave224-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-225 residual**：NSIS re-probe still **BLOCKED**；web-security/updater/events/approval residual；desktop unit **82 PASS**（2 files）；shared-types **34 PASS**（2 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave225-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-226 residual**：NSIS re-probe still **BLOCKED**；directives-pure/session-path/approval-bridge/plan-prompt residual；desktop unit **81 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave226-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-227 residual**：NSIS re-probe still **BLOCKED**；reconcile/IpcError/plan-execution/spawn-handler/first-launch residual；desktop unit **51 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave227-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-228 residual**：NSIS re-probe still **BLOCKED**；settings-nav/plan-sync pure/session-summary/asset-inventory residual；desktop unit **27 PASS**（4 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave228-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-229 residual**：NSIS re-probe still **BLOCKED**；plan-utils/file-scanner/settings-bridge residual；desktop unit **54 PASS**（3 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave229-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-230 residual**：NSIS re-probe still **BLOCKED**；claude/codex importer/subagent registry/pi-packages-store residual；desktop unit **51 PASS**（4 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave230-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-231 residual**：NSIS re-probe still **BLOCKED**；native-session-fork pure/skillhub/memory-service/pi-driver residual；desktop unit **35 PASS**（4 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave231-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-232 residual**：NSIS re-probe still **BLOCKED**；task-service/sdk-runtime/session-summary-service residual；desktop unit **35 PASS**（3 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave232-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-233 residual** (2026-07-21 20:32 UTC): unit depth only — fuzzy-match / fts-query / theme / list-local-skills / checkpoint-service green (106/5); typecheck PASS; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-234 residual** (2026-07-21 20:38 UTC): unit depth only — wildcard / tool-category / defaults / toast-store / task-id green (161/5); typecheck PASS; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-235 residual** (2026-07-21 23:57 UTC): unit depth only — attachments-store / skillhub / main-window-options / mutation-queue green (104/4); typecheck PASS; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-236 residual** (2026-07-22 00:02 UTC): unit depth only — evaluate / runtime-policy / first-launch green (131/3); typecheck PASS; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-237 residual** (2026-07-22 00:07 UTC): unit depth only — markdown-index / markdown-memory-service / shortcuts registry green (96/3); typecheck PASS; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-238 residual** (2026-07-22 00:10 UTC): unit depth only — format / mention-parser / logger green (161/3); typecheck PASS; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-239 residual** (2026-07-22 00:16 UTC): unit depth only — tool-call / pi-status-store / updater-store green (96/3); typecheck PASS; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-240 residual** (2026-07-22 00:21 UTC): unit depth only — runtime-feature-store / insert-reminders / command-risk / app-settings green (desktop 67/2 + shared-types 185/5); typecheck PASS; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-241 residual** (2026-07-22 00:23 UTC): unit depth only — session-path / agent-mode-store / approval-store diffs / events green (desktop 87/3 + events 33/1); typecheck PASS; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-242 residual** (2026-07-22 00:27 UTC): unit depth only — native-session-fork pure / agent-info / plan-prompt / ipc-guards green (desktop 76/3 + ipc-guards 38/1); typecheck PASS; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-243 residual** (2026-07-22 00:32 UTC): unit depth only — ssrf-guard / fts-query / fuzzy-match / wildcard green (193/4); typecheck PASS; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-244 residual** (2026-07-22 08:38 UTC): unit depth only — tool-category / memory-path-guard / theme / defaults green (157/4); typecheck PASS; product note: `isInsideMemoryTree` pure string-prefix (no path.resolve); NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-245 residual** (2026-07-22 08:44 UTC): unit depth only — classifier / pending-edits / permission-store / skills-store green (197/4); typecheck PASS; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-246 residual** (2026-07-22 08:48 UTC): unit depth only — log-redaction / subscription-manager / terminal-command / command-risk green (desktop 125/3 + shared-types 82/1); typecheck PASS; product note: spaced `token =` redacts to `token=[REDACTED]`; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-247 residual** (2026-07-22 08:52 UTC): unit depth only — evaluate / runtime-policy / protected-paths / workspace-selection green (213/4); typecheck PASS; product note: `cmd /c "git status"` not unwrapped for git detect; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-248 residual** (2026-07-22 00:58 UTC): unit depth only — main-window-options / tray-icon / single-instance / web-security green (158/4); typecheck PASS; product note: chrome fresh objects + sunos frameless; tray 6-slot short-circuit; multi-emit restore; ftp/ws/blob external deny + localhost only with allowDevLocalhost; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-249 residual** (2026-07-22 01:00 UTC): unit depth only — path-canonical / local-file-protocol / crash-diagnostics / window-lifecycle green (138/4); typecheck PASS; product note: `.git-credentials` not sensitive-pattern; empty workspace string → localfile 403; requestQuit once; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-250 residual** (2026-07-22 01:03 UTC): unit depth only — diagnostics / updater / desktop-overlay / sqlite-session green (95/4); typecheck PASS; product note: log Cookie redaction; progress zeros; overlay margin 14; archive kept in listSessions; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-251 residual** (2026-07-22 01:06 UTC): unit depth only — agent-modes / mimocode-runtime-port / ssrf-guard / app-settings green (desktop 156/3 + shared-types 37/1); typecheck PASS; product note: plan→build BUILD_SWITCH; composeModeSupported drops compose; 169.254/16 full block; defaults deep clone; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-252 residual** (2026-07-22 01:10 UTC): unit depth only — wildcard / ipc-guards / events / approval green (desktop 43/1 + shared-types 79/3); typecheck PASS; product note: `*` greedy multi-segment; brand short-circuits isIpcError; queue_update/compaction events; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-253 residual** (2026-07-22 01:13 UTC): unit depth only — command-risk / git-service / guarded-tools green (desktop 66/2 + shared-types 84/1); typecheck PASS; product note: curl|sh high; porcelain worktree buckets; .env write deny + shell-off bash; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-254 residual** (2026-07-22 01:16 UTC): unit depth only — fts-query / file-scanner / format / fuzzy-match green (161/4); typecheck PASS; product note: FTS quote-split OR; maxResults cap; relative 24h day; fuzzy prefix 100; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-255 residual** (2026-07-22 01:21 UTC): unit depth only — theme / terminal-command / first-launch / mention-parser green (131/4); typecheck PASS; product note: font clamp 12–20; classifyTerminalCommand mirrors isHighRiskCommand; readBoolFlag true/1|false/0 only; mention cursor0/whitespace; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-256 residual** (2026-07-22 01:27 UTC): unit depth only — export / tool-call / session-grouping / project-scripts green (180/4); typecheck PASS; product note: export omit usage; status aliases; empty workspace drop; case-sensitive managers; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-257 residual** (2026-07-22 01:29 UTC): unit depth only — sounds / notifications / ipc / logger green (174/4); typecheck PASS; product note: volume preserve; canNotify granted gate; brand short-circuit partition; piAPI log stringify; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-258 residual** (2026-07-22 01:33 UTC): unit depth only — mutation-queue / subscription-manager / pi-api / protected-paths green (155/4); typecheck PASS; product note: async reject advances; void ensure; .git-credentials allowed; cert/db blocked; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-259 residual** (2026-07-22 01:36 UTC): unit depth only — workspace-selection / web-security / log-redaction / generated-ui green (196/4); typecheck PASS; product note: empty pick undefined; js/data external deny; x-api-key redact; missing version→v1 title; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-260 residual** (2026-07-22 01:40 UTC): unit depth only — ssrf-guard / classifier / events / app-settings green (desktop 196/2 + shared-types 76/2); typecheck PASS; product note: fe80/IMDS v6 block; high-risk bash elevate; unknown LH keys preserved; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-261 residual** (2026-07-22 01:44 UTC): unit depth only — path-canonical / single-instance / main-window-options / command-risk green (desktop 109/3 + shared-types 86/1); typecheck PASS; product note: outside/.env deny; secondary exit(0); darwin framed; dd if= high; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-262 residual** (2026-07-22 01:50 UTC): unit depth only — tray-icon / window-lifecycle / mimocode-runtime-port / approval green (desktop 104/3 + shared-types 6/1); typecheck PASS; product note: 6 tray candidates; restore no-op; beginQuit idempotent; composeModeSupported false drops compose; ApprovalRequest confirm|select only; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-263 residual** (2026-07-22 01:58 UTC): unit depth only — local-file-protocol / crash-diagnostics / diagnostics / desktop-overlay-window-policy / ipc-guards green (desktop 129/4 + shared-types 42/1); typecheck PASS; product note: config basename protected; sessionStats override; overlay clamp/margin14; SETTINGS 10 tabs; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-264 residual** (2026-07-22 02:06 UTC): unit depth only — list-local-skills / fts-query / evaluate / runtime-policy / agent-modes green (desktop 234/5); typecheck PASS; product note: skills cache TTL; FTS empty/emoji null; EDIT_TOOLS; plan denies bash; registry flag filters; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-265 residual** (2026-07-22 02:11 UTC): unit depth only — memory-path-guard / file-scanner / guarded-tools / updater / command-risk green (desktop 153/4 + shared-types 88/1); typecheck PASS; product note: cc deny; scan maxDepth0; policy controller; getState copy; del/rmdir high; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-266 residual** (2026-07-22 02:16 UTC): unit depth only — tool-category / wildcard / defaults / theme / events green (desktop 144/4 + shared-types 39/1); typecheck PASS; product note: network regex; win32 wildcard case; editor floor 11; compaction threshold; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-267 residual** (2026-07-22 02:21 UTC): unit depth only — terminal-command / first-launch / mention-parser / project-scripts / session-grouping / app-settings green (desktop 190/5 + shared-types 41/1); typecheck PASS; product note: pipe download draft; last @ wins; workspace group order; LH normalize clone; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-268 residual** (2026-07-22 02:27 UTC): unit depth only — sounds / notifications / ipc / logger / approval green (desktop 182/4 + shared-types 8/1); typecheck PASS; product note: volume clamp; notification literal true; partition brand; logger console fallback; FileReview/select; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-269 residual** (2026-07-22 02:34 UTC): unit depth only — git-service / export / tool-call / fuzzy-match / sqlite-session-repository green (desktop 194/5); typecheck PASS; product note: push 60s; HEAD:path maxBuffer; generatedUi merge; pausing→running; empty query score1; search limit clamp; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-270 residual** (2026-07-22 02:38 UTC): unit depth only — tool-call-normalization / run-control / mutation-queue / fuzzy-match(main) / subscription-manager green (desktop 189/5); typecheck PASS; product note: legacy result→output; plan pause marks; queue sync throw advances; empty query score1; LIFO cleanup; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-271 residual** (2026-07-22 02:42 UTC): unit depth only — ssrf-guard / web-security / log-redaction / protected-paths / generated-ui green (desktop 304/5); typecheck PASS; product note: RFC1918 allow; IMDS block; nav same-origin; redaction MaxDepth; id_rsa deny; v2 callout plain; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-272 residual** (2026-07-22 02:46 UTC): unit depth only — path-canonical / pi-api / ipc-guards / events / app-settings / command-risk green (desktop 68/2 + shared-types 218/4); typecheck PASS; product note: readLinkTarget plain; SETTINGS 10 ordered; compaction reasons; defaultMode normalize; curl|bash high; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-273 residual** (2026-07-22 02:48 UTC): unit depth only — diagnostics / crash-diagnostics / tray-icon / desktop-overlay-window-policy / fts-query green (desktop 191/5); typecheck PASS; product note: sessionStats override; dispose isolates; 6 tray candidates; overlay margin14; FTS phrase tokens; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-274 residual** (2026-07-22 02:58 UTC): unit depth only — memory-path-guard / file-scanner / evaluate / list-local-skills / runtime-policy green (desktop 244/5); typecheck PASS; product note: checkpoint-writer allowlist; scan maxDepth6; EDIT_TOOLS share; skills 30s same-ref; plan deny set 6; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-275 residual** (2026-07-22 03:02 UTC): unit depth only — tool-category / wildcard / defaults / agent-modes / guarded-tools green (desktop 209/5); typecheck PASS; product note: plan_write mode-required; space-star optional; .env.example allow; SYSTEM_SUBAGENTS; plan immutable deny; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-276 residual** (2026-07-22 03:06 UTC): unit depth only — protected-paths / path-canonical / ssrf-guard / fts-query / command-risk green (desktop 243/4 + shared-types 92/1); typecheck PASS; product note: .gnupg block; missing nested canonical; localhost allow; CJK FTS; dd/mkfs/kubectl high; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-277 residual** (2026-07-22 03:09 UTC): unit depth only — log-redaction / web-security / tray-icon / diagnostics / theme green (desktop 225/5); typecheck PASS; product note: sensitive keys redact; localhost-only dev nav; tray null/6; sessionStats fallback; font clamp; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-278 residual** (2026-07-22 03:13 UTC): unit depth only — mutation-queue / fuzzy-match / subscription-manager / events green (desktop 167/4 + shared-types 43/1); typecheck PASS; product note: async reject advances; fuzzy parity; ensure idempotent; agent_start/turn_end typed; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-279 residual** (2026-07-22 03:18 UTC): unit depth only — ipc-guards / app-settings / approval / crash-diagnostics / generated-ui green (desktop 89/2 + shared-types 101/3); typecheck PASS; product note: SETTINGS 10; brand short-circuit; nested LH merge; crash log shapes; v1 drops callout; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-280 residual** (2026-07-22 03:20 UTC): unit depth only — first-launch / terminal-command / mention-parser / overlay-policy / project-scripts green (desktop 208/5); typecheck PASS; product note: bool flag storage; high→draft; empty @ query; overlay margin14; unknown npm run; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-281 residual** (2026-07-22 03:24 UTC): unit depth only — sounds / notifications / session-grouping / tool-call / run-control green (desktop 273/5); typecheck PASS; product note: volume clamp; literal true notify; depth cap4; status aliases; isIpcError stop; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-282 residual** (2026-07-22 03:30 UTC): unit depth only — export / sqlite-session-repository / git-service / format / main-window-options green (desktop 207/5); typecheck PASS; product note: usage tokens→0; downloadFile blob; search limit20 matchIndex; execGit 10s/10MB; duration boundaries; chrome keys; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-283 residual** (2026-07-22 03:38 UTC): unit depth only — single-instance / local-file-protocol / list-local-skills / evaluate / updater green (desktop 181/5); typecheck PASS; product note: primary no quit/exit; 404 missing file; skill desc 100-cap; default ask + merge last-wins; ECONNRESET network msg; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-284 residual** (2026-07-22 03:41 UTC): unit depth only — wildcard / defaults / ssrf-guard / protected-paths / command-risk green (desktop 238/4 + shared-types 94/1); typecheck PASS; product note: regex escape; env.example allow; IMDS IPv6 block; pem/db secrets; sudo/npm publish high; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-285 residual** (2026-07-22 03:45 UTC): unit depth only — web-security / log-redaction / path-canonical / fts-query / file-scanner green (desktop 247/5); typecheck PASS; product note: http(s) only external; MaxDepth/Circular redact; outside workspace deny; emoji FTS null; ignore node_modules; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-286 residual** (2026-07-22 03:48 UTC): unit depth only — agent-modes / tool-category / memory-path-guard / theme / approval green (desktop 195/4 + shared-types 12/1); typecheck PASS; product note: LH-off build-only; BUILD_SWITCH plan→build; network set tools; memory tree prefix; theme data-theme; FileReview; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-287 residual** (2026-07-22 03:55 UTC): unit depth only — runtime-policy / tray-icon / diagnostics / events / app-settings green (desktop 163/3 + shared-types 92/2); typecheck PASS; product note: filterActiveTools write/network drop; git &&/|| unwrap cmd /k; tray parent/grandparent order; sessionStats zeros override; auto_retry/tool_execution shapes; dream/distill/memory merge + workflow wins; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-288 residual** (2026-07-22 04:01 UTC): unit depth only — fuzzy-match / mutation-queue / subscription-manager / crash-diagnostics / ipc-guards green (desktop 178/4 + shared-types 48/1); typecheck PASS; product note: score priority 100/75/50/25; empty query=1; queue recovers after throw; LIFO unsub; dispose silent; SETTINGS tabs order + ipcError brand; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-289 residual** (2026-07-22 04:05 UTC): unit depth only — first-launch / terminal-command / mention-parser / overlay-policy / workspace-selection green (desktop 202/5); typecheck PASS; product note: bool 1/0/true/false; sudo/npm publish draft; mention cursor0/ws null; overlay margin14 fractional; lastActiveAt reduce ties; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-290 residual** (2026-07-22 04:08 UTC): unit depth only — window-lifecycle / project-scripts / format / session-grouping / theme green (desktop 233/5); typecheck PASS; product note: tray tooltip idempotent; close hide vs quit; pm script map; formatRelative >30d; unloaded messages skip; CSS font vars; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-291 residual** (2026-07-22 04:12 UTC): unit depth only — tool-call-normalization / task-id / sounds / notifications / tool-call green (desktop 229/5); typecheck PASS; product note: legacy toolCallId ?? id; session-isolated T-ids; volume clamp; notify Chinese titles; persistence drops args/result; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-292 residual** (2026-07-22 04:15 UTC): unit depth only — plan-prompt / judge-prompt / directives-pure / project-detector / agent-info green (desktop 111/5); typecheck PASS; product note: PLAN_DIRECTIVE 7 lines; VerdictSchema; formatPlanDirective create/edit; detectProject yarn scripts filter; listEnabledAgents order; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-293 residual** (2026-07-22 04:19 UTC): unit depth only — session-path / IpcError / tab-defs / settings-nav-metadata / native-session-fork.pure green (desktop 57/5); typecheck PASS; product note: fragment lower+48cap+sha16; translateIpcError key-echo fallback; 10 strict tabs; empty search []; selectDesktopMessages slice(0,i+1); NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-294 residual** (2026-07-22 04:28 UTC): unit depth only — plan-execution-prompt / DiffViewer.pure / GeneratedUiTable.pure / GeneratedUiForm.pure / DateGroupedSessionList.pure green (desktop 43/5); typecheck PASS; product note: Boolean optional filter + DONE/PLAN_DONE; CONTEXT_EXPAND=3 fold; compareValues/formatValue edges; field empty rules; date bucket sessionActivityTime; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-295 residual** (2026-07-22 04:34 UTC): unit depth only — plan-utils / ssrf-guard / diff-parser / file-tree / project-detector green (desktop 182/5); typecheck PASS; product note: findReusable reverse+preferred fallthrough; isSafeUrl metadata/link-local; parseDiff /dev/null backfill; listDirectory file+ignores; detectProject python/rust/go/bun; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-296 residual** (2026-07-22 04:39 UTC): unit depth only — command-risk / format / ipc partition / terminal-command / workspace-selection green (desktop 209/4 + shared-types 97/1); typecheck PASS; product note: force-push/clean-f/curl|sh/terraform; duration+relative boundaries; partition IpcError shapes; draft↔high mirror; lastActiveAt reduce ties; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-297 residual** (2026-07-22 04:44 UTC): unit depth only — tool-category / evaluate / protected-paths / wildcard / theme green (desktop 260/5); typecheck PASS; product note: network substring; EDIT_TOOLS disabled alias; isPathInside; wildcard * is .*; font clamp [12,20] + mono floors; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-298 residual** (2026-07-22 04:47 UTC): unit depth only — classifier / runtime-policy / log-redaction / plan-prompt / fuzzy-match green (desktop 313/5); typecheck PASS; product note: EXTRA_HIGH_RISK+mutation+multi-tool; plan immutableDenied; redact keys/Bearer/sk-; PLAN_DIRECTIVE 7 lines; fuzzy 100/75/50/25; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-299 residual** (2026-07-22 04:51 UTC): unit depth only — agent-info / session-grouping / directives-pure / path-canonical / first-launch green (desktop 237/5); typecheck PASS; product note: listEnabledAgents omit plan/compose; sessionDepth cap/cycle; formatPlanDirective create/edit; assertWorkspacePathAllowed; firstLaunch flags; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-300 residual** (2026-07-22 04:59 UTC): unit depth only — usage-aggregation / settings-nav-metadata / file-workspace-utils / tool-call / defaults green (desktop 162/5); typecheck PASS; product note: formatUsageNumber cleanFixed; searchSettings AND rank; formatBytes /102.4; normalizeStatus aliases; buildDefaults whitelist + env last-wins; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-301 residual** (2026-07-22 05:03 UTC): unit depth only — native-session-fork / task-id / session-path / IpcError / tool-call-normalization green (desktop 110/5); typecheck PASS; product note: selectDesktopMessages falsy full history; nextTaskId parent pattern; session-path fragment+hash; translateIpcError fallback; legacy toolCall ?? map; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-302 residual** (2026-07-22 05:05 UTC): unit depth only — web-security / main-window-options / project-scripts / tab-defs / judge-prompt green (desktop 157/5); typecheck PASS; product note: external http(s) only; navigation same-origin/file path/localhost; chrome opaque frameless win; projectScriptCommand PM map; isSettingsTab 10 ids; VerdictSchema; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-303 residual** (2026-07-22 05:09 UTC): unit depth only — desktop-overlay / single-instance / mention-parser / pi-driver-detect / approval green (desktop 145/4 + shared-types 14/1); typecheck PASS; product note: overlay margin 14 + size clamp; single-instance primary/secondary; mention last-@ + resolve span; detectSync missing CLI; approval RiskLevel/DeferredEdit/FileReview; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-304 residual** (2026-07-22 05:26 UTC): unit depth only — diff-parser / GeneratedUiChart / project-detector / plan-utils / GeneratedUiTable green (desktop 95/5); typecheck PASS; product note: parseDiff multi-file + extractDiff prefers JSON.diff; buildChartOption label/key fallback + stacked total + pie first series; detectProject java/unknown/yarn/Cargo first table; samePlanIdentity empty-filename→title; formatValue boolean after number checks; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-305 residual** (2026-07-22 05:30 UTC): unit depth only — DiffViewer / DateGroupedSessionList / GeneratedUiForm / plan-execution-prompt / file-tree green (desktop 72/5); typecheck PASS; product note: CONTEXT_EXPAND=3 fold >7; date buckets 0/1/7/30; form empty '',false,[]; plan prompt Boolean filter + DONE contract; listDirectory clamp 50 + dir-first sort; NSIS K-004/K-014/K-015 still **BLOCKED**; scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 174 全绿**.
- **wave-306 residual (unit depth only)**: event-bridge custom_message defaults + lifecycle/onTurnEnd; generateUnifiedDiff path basename & split counts; usage-aggregation format thresholds + workspace/archive filters; settings-nav-metadata search ranking/anchors; sdk-runtime resolvePiSdkEntry candidate depths + load cache. Vitest 87 PASS / typecheck PASS. Scoreboard **not** inflated (PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3). NSIS still BLOCKED. ≠174 全绿.
- **wave-307 residual (unit depth only)**: usePlanSyncEffect pure normalize/findReusable; shortcuts registry matchesCombo/dispatch/register overwrite; runtime-feature mode clamps; directives formatPlanDirective/formatComposeDocsBlock; factory resolveBundled* paths. Vitest 129 PASS / typecheck PASS. Scoreboard **not** inflated (PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3). NSIS still BLOCKED. ≠174 全绿.
- **wave-308 residual (unit depth only)**: file-workspace-utils path/diff/git; skillhub tar safety + search JSON; pi-package catalog/list parsers; IpcError translate fallbacks. Vitest 77 PASS / typecheck PASS. Scoreboard **not** inflated (PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3). NSIS still BLOCKED. ≠174 全绿.
- **wave-309 residual (unit depth only)**: mention-parser cursor/@ token edges; insert-reminders mode gates; agent-info enablement; command-risk classify/isHighRisk product patterns. Desktop 171 PASS + shared-types 253 PASS / typecheck PASS. Scoreboard **not** inflated (PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3). NSIS still BLOCKED. ≠174 全绿.
- residual wave-310 (2026-07-21): product-aligned unit depth on protected-paths (sensitive file/dir edges + isPathInside sibling-prefix), theme (applyFontSize CSS scale + getInitialFontSize/Theme floors), first-launch (bool flag fallback/write), spawn-handler (formatSubagentSummary status switch + buildSubagentCustomTools wiring). Vitest 134 PASS / 4 files; typecheck PASS; dual evidence under docs/testing/evidence/wave310-* + C:/tmp + TEMP + /tmp. Scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3 (K-004/K-014/K-015). ≠174 full green; host NSIS install still forbidden.
- residual wave-311 (2026-07-21): product-aligned unit depth on approval classifier (empty shell, EXTRA_HIGH_RISK, multi-tool whitelist), agent-modes plan-path/mkdir/worktree/normalize, pending-edits review/list-cache/id shape, normalizeLongHorizonSettings composeWorkflow alias + defaultMode. Desktop vitest 213 PASS / 3 files; shared-types 49 PASS / 1 file; typecheck PASS; dual evidence wave311-*. Scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3. ≠174 full green; host NSIS still forbidden.
- residual wave-312 (2026-07-21): product-aligned unit depth on web-security URL allowlists, session-grouping group/sort/depth/matches, command-risk HIGH_RISK patterns (mkfs/irm/kubectl/terraform/npm publish/rmdir). Desktop 130 PASS / 2 files; shared-types 101 PASS / 1 file; typecheck PASS; dual evidence wave312-*. Scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3. ≠174 full green; host NSIS still forbidden.
- residual wave-313 (2026-07-21): product-aligned unit depth on ssrf-guard isSafeUrl (local models allowed, cloud metadata/link-local blocked), mutation-queue serial/keyed recovery, fuzzy-match boundary scoring. Vitest 190 PASS / 3 files; typecheck PASS; dual evidence wave313-*. Scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3. ≠174 full green; host NSIS still forbidden.
- residual wave-314 (2026-07-21): product-aligned unit depth on exportSessionAsMarkdown/JSON/HTML (usage defaults, toolCalls, HTML escape), computeDesktopOverlayBounds margin/clamp, registerSingleInstance primary/secondary. Vitest 134 PASS / 3 files; typecheck PASS; dual evidence wave314-*. Scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3. ≠174 full green; host NSIS still forbidden.
- residual wave-315 (2026-07-21): product-aligned unit depth on ipc partition (isIpcError shapes), subscription-manager ensure/cleanup LIFO/idempotent, sounds settings clamp/persist. Vitest 151 PASS / 3 files; typecheck PASS; dual evidence wave315-*. Scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3. ≠174 full green; host NSIS still forbidden.
- residual wave-316 (2026-07-21): product-aligned unit depth on judge-prompt (JUDGE_SYSTEM independent confirm / judgeUser raw embed / VerdictSchema ok+impossible combo), tool-call-normalization (non-object identity, legacy vs modern field preference), task-id (parseInt suffix prefix, deep parent siblings, null top-level). Vitest 83 PASS / 3 files; typecheck PASS; dual evidence wave316-*. Scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3. ≠174 full green; host NSIS still forbidden.
- residual wave-317 (2026-07-21): product-aligned unit depth on workspace-selection (lastActiveAt ?? createdAt reduce, zero valid, ties keep first), fts-query (null empty, phrase OR, quote splits tokens, CJK), project-scripts (yarn/pnpm omit run; bun/npm include; exact manager equality). Vitest 137 PASS / 3 files; typecheck PASS; dual evidence wave317-*. Scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3. ≠174 full green; host NSIS still forbidden.
- residual wave-318 (2026-07-21): product-aligned unit depth on main-window-options (darwin hiddenInset vs frame false; opaque #f4f4f4; performance backgroundThrottling false), plan-prompt (7-line PLAN_DIRECTIVE read-only/plans-only/ABC choices), session-path (charset gate, sha256-16, trailing-dot→session, 48-cap, case-sensitive hash). Vitest 75 PASS / 3 files; typecheck PASS; dual evidence wave318-*. Scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3. ≠174 full green; host NSIS still forbidden.
- residual wave-319 (2026-07-21): product-aligned unit depth on approval types (RiskLevel closed set, confirm/select, DeferredEdit/FileReview fields), native-session-fork pure (inclusive slice, Chinese missing-id error, normalizeText text-parts only, toTimestamp fallback), project-detector (node preference, python/rust/go/java, hasGit, non-string scripts drop). Vitest desktop 45/2 + shared approval 16/1; typecheck PASS; dual evidence wave319-*. Scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3. ≠174 full green; host NSIS still forbidden.
- residual wave-320 (2026-07-21): product-aligned unit depth on file-tree (file node ext/size, DEFAULT_IGNORES, maxDepth/maxEntries clamps, truncated+dir-first sort), terminal-command (rm/Remove-Item/del/rmdir/chmod/curl|sh/irm|iex/npm/reg/mkfs/dd high→draft), ipc helpers (validation short-circuit, PiDriver 尚未初始化, Error label prefixes). Vitest 106 PASS / 3 files; typecheck PASS; dual evidence wave320-*. Scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3. ≠174 full green; host NSIS still forbidden.
- residual wave-321 (2026-07-21): product-aligned unit depth on tool-category (priority read/write/shell/network/extension, CORE_TOOLS closed set, plan_write mode gate), wildcard (backslash normalize, regex escape, trailing space-star optional args), memory paths (global vs scopeId required, type detect basenames, separator/.. safety). Vitest 167 PASS / 3 files; typecheck PASS; dual evidence wave321-*. Scoreboard unchanged PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3. ≠174 full green; host NSIS still forbidden.
- **wave-172 residual**：NSIS re-probe still **BLOCKED**；theme getDiff/getInitialFont/system-data-theme + single-instance lock→listener order + localfile empty-ws/sibling-prefix/unicode + mimocode planModeSupported/master-off-tools/checkpoint-writer + format 29/30-day residual；desktop unit **107 PASS**（5 files）；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave172-evidence.txt`）— scoreboard **PASS 100 / UNIT_PASS 71 / PARTIAL 0 / BLOCKED 3** — **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-88 residual**：NSIS re-probe still **BLOCKED**；form-field focus-visible 扫尾 + permission evaluate residual；unit **77 PASS**；typecheck PASS；text form no-fv **0**（`/tmp/wave88-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-87 residual**：single-instance + diagnostics residual；unit **10 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave87-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-86 residual**：local-file-protocol + path-canonical residual；unit **18 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave86-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-85 residual**：security residual log-redaction/web-security/schemas edges；unit **84 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave85-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-84 residual**：NSIS re-probe still **BLOCKED**（admin=False / Sandbox missing / host 1.0.13）；honesty E2E **4/4**；classifier multi-tool/path/redirect + ssrf edges + ChatInput FV asserts；unit **161 PASS**；typecheck PASS（`/tmp/wave84-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-83 residual**：NSIS re-probe still **BLOCKED**（admin=False / Sandbox missing / host 1.0.13）；UX ChatInput composer triggers/menus focus-visible restore；unit **47 PASS**；typecheck PASS（`/tmp/wave83-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-82 residual**：UX MySkills actions + ChatInput slash options type/focus-visible + bare-button type hygiene；unit **68 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave82-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-81 residual**：UX form fields focus-visible（Skills/MySkills/General/LongHorizon/Git branch/CommandPalette/PlanCard）；unit **79 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave81-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-80 residual**：UX ChatInput primary send/stop + @mention option focus-visible；unit **46 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave80-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-79 residual**：UX ApprovalPanel/ChatInput reference toolbar/SessionExport/Shortcuts/PiConfigEditor focus-visible；unit **60 PASS**；plain-string no-fv **0**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave79-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-78 residual**：UX App panel retry + ChatView title/retry/scroll/readOnly + UsageTab chart/session + Sidebar 新建对话 focus-visible；unit **91 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave78-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-77 residual**：UX Button focus-visible 升级 + ThinkingBlock/GeneratedUiTable/Form/Toast/Onboarding；unit **23 PASS**；typecheck PASS；NSIS still **BLOCKED**（`/tmp/wave77-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-76 residual**：UX PlanCard + DesktopOverlay stop focus-visible；unit **24 PASS**；NSIS still **BLOCKED**（`/tmp/wave76-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-75 residual**：UX CommandCard/Permission/MessageBubble/GeneratedUiCard/CommandPalette focus-visible；unit **63 PASS**；NSIS still **BLOCKED**（`/tmp/wave75-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-74 residual**：UX GitPanel/FileWorkspace/TaskProgressPanel focus-visible；unit **77 PASS**；NSIS still **BLOCKED**（`/tmp/wave74-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-73 residual**：UX GitRailControls/SessionCenter/MiniMaxCodeLayout toggle assert；unit **33 PASS**；NSIS still **BLOCKED**（`/tmp/wave73-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-35 residual**：unit helpers **7** + packages.ipc **6** + usePlanSyncEffect pure **5** + settings-nav-metadata **4**（**22**）；export plan-sync pure helpers；NSIS re-probe still **BLOCKED**（`/tmp/wave35-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-34 residual**：unit useShortcuts **4** + contentWithGeneratedUiText；NSIS re-probe still **BLOCKED**（`/tmp/wave34-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-33 residual**：unit usePrefillConsumer/useInputText/useRenderedPlanCardIds **8**；NSIS re-probe still **BLOCKED**（`/tmp/wave33-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-32 residual**：unit useLatestRequest/useTransientState/useDebouncedSave **9**；NSIS re-probe still **BLOCKED**（`/tmp/wave32-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-31 residual**：unit session-summary-tools + asset-inventory-tools **8** （cluster 19 w/ spawn-handler）；nsis honesty **4/4**；NSIS still **BLOCKED**（`/tmp/wave31-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-30 residual**：unit spawn-handler **11**；NSIS re-probe still **BLOCKED**（`/tmp/wave30-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-29 residual**：unit permission defaults + engine regression **23**；NSIS still **BLOCKED**（`/tmp/wave29-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-28 residual**：live 门控 E2E deep-interactive **5/5** + long-horizon-live **1/1** （host minimax）；NSIS re-probe still **BLOCKED**（`/tmp/wave28-evidence.txt`）— **≠ 装机 PASS / ≠ 174 全绿**。
- **wave-27 residual**：E2E **4/4** continuous-acceptance.generated + nsis honesty；NSIS still **BLOCKED**（`/tmp/wave27-evidence.txt`）— **≠ 装机 PASS**。
- **wave-26 residual**：unit permission/evaluate/wildcard/tool-category + mutation-queue + IpcError + tab-defs + diff-parser **29**；`parseDiff` `/dev/null` isNew fix；E2E **6/6** full-demo/interactive-demo/nsis-artifact-inventory + nsis honesty；NSIS still **BLOCKED**（`/tmp/wave26-evidence.txt`）— **≠ 装机 PASS**。
- **wave-25 residual**：E2E **16/16** session-bound-agent/settings-pi-config/terminal-tools/tray/updater + nsis honesty；NSIS still **BLOCKED**（`/tmp/wave25-evidence.txt`）— **≠ 装机 PASS**。
- **wave-24 residual**：E2E **27/27** permission-more/plan-mode-current-ui/session-*/settings-interactions/window-geometry + nsis honesty；NSIS still **BLOCKED**（`/tmp/wave24-evidence.txt`）— **≠ 装机 PASS**。
- **wave-23 residual**：unit sdk-runtime **4**；E2E **26/26** a11y/chat-view/command-palette/file-git/notification/permission + nsis honesty；NSIS re-probe still **BLOCKED**（`/tmp/wave23-evidence.txt`）— **≠ 装机 PASS**。
- **wave-22 residual**：unit file-workspace-utils **12** + tool-call-normalization **5**（17）；E2E **18/18** smoke/draft-clear/overlay/running-control/plan-mode/status-onboarding + nsis honesty；NSIS re-probe still **BLOCKED**（`/tmp/wave22-evidence.txt`）— **≠ 装机 PASS**。
- **wave-21 residual**：unit useMentions **5**（hooks cluster 12）；E2E **10/10** deep-use/continuous-f06-f10/provider-error + nsis honesty；NSIS re-probe still **BLOCKED**（`/tmp/wave21-evidence.txt`）— **≠ 装机 PASS**。
- **wave-20 residual**：unit **7**（useCommandPalette/useSession）；E2E **12/12** compose/m5/m6/programmer/settings-v2/third-party-model + nsis honesty；NSIS re-probe still **BLOCKED**（`/tmp/wave20-evidence.txt`）— **≠ 装机 PASS**。
- **wave-19 residual**：unit **14**（ipc/sounds/focus-trap/motion-presence）；E2E **12/12** core/continuous/git/token/generated-ui + nsis honesty；NSIS re-probe still **BLOCKED**（`/tmp/wave19-evidence.txt`）— **≠ 装机 PASS**。
- **wave-18 residual**：unit **26**（path-canonical + attachments/permission/skills store）；E2E **16/16** launch/layout/motion/m2/visual/settings/skills + nsis honesty；NSIS re-probe still **BLOCKED**（`/tmp/wave18-evidence.txt`）— **≠ 装机 PASS**。
- **wave-17 residual**：unit **45**（fuzzy/run-control/runtime-feature/first-launch/toast 等）；E2E **12/12** session + nsis honesty；NSIS re-probe still **BLOCKED**（`/tmp/wave17-evidence.txt`）— **≠ 装机 PASS**。
- **wave-16 residual**：`plan-utils` **16** + `useInputShortcuts` **9** + plan-execution **5** unit；E2E **9/9**（draft/nsis/plan/running）；security/window unit **108**；typecheck PASS；NSIS re-probe still **BLOCKED**（`/tmp/nsis-probe-wave16.txt`）— **≠ 装机 PASS**。
- **wave-10/12 NSIS 隔离**：无 Sandbox/admin/Docker + 主机已装 1.0.13 → K-004/K-014/K-015 **BLOCKED**（`/tmp/nsis-isolation-gate-wave12.txt`）；`e2e/nsis-isolation-gate.spec.ts` **3/3**（ready-pack + probe + host refuse）；就绪包：smoke + wsb + runbook — **仍 ≠ 装机 PASS**。
- 历史 postfix：`%TEMP%/e2e-full-postfix.log`（**100 / 5 / 2 / 6**）已 superseded，仅作时间线。

### 汇总

| Status | 数量 | 含义 |
|---|---:|---|
| PASS | 100 | 本波 Electron E2E / 定点契约直接证据 |
| UNIT_PASS | 71 | 单测/IPC smoke 主路径 |
| PARTIAL | 0 | — |
| BLOCKED | 3 | NSIS 装机隔离（K-004/K-014/K-015） |
| NOT_RUN | 0 | — |
| **合计** | **174** | **≠ 全绿**（3 BLOCKED） |

P0（55）：PASS 38 / UNIT_PASS 17 / PARTIAL 0 / BLOCKED 0（自动化 ID 闭环；非场景穷尽）。  
P1（101）：PASS 50 / UNIT_PASS 48 / PARTIAL 0 / BLOCKED 3。  
P2（18）：PASS 12 / UNIT_PASS 6 / PARTIAL 0 / BLOCKED 0 / NOT_RUN 0。

### 块级摘要

| 映射范围 | 代表 ID | 证据 | 状态口径 |
|---|---|---|---|
| 窗口/主题/设置再开 | A-009/A-010/A-011/B-017/B-018/B-022/K-011 | `visual-audit`、`settings-pi-config-sync` | 自动化 **PASS**；A-014 multi-scale force-dpr **PASS** |
| 工作区/会话/搜索 | C-001–C-014 核心 P0 | `session-history`、`m2`、`m5`、`deep-use` | 核心 **PASS**；C-003 越权 **UNIT_PASS** |
| 程序员主路径/M6 | D/E + 审批/设置/导出 | `m6-final`、`programmer-workflow` | **PASS**（dual-path stub 凭证） |
| 文件/终端/Git | F/G/H | `terminal-and-tools`、`file-and-git`、`right-rail-git`、`command-palette-callbacks` residual | 契约 **PASS**；F-009/H-005 **UNIT_PASS**；G-008/G-009 **UNIT_PASS**；palette→终端 **PASS** |
| 长程/记忆/任务 | I | `deep-use`、memory unit、compose、**live deep-interactive 5/5**、long-horizon wave9b | 链路 **PASS/UNIT**；I-012 **PASS**（模型写 plan_probe + BUILD_SWITCH） |
| generated UI / skills | J | `generated-ui`、`skills-and-plugins` | J-010 **PASS**；marketplace 网络 unit 边界 |
| overlay / a11y / visual | A-012/A-013/K-009/K-011 | `overlay-anchors`、`a11y`、`visual-audit` | **PASS**；A-015 theme unit **UNIT_PASS** |
| 安全 IPC | E-009–E-012/K-006 | protected-paths / classifier / schemas / smoke | **UNIT_PASS** 刷新绿 |
| 安装升级 / live provider | K-004/K-014/K-015、I-012 | NSIS inventory + isolation gate deny；live deep 5/5 + long-horizon wave9b | NSIS **BLOCKED**；I-012 **PASS** |
| DPI / perf / tray quit | A-008/A-014/K-012/K-013 | force-dpr matrix + k013 JSON + quit child windows | **PASS** |

**禁止**把 suite **116** passed、100 PASS、deep 5/5 live 或 171 PASS+UNIT 解读为矩阵 174 全部 PASS。
