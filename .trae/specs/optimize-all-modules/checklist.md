# Checklist

按 Phase 分组,每个 checkpoint 对应 spec 中的一个 Requirement 或 Task 验证点。

## Phase 1: Critical 安全与数据一致性

- [ ] `git:undo` 在 targetPath 超出 workspace 时返回 `ipcErrors.chat.gitUndoInvalid`,不调用 `rmSync`
- [ ] `git:undo` fallback 分支只对 `git status --porcelain` 显示 `??` 的 untracked 文件 `rmSync`,tracked 文件返回 ipcError
- [ ] `git:undo` 单测覆盖 workspace 外路径、tracked 文件、untracked 文件三种场景
- [ ] `long-horizon/database.ts` `listRecentMemories` WHERE 子句含 `scope='global'` 限制,跨 workspace 查询不返回无主记忆
- [ ] `long-horizon/database.ts` `searchLayered` 先过滤 `record.score != null && record.score > 0`,cutoff 语义为 `topScore * (1 - floor)`
- [ ] `long-horizon/database.ts` `buildTree` 有 `maxDepth=16` + `Set<string>` 防环
- [ ] `long-horizon/database.ts` `setSourceTasks` 用 `BEGIN/COMMIT` 事务包裹
- [ ] 跨 workspace 查询隔离单测通过
- [ ] score floor 过滤单测通过(低质量记忆被过滤)
- [ ] 循环 parent_id 防护单测通过(不栈溢出)
- [ ] `agent-runtime/registry.ts` `suppressedAgentIds` 为 `Set<string>`,按 agentId 隔离
- [ ] 多 agent 并发模式切换单测:非目标 agent 事件不被抑制
- [ ] `usePiStream.ts` 不再用 `useSessionStore()` 解构 actions,改为 `useSessionStore.getState().xxx` 或细粒度 selector
- [ ] `usePiStream.ts` followUp 分支 return 前 `promptInFlightRef.current = false`
- [ ] `usePiStream.ts` `streamPersistRef` 重建时用 spread 保留 + textRef/thinkingRef 初始化
- [ ] `usePiStream.ts` 流式时兄弟组件 re-render 次数为 0(用 React DevTools Profiler 验证)
- [ ] `usePiStream.ts` followUp 后续发送正常创建 message row(手动测试)
- [ ] `usePiStream.ts` message_end 后 thinking/toolCalls 不丢失(手动测试)
- [ ] `TopTabBar.tsx` rightSlot 不再用 `sr-only` 包裹,WorkspaceSwitcher 正常显示
- [ ] `CommandPalette.tsx` 不再在 render 体内调 `useSessionStore.getState()`,改为顶部 selector
- [ ] `CommandPalette.tsx` `results` 与 `gitContextCommands` 用 `useMemo` 包裹
- [ ] `ChatView.tsx` `handleStop` 用 `useEventCallback` 模式,无 stale closure
- [ ] `ChatView.tsx` plan action effect 拆为 `usePlanSyncEffect` + `useRenderedPlanCardIds` 独立 hook
- [ ] `settings.ipc.ts` `hasApiKey` 返回 `Boolean(p.apiKey)` 而非硬编码 false
- [ ] `settings.ipc.ts` `pi:list-skills` 接收 `workspaceId` 参数,从 store 查 path 后定位
- [ ] `services/skills/list-local-skills.ts` 抽出,IPC 层只调 service
- [ ] `preload/index.ts` `ALLOWED` 拆为 `ALLOWED_INVOKE` + `ALLOWED_SEND`,通用 `invoke` 只接受前者
- [ ] `log:write`/`approval:respond`/`approval:set-auto-approve`/`plan:respond`/`permission:respond`/`workbench:set-active-file` 不再走 `invoke` 通用方法
- [ ] `child-agent.ts` spawn 前检查 `input.signal?.aborted` 短路
- [ ] `child-agent.ts` spawn 前注册 abort listener + pendingKill 标志,spawn 后检查
- [ ] `child-agent.ts` stdin error 时 `child.kill()` + `finish(1)`,不只追加 stderr
- [ ] `child-agent.ts` `finish` 内 `removeEventListener("abort", abort)` 防 listener 累积
- [ ] `smoke-main-runtime.cjs` 复制 `out/main/index.js` 到 tmp 后 patch,不修改原文件
- [ ] `ci.yml` 在 `Build renderer` 后加 `node scripts/smoke-main-runtime.cjs` 步骤
- [ ] `release.yml` 在 typecheck/lint/test 后加 smoke 步骤
- [ ] `release.yml` "Install dependencies" 后加 `electron-rebuild -f -w node-pty,sharp` 步骤
- [ ] `electron-builder.yml` `!src/*` 改为 `!src/**/*`
- [ ] `electron-builder.yml` `warningsAsErrors: true`
- [ ] `electron-builder.yml` 含 `asarUnpack: ["**/node-pty/**", "**/sharp/**", "**/*.node"]`
- [ ] `electron-builder.yml` 删除占位 `publisherName` 或改为环境变量注入
- [ ] 干净 Windows 机器安装 release 产出的 NSIS installer,app 启动无 native module 错误
- [ ] `AGENTS.md` 删除 `better-sqlite3 (persistence)` 与 `session-sqlite.ts (v1.2 baseline)` 描述
- [ ] `AGENTS.md` 改为 "long-horizon/database.ts 使用 node:sqlite;会话持久化走 electron-store"
- [ ] `AGENTS.md` "Native modules" 列表删除 better-sqlite3(若实际未引入)
- [ ] 全代码库 grep `better-sqlite3` 无 import 语句

## Phase 2: High 性能与并发

- [ ] `git-service.ts` 全部 `execFileSync` 改为 `execFile` 异步
- [ ] `git-service.ts` 所有调用含 `timeout: 10000`
- [ ] `git-service.ts` `gitChangedFiles` 用 `git status -z` 解析,支持文件名含空格
- [ ] 大型 git 仓库切换工作区时 UI 保持响应(手动测试)
- [ ] `long-horizon/database.ts` DB 调用包到 `worker_threads`
- [ ] `migrateLegacyMemoryJsonl` 改为 `readline.createInterface` 流式 + 分批事务
- [ ] `tokenize` CJK 单字也建索引(unigram + bigram 混合)
- [ ] 用户搜单字(如"图")返回非空结果(手动测试)
- [ ] `session-store.ts` `mutexChains` 为 `Map<string, Promise<unknown>>`,按 sessionId 加锁
- [ ] `session-store.ts` 维护 `sessionIndex: Map<string, Session>`,读取时 `sessionIndex.get(id)` 替代 `all.find(...)`
- [ ] workspace A 的 `appendMessage` 不阻塞 workspace B 的写(并发测试)
- [ ] `extension-ui-bridge.ts` `pendingRequests` 为 `Map<workspaceId, Map<string, PendingRequest>>`
- [ ] `extension-ui-bridge.ts` `currentPermissionMode` 为 `Map<workspaceId, ...>`
- [ ] `extension-ui-bridge.ts` `createExtensionUiBridge` 接受 `getTargetWindow` 参数
- [ ] `approval-bridge.ts` 按 workspaceId 隔离 pending map
- [ ] 多窗口 permission 决策不串扰(开主窗口 + 设置窗口手动测试)
- [ ] `pi-session/registry.ts` `entry` 含 `subscribing: Promise<void>` 字段,复用 in-flight promise
- [ ] `pi-session/registry.ts` watchdog 改为"最后活动时间"检测,任何事件都更新 lastActivity
- [ ] `chat.ipc.ts` 拆分为 `chat-slash.ipc.ts`/`chat-plan.ipc.ts`/`chat-longhorizon.ipc.ts`/`chat-prompt.ipc.ts`/`chat-git.ipc.ts`,`chat.ipc.ts` 只保留 `setupChatIpc` 编排
- [ ] `materializeInlinePlan` 用 `fs/promises` 的 `mkdir` + `writeFile`
- [ ] `pi:runtime-feature-state` 一次性调用 `resolveBundledDesktopExtensionPaths` + memoize
- [ ] `pi:list-slash-commands` 中 `mode` 变量与 `void mode` 死代码已删除
- [ ] `pi:send` 中 `agentModeByWorkspace.set(ws.id, mode)` 在算出 mode 后立即调用,在 await 之前
- [ ] `services/ssrf-guard.ts` 抽出 `isSafeUrl`,被 `config.ipc.ts` 与 `skills.ipc.ts` 复用
- [ ] `skills.ipc.ts` `skills:github-import` URL 校验复用 `isSafeUrl`
- [ ] `skills.ipc.ts` 行 15 顶部 `rmSync` import 保留,行 204 重复动态 import 已删除
- [ ] `skills:toggle` 用 `withLock(stateFile)` 序列化 read-modify-write
- [ ] `skills:github-import` `emptyHooksDir` 用 `os.tmpdir()`,cleanup 失败加 `log.warn`
- [ ] 并发 toggle 单测:快速点击多次,状态不混乱
- [ ] `workspace.ipc.ts` 抽 `mutateWorkspaces(fn)` + mutex
- [ ] `workspace:create`/`create-empty` 加 `getProtectedPathReason(path)` 校验
- [ ] `files:select` handler 移到 `files.ipc.ts`
- [ ] `workspace:delete` 返回 `{ success: true }`
- [ ] `schemas.ts` 所有 `z.array(...)` 含 `.max(...)`
- [ ] `schemas.ts` 所有 query `z.string()` 含 `.max(256)`
- [ ] `schemas.ts` 定义 `toolCallSchema` 替代 `z.array(z.unknown())`
- [ ] `schemas.ts` 补 `gitLogSchema`/`gitStatusSchema`/`gitBranchesSchema`
- [ ] `schemas.ts` `updateSessionMetadataSchema` `lastOpenedAt` 含 `.nonnegative()`
- [ ] `shared-types/src/` 拆分为 ~20 个领域文件,`index.ts` 只 re-export
- [ ] `IpcError` 含 `__brand: "IpcError"` 字段
- [ ] 全代码库 grep `await piAPI.` 找出的未 narrow 调用已补 `isIpcError` narrow
- [ ] 运行时常量(`DEFAULT_LONG_HORIZON_SETTINGS`/`ipcError`/`isIpcError`/`classifyCommandRisk`)移到 `@pi-desktop/shared-runtime` 或显式标注保留
- [ ] `events.ts` Legacy 扁平形状加 `@deprecated since v1.0, removed in v1.3` 注释
- [ ] `preload/index.ts` 所有 `ipcRenderer.invoke` 加 `as Promise<T>` 或抽 `invokeTyped<T>` helper
- [ ] `preload/index.ts` `onPermissionUpdate` 定义 `PermissionUpdatePayload` 接口
- [ ] `preload/index.ts` `onSettingsChanged` 改为 `subscribe<AppSettings>("settings:changed", cb)`
- [ ] `preload/index.ts` `describeImages` 实现或从 `PiAPI` 类型删除
- [ ] `preload/index.ts` `permissionRespond` 第二参数收敛为 `ExtensionUiResponse | PermissionDecision`
- [ ] `compose-workflow.ts` 入口计算 `deadline`,每阶段前检查,超时调 `store.requestCancel`
- [ ] `compose-workflow.ts` 入口处 `args.commit === true && worktreeSupport.clean === false` 立即抛 IpcError
- [ ] `compose-workflow.ts` `topoSortTasks` 改 Tarjan SCC,环内串行 + 环外拓扑批次
- [ ] `compose-workflow.ts` `parseReviewResult` 改 JSON 协议,失败默认 `ready=false`
- [ ] `compose-workflow.ts` 拆分为 `phases/brainstorm.ts`/`design.ts`/`implement.ts`/`verify.ts`/`review.ts`/`report.ts`/`merge.ts`
- [ ] Compose 全局超时手动测试:设 `timeoutMs: 5000`,5s 后自动取消
- [ ] Compose AbortSignal race 测试:在 spawn 与 listener 注册之间 abort,child 仍被 kill
- [ ] Compose dirty repo fast-fail 测试:commit=true + dirty repo,立即抛错不进入任何阶段
- [ ] `ChatInput.tsx` 拆分为 5 子组件 + 3 hook,主文件 < 300 行
- [ ] `useInputText` 用 `useLayoutEffect` 调整 textarea 高度
- [ ] `usePrefillConsumer` effect 依赖数组只放 `[prefill?.id]` 稳定标识
- [ ] `FileWorkspace.tsx` 抽 `useLatestRequest()` 替换 4 处 sequence ref
- [ ] `FileWorkspace.tsx` 抽 `useDebouncedSave()` hook,unmount 统一 clearTimeout
- [ ] `FileWorkspace.tsx` 23 个 useState 改为 `useReducer` + 6 slice
- [ ] `FileWorkspace.tsx` 抽 `<SaveConflictDialog />` 子组件
- [ ] `RightRail.tsx` 15s 轮询加 `visibilityState` 监听,隐藏时 clearInterval
- [ ] `RightRail.tsx` 抽 `useTransientState(duration)` hook 替换 4 处 setTimeout
- [ ] `RightRail.tsx` 滚动监听加 `requestAnimationFrame` 节流
- [ ] 后台隐藏窗口时不再触发 git 轮询(手动测试)
- [ ] `MessageBubble.tsx` 加 `React.memo` + 自定义 areEqual
- [ ] `MessageBubble.tsx` `inferInlinePlanAction`/`toolSummary`/`splitInlineThinking` 用 `useMemo`
- [ ] `MessageBubble.tsx` `usePlanStore` selector 订阅 `state.steps[planAction?.id]` 而非整个 steps
- [ ] 虚拟列表滚动流畅(快速滚动 100+ 消息无卡顿,手动测试)
- [ ] `components/MiniMaxCode/SessionRow.tsx` 共享模块抽出
- [ ] `DateGroupedSessionList.tsx` 与 `ProjectGroupedSessionList.tsx` import 共享模块,无代码克隆
- [ ] `DateGroupedSessionList.tsx` `expandedGroups` init 用 `useMemo` 依赖 `[t]`,语言切换不丢失展开状态
- [ ] `PlanCard.tsx` 行 41-60 `statusLabel` 全部改 `t()`
- [ ] `PlanCard.tsx` 所有硬编码中文改 `t()`
- [ ] zh-CN.json / en-US.json 含 `planCard.status.*` key
- [ ] `PlanCard.tsx` `extractChoiceOptions` 用 `useMemo`
- [ ] `vitest.config.ts` 含 `@pi-desktop/shared-types` 与 `@pi-desktop/*` 别名
- [ ] `vitest.config.ts` 含 v8 coverage 配置 + thresholds(70/70/60/70)
- [ ] `vitest.config.ts` 含 `environmentMatchGlobs` 让 renderer tsx 走 jsdom
- [ ] `vitest.config.ts` `include` 含 `../../packages/shared-types/**/*.test.ts`
- [ ] `pnpm --filter @pi-desktop/desktop test` 在含 `@pi-desktop/shared-types` import 的测试文件上不报 module not found
- [ ] `pnpm -r test` 后 coverage 报告生成,lines ≥ 70%
- [ ] `ci.yml` 含 pnpm store + `.electron-cache` + `~/.cache/electron` cache
- [ ] `ci.yml` 含 matrix `os: [windows-latest, macos-latest, ubuntu-latest]`
- [ ] `ci.yml` typecheck/lint/test/build 拆为 `quality` + `build` 两个并行 job
- [ ] `ci.yml` 含 E2E job `needs: build`
- [ ] `ci.yml` 含 `actions/upload-artifact@v4` 上传 `apps/desktop/out/`
- [ ] `eslint.config.js` 删除 30+ 行手动 globals 列表
- [ ] `eslint.config.js` `react-hooks/exhaustive-deps` 为 `error`
- [ ] `eslint.config.js` `no-empty` `allowEmptyCatch: false`
- [ ] `eslint.config.js` 含 `eslint-plugin-import` + `eslint-plugin-jsx-a11y`
- [ ] `lefthook.yml` 含 `pre-push: commands: test`
- [ ] `lefthook.yml` 含 `commit-msg` conform 校验
- [ ] `lefthook.yml` typecheck/lint 含 `files: git diff --name-only HEAD HEAD~1 -- '*.ts' '*.tsx'` glob 限定
- [ ] `tsconfig.base.json` 含 `"@": ["apps/desktop/src/renderer/src"]` 与 `"@/*": ["apps/desktop/src/renderer/src/*"]`
- [ ] `tsconfig.base.json` 含 `verbatimModuleSyntax: true`
- [ ] `pnpm -r typecheck` 通过(无 `@` 别名解析错误)

## Phase 3: Medium/Low 改进

- [ ] `utils/subscription-manager.ts` 抽出 `createSubscriptionManager()`
- [ ] plan-store/queue-store/permission-store 用 `createSubscriptionManager()` 替换模块级 `subscribed` + `unsubscribe`
- [ ] `utils/pi-api.ts` 抽出 `getPiAPI()`,替换 session-store/workspace-store/updater-store 重复定义
- [ ] `utils/ipc.ts` 抽出 `partition`/`unwrapIpcResult`,替换 pi-status-store/updater-store 重复
- [ ] session-store/workspace-store/settings-store 的 `loadXxx()` 从 store creator 改为显式 `init()` action
- [ ] `approval-store.ts` `_pendingResolves` 改模块级 Map,不进 state
- [ ] `attachments-store.ts` `byWorkspace` 改 `Record<string, Attachment[]>`
- [ ] `pi-status-store.ts`/`updater-store.ts` `_cleanup` 改模块级变量
- [ ] `approval-store.ts` `waitForApproval` 加默认 5 分钟超时
- [ ] `withValidation(schema, input, onValid)` helper 抽出
- [ ] `setupSessionImporterIpc(importer, prefix)` helper 抽出,claude-sessions + codex-sessions 合并
- [ ] `withPiDriver<T>(fn)` helper 抽出,pi-driver.ipc.ts 5 处重复消除
- [ ] `withPackageSource(handler, action)` helper 抽出
- [ ] `withUpdaterAction(action, errorCode)` helper 抽出
- [ ] `command-risk.test.ts` 覆盖 17 个正则边界 case
- [ ] `rm -rf` 正则覆盖 `--recursive`/`--force` 长选项
- [ ] `chmod 777` 改为 `/\bchmod\s+777\b/`(任何路径)
- [ ] `command-risk.ts` 加 `npm publish`/`docker rm -f`/`kubectl delete`/`terraform destroy`
- [ ] `classifyCommandRisk("")` 返回 "normal"
- [ ] `git.ipc.ts` `git:status`/`git:log`/`git:branches`/`git:checkout`/`git:create-branch` 加 protected path 校验
- [ ] `git.ipc.ts` `gitOriginalContent` 加 protected path 双参校验
- [ ] `local-file-protocol.ts` 集成 `getProtectedPathReason` 拦截敏感文件
- [ ] `local-file://` 协议读取 `.ssh/id_rsa` 等敏感文件被拒绝(手动测试)
- [ ] `config.ipc.ts` `isSafeUrl` IPv4 校验修复(`b1 === 169 && b2 === 254`)
- [ ] `config.ipc.ts` `isSafeUrl` 加 DNS 解析后 IP 二次校验
- [ ] `files.ipc.ts` `files:readTextFile` 改 `fs/promises` 的 `open + read(head, 0, maxBytes)`
- [ ] `files.ipc.ts` `files:search`/`files:list` 加 30s TTL 缓存
- [ ] `settings.ipc.ts` `pi:list-skills` 改 `fs/promises` + 30s TTL 缓存
- [ ] `settings.ipc.ts` `settings:set` `structuredClone(current)` 改为只拷贝被覆盖字段
- [ ] `config/config-manager.ts` 加内存缓存(mtime 失效)
- [ ] `config/config-manager.ts` 原子写(tmp + rename)
- [ ] `approval/pending-edits.ts` 加 `getByToolCallId(toolCallId)` + 缓存 list 结果
- [ ] `approval/interceptor.ts` high risk 分支加 fallback(扩展未加载时走老弹窗)
- [ ] `updater.ts` setInterval/setTimeout 保存引用 + 返回 dispose

## 最终验证

- [ ] `pnpm -r typecheck` 通过
- [ ] `pnpm -r lint` 通过
- [ ] `pnpm -r test` 通过,coverage 达标(lines 70% / functions 70% / branches 60% / statements 70%)
- [ ] `pnpm --filter @pi-desktop/desktop e2e:build` 通过
- [ ] `node scripts/smoke-main-runtime.cjs` 输出 `[PASS]`,且 `out/main/index.js` 未被修改(git diff 干净)
- [ ] 干净 Windows 机器安装 release NSIS installer,app 启动成功,无 native module 错误
- [ ] 多窗口场景(主窗口 + 设置窗口)permission 决策不串扰
- [ ] 多 agent 并发(compose workflow)事件流不互相抑制
- [ ] 大型 git 仓库切换工作区 UI 不卡顿
- [ ] 流式响应期间兄弟组件不 re-render(React DevTools Profiler 验证)
- [ ] Compose 工作流全局超时生效(设短 timeoutMs 测试)
- [ ] Compose dirty repo + commit=true 立即失败,不进入任何阶段
- [ ] AGENTS.md 与实际实现一致(grep `better-sqlite3` 无 import,AGENTS.md 无虚假描述)
- [ ] `shared-types/src/` 拆分为 ~20 文件,`index.ts` 只 re-export
- [ ] `IpcError` 含 `__brand` 字段,调用方必须 narrow
- [ ] `chat.ipc.ts` 拆分为 5 子文件,主文件只保留 `setupChatIpc` 编排
- [ ] `ChatInput.tsx` 拆分后主文件 < 300 行
- [ ] `usePiStream.ts` 不再订阅整个 `useSessionStore`
- [ ] 全代码库无 `execFileSync`/`readFileSync`/`writeFileSync` 在主进程 IPC 路径(除显式标注启动期)
