{
  "name": "@pi-desktop/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types for Pi Desktop",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest --run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "typescript": "^5.6.0"
  }
}
